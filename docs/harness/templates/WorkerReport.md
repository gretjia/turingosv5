# WorkerReport

ATOM:
TASK_REVISION:
WORKER_SLOT:
BRANCH:
PR_URL:
CLAIM_PR_URL:
READY_PR_URL:
WORKTREE:
FILES_CHANGED:
TESTS_RUN:
TEST_RESULTS:
FORBIDDEN_FILES_TOUCHED:
CLASS4_TOUCHED:
NEW_DEPENDENCIES:
KARPATHY_SIMPLE_CODE_CHECK:
  ADDED_DEPENDENCY:
  ADDED_ABSTRACTION:
  ABSTRACTION_REASON:
  LARGEST_NEW_FUNCTION_LINES:
  CHANGED_ONLY_ALLOWED_FILES:
  SMALLEST_SOLUTION_REASON:
WORKER_HALT_CONFIRMATION: [WORKER_HALT]
RISKS:
NEEDS_META:

After PR submission, output `[WORKER_HALT]` and stop this task.
