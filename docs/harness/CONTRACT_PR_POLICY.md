# Contract PR Policy

Shared interfaces require Contract PR before implementation.

Each Contract PR defines schema, input, output, CID/evidence relation, accepted
path relation, rejected path relation, UI-visible fields, hidden/shielded fields,
forbidden leaks, ownership, acceptance tests, and fixtures.

Feature PRs must not drift shared contracts unless explicitly assigned as a
Contract PR.
