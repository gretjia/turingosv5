name: ci-evidence

on:
  pull_request:
  workflow_dispatch:

jobs:
  evidence:
    name: ci-evidence
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Prompt capsule gate
        run: cargo test --test constitution_prompt_capsule
      - name: Attempt telemetry gate
        run: cargo test --test cli_phase63_cas_wire
