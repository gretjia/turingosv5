# TB-16 Charter — Controlled Market Smoke Arena

**Status**: RATIFIED 2026-05-04 (architect spec verbatim per `handover/directives/2026-05-03_TB13_TO_TB17_POST_TB12_ARCHITECT_RULING.md` §7).
**Date**: 2026-05-04.
**Predecessor**: TB-15 SHIPPED 2026-05-03 (`2337381` + R3 closure `eddab36`).
**Authority**: architect §7 (operative spec; FR-16.1..7 + CR-16.1..7 + SG-16.1..8 + forbidden list + halt triggers + Class 3 envelope).
**Implementation contract (design)**: `handover/tests/REAL_LLM_COMPREHENSIVE_AUDIT_FROM_TAPE_DESIGN_2026-05-04.md` (38 audit assertions; 6-task scenario; audit-from-tape contract).
**User authorization (autonomous loop mode)**: 2026-05-04 — *"proceed to TB-16 implementation with architect's directive, auto mode on until dual audit and real comprehensive LLM smoke test passed"*.
**Phase**: **P4 Information Loom** (capacity exit — CompleteSet redeem + autopsy emission run end-to-end on a chain-backed multi-agent run) + **P6 Multi-Org Epistemic Lab v0 prep** (3+ agents in a single sandbox arena = pre-multi-org rehearsal). NOT P7 (no real-world; no public chain; sandbox only).
**Risk class envelope**: **Class 3 integration smoke** (architect §7.7 verbatim). Spans production-wire-up code paths (sequencer dispatch + git2 chain + LLM solver attestation + dashboard regen) → external dual audit MANDATORY at ship per `feedback_dual_audit` + `feedback_risk_class_audit`.
**Iteration cap**: 24h per atom for non-spec atoms; production wire-up exception = 72h-to-feedback-loop on Atom 5+6 per `feedback_iteration_cap_24h` (the real-LLM run IS the feedback loop).

**FC-trace**: `Art.0.2` (Tape Canonical — audit verdict reproducible from L4 + L4.E + CAS only) + `Art.I.1` (cargo workspace tests pass) + `Art.II.1` (typical-error broadcast under N≥3 cluster threshold reused from TB-15) + `Art.III.1..4` (privacy contracts on autopsy private_detail; raw failure shielding; broadcast pollution prevention; Goodhart shield) + `Art.IV` (terminal anchor distribution: OmegaAccepted / MaxTxExhausted / ChallengeUpheldDeferred / TaskExpired / TaskBankrupted) + `Art.V.1` (Generator ≠ Evaluator: comprehensive_arena drives evaluator; SEPARATE audit_tape binary derives verdict; auditor never reads live process state).

**Flowchart-trace**:
- **Flowchart 1 (runtime)**: `FC1-N34` = `audit_tape` binary (NEW; reads runtime_repo + cas_dir + bootstrap files; emits `verdict.json`). `FC1-N35` = `audit_tape_tamper` harness (NEW; tamper-detection harness over copies). `FC1-N36` = `comprehensive_arena` evaluator (NEW; orchestrates 6-task scenario across multi-agent + adversarial-challenger pool).
- **Flowchart 2 (signal)**: `FC2-N31` = audit verdict.json schema v1 (NEW; 38 assertions × 8 layers). `FC2-N32` = dashboard §15 live regen (closes OBS_TB_15_DASHBOARD_LIVE_REGEN_TB16 by walking replayed `agent_autopsies_t`). `FC2-N33` = dashboard §16 sandbox banner (CR-16.7 / SG-16.8).
- **Flowchart 3 (meta)**: `FC3-N44` = real-LLM-driven Markov capsule (first MARKOV_TB-16_2026-05-04.json with non-empty `typical_errors` because TaskBankruptcyTx fires in Task D → autopsies clustered).

**Phase declarations** (per `feedback_tb_phase_tag_required`):

```text
phase_id: P4 Information Loom (capacity exit — first end-to-end run that
                               emits all 13 shipped tx kinds + 6 CAS object
                               types in a single chain-backed multi-agent
                               arena; first cluster-eligible cluster of
                               autopsies because Task D forces ≥1 bankruptcy)
          + P6 Multi-Org Epistemic Lab v0 prep (3+ agents in a single
                               sandbox; sandbox-labeled IDs; multi-org gate
                               is TB-17+ — TB-16 is the rehearsal)

roadmap_exit_criteria_addressed:
  P1-Exit8  state.db deletable; rebuild from L4 alone reproduces head
            state_root. audit_tape binary EXERCISES this exit criterion
            as a system-level acceptance gate (Layer C — replay determinism,
            assertions 12+13+16). MUST PASS.
  P4-Exit1  Single-Agent failure → local error (not global broadcast).
            Carry-forward from TB-15 verified end-to-end across real LLM
            run: Task D solver_1 exhaustion → autopsy in agent_autopsies_t
            for solver_1 only; never in solver_0/2/3 AgentVisibleProjection.
            Layer F assertions 28+29+30+31. MUST PASS.
  P4-Exit2  Multiple Agents same-class failure → typical-error cluster.
            Task D forces ≥3 same-class (Bankruptcy) autopsies (per
            stake-distribution; design §4 specifies ≥3 stakers on the
            bankrupted task). cluster_autopsies(threshold=3) emits a
            TypicalErrorSummary embedded in the Markov capsule. SG-16.7
            verifies. MUST PASS.
  P4-Exit3  Broadcast = abstract rule, not raw failure log.
            cluster_autopsies output contains ONLY public_summary strings;
            never private_detail_cid bytes. Layer F #30 (deserialize
            TypicalErrorSummary; assert no Cid match). MUST PASS.
  P6-Exit1  Multi-org rehearsal in sandbox: 4 solver agents + 1 verifier
            + 1 special CompleteSet operator + 2 sponsors = 8 distinct
            agent-key pairs in a single arena. Sandbox flag in agent IDs
            ("Agent_solver_*" / "tb7-7-sponsor" prefixes; CR-16.7
            sandbox-labeled). Real multi-org (separate cryptographic
            roots-of-trust per org) is P6 v1 / TB-17+; TB-16 is the
            single-org rehearsal under one trust_root.
  P5-prep   First Markov capsule with non-empty typical_errors (TB-15
            ship-time capsule had typical_errors=[] because no ship-time
            bankruptcy). TB-16's capsule is the FIRST cluster-eligible
            Markov head. Layer G #33 verifies recomputation determinism.

kill_criteria_tested:
  conservation_failure
            Layer D #17 (assert_no_post_init_mint over every accepted tx)
            + #18 (total_supply_micro constant across all L4 rows) +
            #19 (CompleteSet MIN-balanced invariant) + #20 (cache=truth
            for task_markets_t.total_escrow). HALT on any failure.
  raw_log_leak
            Layer F #28 (AgentVisibleProjection contains no autopsy/
            private_detail bytes) + #29 (private_detail creator is
            sequencer-epoch only) + #30 (TypicalErrorSummary contains no
            private_detail_cid) + #31 (AutopsyIndex value type is Vec<Cid>
            structural fence). HALT on any leak.
  price_as_truth
            Layer E #25 (every L4.E rejection_class re-derivable from
            re-dispatch — no path through compute_price_index changes
            the rejection class) + #26 (PriceIndex is derived view only,
            never an input to dispatch). architect §7.7 explicit halt.
  non_sandbox_funds_used
            Bootstrap preseed agent IDs match sandbox prefix pattern
            ("Agent_solver_*" / "Agent_verifier_*" / "Agent_user_*" /
            "tb7-7-sponsor"); audit_tape Layer A asserts no production-
            wallet pattern. CR-16.5 + CR-16.7 + SG-16.8.
  unresolved_evidence_gap
            Layer B #9 (every tx_payload_cid resolves in CAS) +
            Layer E #24 (every accepted WorkTx has reachable
            ProposalTelemetry → VerificationResult) + #27 (every
            TerminalSummaryTx has reachable EvidenceCapsule). HALT on
            unresolved CID per architect §7.7.

flowchart_trace:
  Flowchart 1 (runtime):
    FC1-N34 = audit_tape binary (38 assertions; verdict.json output;
                forbidden inputs: live Sequencer / state.db / process
                logs / handover/ai-direct/).
    FC1-N35 = audit_tape_tamper harness (3 temp copies; flip + remove
                + flip-cas; assert TamperDetected on each).
    FC1-N36 = comprehensive_arena evaluator orchestrator (6-task
                scenario; multi-agent + adversarial; sandbox-labeled).
  Flowchart 2 (signal):
    FC2-N31 = verdict.json schema v1 (8 layers × 38 assertions;
                tape_root + tx_kind_counts + assertion table +
                feature_coverage + verdict).
    FC2-N32 = dashboard §15 live regen (replay_full_transition →
                replayed_econ.agent_autopsies_t → autopsy_event_counts).
    FC2-N33 = dashboard §16 sandbox banner (SANDBOX-RUN — NOT
                PRODUCTION — NO REAL FUNDS — SOURCE: tb_16_real_llm_arena_*).
  Flowchart 3 (meta):
    FC3-N44 = real-LLM-driven MARKOV_TB-16_2026-05-04 capsule
                (first capsule with non-empty typical_errors;
                previous_capsule_cid chains to TB-15 capsule).
```

---

## §0 Why TB-16 exists (architect §7 — verbatim)

```text
TB-16 — Controlled Market Smoke Arena

7.1 目标
在受控沙盒中跑通：
compute + position + complete set + price + mask + autopsy
仍不开放真实市场。

7.2 Scenario
Lean task
multiple Agents
WorkTx FirstLong
ChallengeTx Short
CompleteSet share inventory
PriceIndex updates
Boltzmann scheduler selects next candidate
some agents lose positions
Autopsy generated

7.7 Loop-mode instruction
Risk class: Class 3 integration smoke
AI coder may implement autonomously, but ship requires external audit.
Halt if:
  any conservation failure;
  raw log leak;
  price-as-truth behavior;
  non-sandbox funds used;
  unresolved evidence gap.
```

---

## §1 One-line goal

```text
Goal: Drive a single real-LLM evaluator run over 6 engineered Lean
      tasks that exercises EVERY shipped tx type (TB-1..TB-15: Work +
      Verify + Challenge + TaskOpen + EscrowLock + CompleteSetMint +
      CompleteSetRedeem + MarketSeed + FinalizeReward + ChallengeResolve
      + TerminalSummary + TaskExpire + TaskBankruptcy = 13) on a
      multi-agent (≥3) Lean-proof market. Persist the run as a
      chain-backed ChainTape (Sequencer::apply_one + on-disk
      LedgerEntry chain + L4.E rejection ledger + CAS objects). Then
      run a SEPARATE audit_tape binary over ONLY the persisted
      artifacts (runtime_repo + cas_dir + bootstrap files) and emit a
      verdict over 38 enumerated assertions covering chain integrity,
      replay determinism, monetary invariants, predicate fidelity,
      privacy contracts, Markov continuity, and tamper detection.
      Verdict MUST be PROCEED with all 38 assertions GREEN before
      external dual audit gate (Class 3, architect §7.7).

      NO public chain. NO real-money market. NO external domain. NO
      production user funds. NO AMM trading. NO DPMM / pro-rata. NO
      Goodhart leak of private predicates. NO live-process state in
      audit input set.

Key objects (new in TB-16):
  AuditInputs { runtime_repo, cas_dir, agent_pubkeys, pinned_pubkeys,
                genesis, constitution, markov_pointer, alignment_dir }
  AssertionResult { id, name, layer, result: PASS|FAIL|HALT, detail }
  TapeAuditVerdict {
    schema_version: "v1/audit_tape_verdict",
    tape_root: { l4_count, l4e_count, head_state_root_hex,
                 head_ledger_root_hex, cas_object_count,
                 constitution_hash_hex },
    tx_kind_counts: { Work, Verify, Challenge, TaskOpen, EscrowLock,
                      CompleteSetMint, CompleteSetRedeem, MarketSeed,
                      FinalizeReward, ChallengeResolve, TerminalSummary,
                      TaskExpire, TaskBankruptcy },
    assertions: { 1..38 },
    passed: u32, failed: u32,
    feature_coverage: { TB-1..TB-15: GREEN|YELLOW|RED },
    verdict: PROCEED|BLOCK
  }
```

## §1.1 Sandbox contract (architect §7.4 + §7.6 — binding)

```text
- Agent IDs MUST start with one of: "Agent_solver_", "Agent_verifier_",
  "Agent_user_", or be one of the named sponsor IDs ("tb7-7-sponsor").
  Production-wallet patterns are forbidden in TB-16 (CR-16.5).
- Bootstrap preseed total = 30_000_000 μC (matches default_pput_preseed_pairs).
- Dashboard renders SANDBOX banner on every section that surfaces market
  signals; SANDBOX flag in run metadata; verdict.json `feature_coverage`
  carries sandbox=true (CR-16.7, SG-16.8).
- No real-money market: prediction_market.rs legacy CPMM remains
  quarantined per TB-13 Atom 0.5; TB-16 imports nothing from it.
- No external domain: Lean-only (mathd_algebra subset of MiniF2F, in
  trust_root). No medical / legal / financial domain.
```

## §1.2 Halt-trigger contract (architect §7.7 + design §10)

13 halt triggers from design §10 (H1..H13). Each MUST be a unit test in
`tests/tb_16_halt_triggers.rs` that asserts the structural fence holds.
Atom 1 stubs all 13 with `unimplemented!()`; later atoms backfill to GREEN.

| ID | Trigger | Backfill atom |
|---|---|---|
| H1 | Pinned-pubkey verify failure on system tx | Atom 2 (audit_assertions Layer A #3) |
| H2 | Agent-pubkey verify failure on agent tx | Atom 2 (Layer B #8) |
| H3 | Replay state_root mismatch | Atom 2 (Layer C #12) |
| H4 | L4 hash chain broken link | Atom 2 (Layer B #4) |
| H5 | L4.E hash chain broken link | Atom 2 (Layer B #6) |
| H6 | L4.E entry advances logical_t or state_root | Atom 2 (Layer B #6 negative) |
| H7 | L4 row references unresolved CAS Cid | Atom 2 (Layer B #9) |
| H8 | AgentAutopsyCapsule private_detail leak in projection | Atom 2 (Layer F #28) |
| H9 | TypicalErrorSummary contains private_detail_cid | Atom 2 (Layer F #30) |
| H10 | Markov capsule constitution_hash mismatch | Atom 2 (Layer G #32) |
| H11 | Deep-history ingest without override | Atom 2 (Layer G #35 + Atom 6 binary smoke) |
| H12 | LLM self-narrative bytes in autopsy evidence_cids | Atom 2 (Layer F new) |
| H13 | total_supply_micro mutates across L4 rows | Atom 2 (Layer D #18) |

---

## §2 What's already shipped (substrate TB-16 builds on)

| Foundation | Source | TB |
|---|---|---|
| Sequencer dispatch (10 typed-tx arms) | `src/state/sequencer.rs` | TB-1..TB-13 |
| Git2-backed L4 ledger writer | `src/bottom_white/ledger/git2_writer.rs` | TB-6 |
| L4.E rejection ledger | `src/bottom_white/ledger/rejection_evidence.rs` | TB-2 |
| CAS object schema + store | `src/bottom_white/cas/{schema,store}.rs` | foundational |
| Agent Ed25519 pubkey manifest | `src/runtime/agent_keypairs.rs` + `agent_pubkeys.json` | TB-7 |
| Pinned system pubkeys | `pinned_pubkeys.json` | TB-5 |
| `verify_chaintape` (replay verifier) | `src/runtime/verify.rs` | TB-6 |
| `runtime::bootstrap::default_pput_preseed_pairs` | `src/runtime/bootstrap.rs` | TB-10 |
| EconomicState 13 sub-fields incl. `agent_autopsies_t: AutopsyIndex` | `src/state/q_state.rs:170+` | TB-15 |
| `AgentVisibleProjection { views, mask_set }` | `src/state/q_state.rs:131-135` | TB-14 |
| `compute_price_index` pure-fn | `src/state/price_index.rs` | TB-14 |
| `compute_mask_set` (Boltzmann v2) | `src/state/q_state.rs` | TB-14 |
| `cluster_autopsies` + `derive_autopsies_for_bankruptcy` | `src/runtime/autopsy_capsule.rs` | TB-15 |
| `MarkovEvidenceCapsule` + `generate_markov_capsule` binary | `src/runtime/markov_capsule.rs` + `src/bin/generate_markov_capsule.rs` | TB-15 |
| `audit_dashboard` §1-§14 + §15 (skeleton, hard-coded empty autopsy_event_counts) | `src/bin/audit_dashboard.rs` | TB-14+15 |
| `evaluator` real-LLM driver (single-task) | `experiments/minif2f_v4/src/bin/evaluator.rs` | TB-7+9 |
| `lean_market` mode (multi-agent prior smoke) | `experiments/minif2f_v4/src/bin/lean_market.rs` | TB-10+13 |
| First Markov capsule (`MARKOV_TB-15_2026-05-04.json`) | `handover/markov_capsules/` | TB-15 |

**Open follow-ups absorbed by TB-16**:
- `OBS_TB_15_DASHBOARD_LIVE_REGEN_TB16_2026-05-04.md` — closed by Atom 4 (replay-driven autopsy_event_counts).
- TB-15 Markov capsule's `typical_errors=[]` empty (no ship-time bankruptcy) — closed by TB-16 capsule (Task D forces TaskBankruptcyTx → ≥3 autopsies cluster).

---

## §3 Deliverables (8 atoms; Class 3 envelope; production-wire-up exception applies for Atoms 5+6)

### Atom 0 — Charter (this document) (Class 0)
**Action**: this document IS the charter. Self-ratification under autonomous-execution authority granted by user 2026-05-04 (`proceed to TB-16 implementation with architect's directive, auto mode on until dual audit and real comprehensive LLM smoke test passed`). Architect §7 spec is binding; design §3-§10 is the implementation contract.
**Iter cap**: 0h (already complete on commit).

### Atom 1 — Halt-trigger fixture (Class 2, 24h)
**NEW**: `tests/tb_16_halt_triggers.rs` — 13 `#[test]` per architect §7.7 forbidden + design §10 halt list. All stub via `unimplemented!()` until later atoms fill in.
**CP gate**: file compiles; 13 tests fail with `unimplemented!()`.

### Atom 2 — `audit_assertions` module (Class 2, 24h)
**NEW**: `src/runtime/audit_assertions.rs` — 38 pure-fn assertions in 8 layers per design §6.2.
**MOD**: `src/runtime/mod.rs` (`pub mod audit_assertions`).
**Tests** (in-module + `tests/tb_16_audit_assertions.rs`):
- Each layer: at least one happy-path test (constructed AuditInputs from in-memory tape) + at least one negative test (synthetic break → assertion FAIL).
- Determinism test: same inputs → byte-identical AssertionResult vector.
**Halt triggers green**: H1, H2, H3, H4, H5, H6, H7, H8, H9, H10, H12, H13 (12 of 13; H11 is a binary-level fence verified in Atom 6 smoke).
**CP gate**: `cargo test --workspace` PASS.

### Atom 3 — `audit_tape` + `audit_tape_tamper` binaries (Class 3, 24h)
**NEW**: `src/bin/audit_tape.rs` — CLI: parse inputs, build AuditInputs, run all 38 assertions, emit `verdict.json` per design §6.3.
**NEW**: `src/bin/audit_tape_tamper.rs` — fork tape into 3 temp copies, flip 1 byte in random L4 row / random CAS object / remove random L4 row; re-run audit_tape on each; assert TamperDetected verdict; write tamper_report.json.
**Tests** (`tests/tb_16_audit_tape_binary.rs`):
- end-to-end smoke with constructed mini-tape (3 tx); verdict.json contains 38 PASS.
- audit_tape_tamper smoke: 3 corruptions → 3 BLOCK verdicts.
**CP gate**: `cargo run --bin audit_tape -- --help` succeeds; binary tests PASS.

### Atom 4 — Dashboard §15 live regen + §16 sandbox banner (Class 2, 24h)
**MOD**: `src/bin/audit_dashboard.rs` — extend `build_report` with `replay_full_transition` over the L4 chain to reconstruct EconomicState; populate `autopsy_event_counts` from `replayed_econ.agent_autopsies_t.0`. Add §16 SANDBOX banner (CR-16.7, SG-16.8) when run metadata flag `sandbox=true`.
**Tests** (`tests/tb_16_dashboard_live_regen.rs`):
- chain with TaskBankruptcyTx → AgentAutopsyCapsule → dashboard §15 renders expected `autopsy_event_counts`.
- SANDBOX flag → §16 banner present.
**Closes**: `OBS_TB_15_DASHBOARD_LIVE_REGEN_TB16_2026-05-04.md`.
**CP gate**: `cargo test --workspace` PASS.

### Atom 5 — `comprehensive_arena` evaluator (Class 3, 72h-to-feedback-loop exception)
**NEW**: `experiments/minif2f_v4/src/bin/comprehensive_arena.rs` — orchestrator binary:
- Bootstrap: read `default_pput_preseed_pairs` (30M μC total); 4 solver + 1 verifier + 1 special CompleteSet operator + 2 sponsors = 8 sandbox-labeled agents.
- 6 engineered Lean tasks per design §4 (A happy_path, B challenge_dismissed, C challenge_upheld, D exhaustion → bankruptcy, E expiry, F complete_set_market).
- Provider: `deepseek-v4-flash` thinking-off via `src/drivers/llm_proxy.py` round-robin (per `project_chat_over_reasoner` + `feedback_routines_entropy` concurrency=4).
- Caps: TURINGOS_WALL_CLOCK_CAP_MS=1800000 / TURINGOS_COMPUTE_CAP_TOKENS=120000 / TURINGOS_COST_CEILING_USD=15.
- Persistence: writes `runtime_repo/` (Git2 chain) + `cas/` (CAS) + `agent_pubkeys.json` + `pinned_pubkeys.json` + `genesis_payload.toml` copy.
**Tests** (`tests/tb_16_comprehensive_arena_smoke.rs`):
- mock-LLM smoke (no network): 6-task scenario completes; all 13 tx_kinds appear.
**CP gate**: `cargo test --workspace` PASS; `comprehensive_arena --help` succeeds.

### Atom 6 — Run scripts + first real-LLM ship (Class 3, 72h-to-feedback-loop exception)
**NEW**: `handover/tests/scripts/run_real_llm_arena.sh` — CLI wrapper:
1. `cargo build --release --bin comprehensive_arena --bin audit_tape --bin audit_tape_tamper --bin audit_dashboard --bin generate_markov_capsule`
2. Start `src/drivers/llm_proxy.py` (3-key round-robin per `reference_siliconflow` style).
3. Run `comprehensive_arena --out-dir handover/evidence/tb_16_real_llm_arena_<DATE>/`.
4. Run `audit_tape --runtime-repo .../runtime_repo --cas-dir .../cas --... --out verdict.json`.
5. Run `audit_tape_tamper` over the produced tape; emit `tamper_report.json`.
6. Run `generate_markov_capsule --tb-id 16 ... --constitution-path constitution.md`.
7. Run `audit_dashboard --repo .../runtime_repo --cas .../cas --out dashboard.txt`.
8. Run `audit_tape` AGAIN; assert byte-identical verdict.json (replay determinism).
**NEW**: `handover/tests/scripts/audit_tape_smoke_test.sh` — wraps run_real_llm_arena.sh + asserts all 13 expected tx_kinds appear + verdict.json `verdict == "PROCEED"`.
**Run**: `bash handover/tests/scripts/run_real_llm_arena.sh` (requires LLM proxy keys in env).
**Evidence**: `handover/evidence/tb_16_real_llm_arena_2026-05-04/`:
- `runtime_repo/` (Git2 LedgerEntry chain)
- `cas/` (CAS objects)
- `agent_pubkeys.json` + `pinned_pubkeys.json` + `genesis_payload.toml`
- `verdict.json` (38 assertions PASS)
- `verdict_replay.json` (byte-identical re-run)
- `tamper_report.json` (3/3 TamperDetected)
- `dashboard.txt` (15-section render incl. §15 live regen + §16 sandbox)
- `MARKOV_TB-16_2026-05-04.json` (first capsule with non-empty typical_errors)
- `README.md` (gen-command + halt-trigger battery results + acceptance gate table)
**Final gate**: All ship gates SG-16.1..8 GREEN; `cargo test --workspace` PASS; verdict.json verdict=PROCEED.
**Doc**: `handover/ai-direct/TB-16_SHIP_STATUS_2026-05-04.md` (8/8 SG + 13/13 halt-triggers + ledger of FRs/CRs).
**MOD**: `handover/ai-direct/LATEST.md` — append TB-16 ship section.
**Final commit (pre-audit)**: `TB-16 SHIPPED (pre-audit) — Controlled Market Smoke Arena (Class 3 integration smoke; 8/8 SG; 13/13 halt-triggers; all 13 tx kinds + 6 CAS object types exercised on chain-backed real-LLM run; verdict.json PROCEED; closes architect §7 spec FR-16.1..7 + CR-16.1..7 + SG-16.1..8 + closes OBS_TB_15_DASHBOARD_LIVE_REGEN_TB16)`. FC-trace: `FC1-N34 + FC1-N35 + FC1-N36 + FC2-N31 + FC2-N32 + FC2-N33 + FC3-N44`.

### Atom 7 — Class 3 dual external audit (Class 3 — architect §7.7)
**Action**: per `feedback_dual_audit` + `feedback_risk_class_audit` — Class 3 integration smoke requires Codex + Gemini hybrid full dual audit at ship.
**Codex audit**: invoke `codex:rescue` agent with audit prompt; collect verdict.
**Gemini audit**: run `handover/audits/run_gemini_*` against TB-16 ship state; collect verdict.
**Conservative resolution**: VETO > CHALLENGE > PASS per `feedback_dual_audit_conflict`.
**Iterate**: round-cap=2 per `feedback_elon_mode_policy`; cheap fixes get fixed (per `feedback_audit_obs_bias`); multi-hour future-arch OBS-deferred.
**Final commit (post-audit)**: `TB-16 R<N> closure — recursive dual audit PASS PASS / Gemini-degraded PASS / etc.`. FC-trace stays.

---

## §4 Audit mode

| Atom | Risk class | Audit |
|---|---|---|
| 0 | Class 0 | Self (charter; no code) |
| 1 | Class 2 | Self-audit (cargo check + cargo test --workspace) |
| 2 | Class 2 | Self-audit (cargo test --workspace; assertion-by-assertion CP gate) |
| 3 | Class 3 | Self-audit pre-ship + dual external at Atom 7 |
| 4 | Class 2 | Self-audit (cargo test --workspace) |
| 5 | Class 3 | Self-audit pre-ship + dual external at Atom 7 |
| 6 | Class 3 | Self-audit + smoke tape evidence + dual external at Atom 7 |
| 7 | Class 3 | External (Codex + Gemini, conservative resolution) |

Per `feedback_dual_audit` (hybrid by risk class): Class 3 integration smoke = full dual external at ship. **Promotion to Class 4 trigger**: if ANY atom modifies `constitution.md`, `genesis_payload.toml [trust_root]`, `pinned_pubkeys.json` initial set, or kernel.rs admission gate — STOP, re-charter as Class 4.

---

## §5 Forbidden (architect §7.6 + design §9 verbatim)

```text
No public chain.
No real-money market (sandbox-labeled in agent IDs + dashboard banner).
No external domain (Lean only; no medical/legal/financial).
No unbounded leverage.
No AMM trading (TB-13 forbids).
No DPMM / pro-rata.
No production user funds.

Design-specific:
No external attestation in audit verdict (verdict MUST be derivable
  from on-disk artifacts only; live-process state is forbidden input).
No "smoke evidence" labeling (this test produces ChainTape smoke per
  feedback_smoke_evidence_naming binding D5).
No agent-side autopsy invention (autopsies derive from sequencer-side
  derive_autopsies_for_bankruptcy ONLY; LLM self-reports are ignored
  per CR-15.3 + DECISION_LAMARCKIAN §1.2 hard prohibition B).
```

**Halt triggers (instant stop, no round-2)** — design §10 verbatim H1..H13.

---

## §6 Ship gates (architect §7.5 + engineering)

| ID | Gate | Test |
|---|---|---|
| SG-16.1 | Controlled market smoke produces replayable ChainTape | `tb_16_audit_tape_binary` Layer C #12+#13+#16 |
| SG-16.2 | Dashboard shows positions, prices, masks, autopsies | `tb_16_dashboard_live_regen.rs` + Atom 6 dashboard.txt §13+§14+§15 |
| SG-16.3 | No fake accepted nodes | Layer E #23 (every accepted WorkTx has predicate_results.acceptance.* all true) + #24 (VerificationResult{verified:true} reachable) |
| SG-16.4 | Unsolved tasks show failure evidence / bankruptcy anchors | Layer E #25 + #27 (every TerminalSummary has reachable EvidenceCapsule); Task D + Task E coverage |
| SG-16.5 | All market balances conserved | Layer D #17+#18+#19+#20 |
| SG-16.6 | No unresolved evidence gaps | Layer B #9 + Layer E #24+#27 |
| SG-16.7 | At least one loss → autopsy path | Atom 6 Task D forces TaskBankruptcyTx → autopsy emission; verdict.json tx_kind_counts.TaskBankruptcy ≥ 1 + agent_autopsies_t non-empty |
| SG-16.8 | Sandbox flag prevents real-money interpretation | Layer A new (sandbox prefix scan on agent IDs) + dashboard §16 banner test |
| G-16.9 | `cargo test --workspace` ≥ TB-15 baseline / 0 fail / ≤150 ignored | Atom 6 ship report |
| G-16.10 | All 13 tx_kinds present in tape | verdict.json tx_kind_counts each ≥ 1 |
| G-16.11 | All 6 CAS object types reachable | verdict.json layer counts |
| G-16.12 | Replay determinism: byte-identical verdict.json across runs | Layer C #16 + Atom 6 step 8 |
| G-16.13 | First Markov capsule with non-empty typical_errors persisted | `handover/evidence/tb_16_*/MARKOV_TB-16_2026-05-04.json` typical_errors.length ≥ 1 |
| G-16.14 | Tamper detection: 3/3 corruptions detected | Atom 6 tamper_report.json |
| G-16.15 | FC1-N34 + FC1-N35 + FC1-N36 + FC2-N31..N33 + FC3-N44 each have ≥1 witness | `tests/fc_alignment_conformance.rs` |

---

## §7 Auto-resolutions (autonomous judgment log per `feedback_architect_deviation_stance`)

| # | Question | Decision | Reason |
|---|---|---|---|
| A | Test name discipline | exact match: `audit_assertion_layer_<L>_<id>_<name>` | per architect 2026-05-03 §1.2 (test name = traceability contract); names appear verbatim in verdict.json. |
| B | Audit binary input set | runtime_repo + cas + agent_pubkeys + pinned_pubkeys + genesis + constitution + markov_pointer + alignment_dir | per design §6.1 verbatim; live-process state excluded by construction (CR-16.6 replayability + Art.0.2 Tape Canonical). |
| C | tamper-detection scope | 3 modes: flip L4 row, flip CAS object, remove L4 row | per design §6.2 #36-38; ensures hash-chain + CAS-CID + L4-row-count detection paths each tested. |
| D | LLM provider | deepseek-v4-flash thinking-off (per `project_chat_over_reasoner`) | 30-day arc backbone; deterministic thinking-off; prior smokes proven. |
| E | Concurrency | 4 (per `feedback_routines_entropy`) | Earn the cost; balanced with 30-min wall clock cap. |
| F | Wall clock cap | 30 min | per design §5; tighter than typical TB experiment to keep iteration fast. |
| G | Cost ceiling | $15 | per design §5; production-wire-up exception applies but cost ceiling enforced. |
| H | Sandbox prefix list | ["Agent_solver_", "Agent_verifier_", "Agent_user_", "tb7-7-sponsor"] | matches default_pput_preseed_pairs identifiers; Layer A assertion scans this prefix list. |
| I | Dashboard §16 banner | NEW section (not §15 extension) | §15 is autopsy-specific (TB-15); §16 is sandbox flag (TB-16); cohesion. |
| J | Open OBS handling | TB-15 dashboard regen OBS closed by Atom 4; other open OBS (DOOM_LOOP, RESOLUTIONS_INDEX, etc.) carry-forward unchanged | Per `feedback_audit_obs_bias`: cheap fixes get fixed; multi-hour OBS-deferred. |
| K | Markov override semantics | TURINGOS_MARKOV_OVERRIDE=1 enables deeper history; default-deny | TB-15 decision; TB-16 inherits; verified at Layer G #35 + Atom 6 binary smoke. |
| L | Comprehensive_arena fallback for solver_3 | If real LLM agreed-upon-correctness gradient cannot be deterministically forced for Task C, emit deterministic incorrect-proof fixture for solver_3 | Per `feedback_oracle_preflight` + `feedback_smoke_before_batch`: pre-validate scenario forcing; document as auto-resolution if invoked. |
| M | Atom-6 evidence dir name | `handover/evidence/tb_16_real_llm_arena_2026-05-04/` | `_arena` distinguishes from `_chaintape_smoke` and `_real_llm_smoke` patterns; reads as "TB-16 real-LLM controlled-market arena". |
| N | Audit mode for Atom 7 | full Codex + Gemini per Class 3; degrade-to-Codex-only acceptable if Gemini exhausted | Per `feedback_dual_audit` Gemini exhaustion clause; degraded label mandatory. |

---

## §8 Cross-references

- Architect spec (lossless): `handover/directives/2026-05-03_TB13_TO_TB17_POST_TB12_ARCHITECT_RULING.md` §7
- Implementation contract (design): `handover/tests/REAL_LLM_COMPREHENSIVE_AUDIT_FROM_TAPE_DESIGN_2026-05-04.md`
- TB-15 SHIP: commit `2337381` + R3 `eddab36`; charter `handover/tracer_bullets/TB-15_charter_2026-05-03.md`
- TB-14 SHIP: commit `8b93fd9`; charter `handover/tracer_bullets/TB-14_charter_2026-05-03.md`
- TB-13 SHIP: charter `handover/tracer_bullets/TB-13_charter_2026-05-03.md`
- TB-12 SHIP: commit `fa36eca`; charter `handover/tracer_bullets/TB-12_charter_2026-05-03.md`
- DECISION_LAMARCKIAN: `handover/alignment/DECISION_LAMARCKIAN_AUTOPSY_BOLTZMANN_2026-05-02.md`
- Roadmap: `handover/architect-insights/ROADMAP_9_PHASE_2026-04-29.md` (P1-Exit8 + P4 + P6)
- OBS closed by TB-16 Atom 4: `handover/alignment/OBS_TB_15_DASHBOARD_LIVE_REGEN_TB16_2026-05-04.md`
- Predecessor smokes (chain-backed pattern): `handover/evidence/tb_{6,13,14}_chaintape_smoke_*/`
- Memory: `feedback_smoke_evidence_naming` (binding D5), `feedback_o1_chain_on_auditability`, `feedback_no_retroactive_evidence_rewrite`, `feedback_dual_audit` + `feedback_risk_class_audit` (Class 3 = full dual audit at ship), `feedback_kolmogorov_compression`, `feedback_iteration_cap_24h` (production wire-up exception), `feedback_phased_checkpoint`, `feedback_smoke_before_batch`, `feedback_audit_obs_bias`, `feedback_audit_loop_roi_flip`, `feedback_architect_deviation_stance`, `feedback_no_fake_menus`.
