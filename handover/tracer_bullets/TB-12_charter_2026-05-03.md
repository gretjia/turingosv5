# TB-12 Charter — Node Exposure Index

**Status**: **RATIFIED 2026-05-03 by user-architect** ("一直做到双审结束"). Q6 sync mode AMENDED from (ii) to **(ii.5)** — AI coder runs Atom 0.5 → Atom 5 continuously, **continues through Atom 6 dual audit**, **STOPS after audit verdict** (Codex + Gemini complete), presents verdict to user. SHIP at Atom 7 ONLY after explicit user authorization on the verdict. Halting triggers (charter §3 Atom 0; architect ruling §7) remain enforced throughout.
**Date**: 2026-05-03 morning (drafted same day as TB-11 SHIPPED commits 870cd29..3eea2a3 + 2026-05-03 architect ruling on TB-12).
**Predecessor**: TB-11 SHIPPED 2026-05-03 (`3eea2a3`).
**Authority**: 2026-05-03 architect ruling lossless archive at `handover/directives/2026-05-03_TB12_NODE_EXPOSURE_INDEX_ARCHITECT_RULING.md` (operative spec) + 2026-05-02 supplementary directive at `handover/directives/2026-05-02_TB11_TO_TB17_SUPPLEMENTARY_DIRECTIVE.md` (TB-12 §1-§4 base spec).
**Phase**: **P3 carry-forward** (TB-11 G3/G4 closure absorbed as Atom 0.5) + **P4 primary** (NodePosition exposure index — first market-skeleton substrate that TB-13 CompleteSet + TB-14 PriceIndex will build on).
**Risk class**: **Class 3 envelope** (per architect §2 ruling). Per-atom: Atom 0.5 = Class 2 (production wire-up; absorbs already-shipped TB-11 kernel surface). Atom 1 = Class 2 (additive schema). Atom 2 = **Class 3** (touches WorkTx + ChallengeTx accept arms — production money paths). Atoms 3-5 = Class 1-2. Ship audit (Atom 6) = **Class 3 dual** (Codex + Gemini, no honest-deferral).
**Iteration cap**: **72h with 24h checkpoints** (production wire-up exception per `feedback_iteration_cap_24h`).

**Operating mode (Q6 ii.5 — RATIFIED 2026-05-03)**: charter ratification → continuous run Atom 0.5 → Atom 5 → **continue through Atom 6 dual audit (Codex + Gemini)** → **STOP after audit verdict** → SHIP at Atom 7 ONLY after explicit user authorization.

**FC-trace**: `Art. 0.2` (Tape Canonical — NodePosition canonical-encoded; replay-deterministic from L4 + accept-arm side-effect rule) + `Art. I.1` (5-step compile loop — accept-side now derives an exposure record, but DOES NOT affect predicate outcome; Q_t mutation is additive index, not money) + `Art. III.4` (no fake accepted preserved — NodePosition is created on accept, not before; CR-12.3 explicit) + `Art. V.1.3` (Anti-Oreo — NodePosition is sequencer-derived from accepted typed-tx; no agent-callable surface; no system_tx variant added) + `WP-§14.1` (no new typed_tx; pure side-effect on existing accept arms) + `WP-§19` (RSP-2 / RSP-3 substrate — work / challenge stake now leaves a chain-resident exposure trail).

**Flowchart-trace**: **Flowchart 1** (runtime — accepted WorkTx → existing money mutation + NEW NodePosition write to `node_positions_t`; accepted ChallengeTx → existing money mutation + NEW NodePosition write; VerifyTx → NO position; dashboard reads node_positions_t for §13 view) + **Flowchart 2** (boot — runtime preseed unchanged; EconomicState 10→11 sub-fields with backward-compat #[serde(default)] empty index) + **Flowchart 3** (meta — exposure records are TB-13 CompleteSet's claim substrate + TB-14 PriceIndex's price-derivation substrate; TB-12 lays bytes only).

**Phase declarations** (per `feedback_tb_phase_tag_required`):

```text
phase_id: P3 carry-forward (TB-11 G3/G4 closure as Atom 0.5)
          + P4 primary (Node exposure index substrate for TB-13/TB-14)
roadmap_exit_criteria_addressed:
  P3:G3   lean_market 6 subcommands (TB-11 deferred): tick + view-bankruptcy
          land in Atom 0.5; view-positions lands in Atom 4 (TB-12 net-new)
  P3:G4   evaluator forced-exhaust real-LLM smoke (TB-11 deferred):
          Atom 0.5 wires the call site; optional zeta rerun is Atom 0.5
          ship gate, NOT TB-12 main gate
  P4:1    first chain-resident exposure record per accepted WorkTx +
          ChallengeTx with stake > 0 (architect FR-12.1 / FR-12.2)
  P4:2    immutable exposure record discipline — NodePosition created at
          accept; no close / settle / transfer / mark-to-market in TB-12
  P4:3    second-source-of-truth prevention — NO NodeMarketEntry as
          canonical state; that's TB-14 derived view (architect §3.2)
kill_criteria_tested:
  P3:1    no post-init mint — NodePosition is index entry, not money;
          no mint anywhere in dispatch (architect CR-12.1 + CR-12.2)
  P3:2    no stake-less write — accepted WorkTx with stake == 0 → NO
          NodePosition (architect FR-12.1 — "with stake")
  P3:3    no payout from positions — TB-12 has no settle / payout path
          (architect "No settlement change. No payout change.")
  P3:4    no agent-self-reported reward — NodePosition derived from
          sequencer-accepted typed-tx; no agent surface (Anti-Oreo)
  P3:5    no full payout pre-challenge-window — TB-8 invariant unchanged
  P4:4    NodePosition.amount NOT in total_supply_micro (CR-12.2;
          CTF 5-holding sum unchanged at 5 holdings)
flowchart_trace:
  Flowchart 1 (runtime): WorkTx/ChallengeTx accept → NodePosition write
                          (additive side-effect on existing accept arm).
  Flowchart 2 (boot):    EconomicState additive +node_positions_t.
  Flowchart 3 (meta):    NodePosition is the TB-13/TB-14 substrate;
                          TB-12 lays bytes only.
```

---

## §0 Why TB-12 exists (architect ruling §0)

**Architect spec verbatim** (lossless archive §1 / §1.1):

```text
TB-12 = Node exposure index
不是 prediction market
不是 trading market
不是 AMM
不是 CompleteSet
不是 price oracle

可以启动。
但必须满足两个前置条件：
1. TB-11 的核心成果已经 ship；
2. TB-11 遗留 G3/G4 必须作为 TB-12 Atom 0.5 先补齐，不能藏在 Atom 3 中段。
```

**Driver context** (architect §0):
- TB-10 SHIPPED (Lean Proof Task Market MVP).
- TB-11 SHIPPED (Epistemic Exhaust + Capital Liberation; 747 tests).
- Zeta压力测试 demonstrated Invisible Graveyard; TB-11 closes the failure-anchor + capital-liberation gap.
- Constitution invariants preserved: Information is Free, Only Investment Costs Money, 1 Coin = 1 YES + 1 NO, on_init unique mint.

---

## §1 One-line goal

```text
Goal: For every accepted L4 WorkTx with stake > 0, write a FirstLong
      NodePosition to economic_state_t.node_positions_t (a NEW flat
      BTreeMap<TxId, NodePosition>) keyed by source_tx_id. For every
      accepted L4 ChallengeTx with stake > 0, write a ChallengeShort
      NodePosition. VerifyTx writes NOTHING. Dashboard §13 +
      lean_market view-positions render the index as "Exposure records
      per node". CTF + total_supply UNCHANGED. Replay reconstructs
      node_positions_t bit-for-bit from L4 alone. NO trading. NO
      price. NO settlement.

Pre-gate (Atom 0.5): TB-11 G3/G4 closed:
      evaluator binary on MAX_TX exhausted now writes EvidenceCapsule
      + emits TerminalSummary with capsule_cid. lean_market tick +
      view-bankruptcy subcommands callable. Optional zeta-LLM rerun
      produces a single self-contained tar.gz proving G4.
```

---

## §2 What's already shipped (TB-3..TB-11 foundations)

| Foundation | Source | Status |
|---|---|---|
| `WorkTx.stake: StakeMicroCoin` field + accept arm + state-root domain | `src/state/typed_tx.rs:222-236` + `src/state/sequencer.rs` | shipped TB-3 |
| `ChallengeTx.stake: StakeMicroCoin` field + accept arm | `src/state/typed_tx.rs:269-279` + `src/state/sequencer.rs` | shipped TB-4 |
| `VerifyTx.bond: StakeMicroCoin` (NOT a market position; FR-12.3 architect ruling) | `src/state/typed_tx.rs:245-255` | shipped TB-4 |
| `EconomicState` 10 sub-fields (incl. runs_t added by TB-11) | `src/state/q_state.rs:155-175` | shipped TB-3 + TB-11 |
| `stakes_t: StakesIndex` (StakeEntry + task_id backref) | `src/state/q_state.rs:202-220` | shipped TB-3 |
| `challenge_cases_t: ChallengeCasesIndex` (ChallengeCase + status) | `src/state/q_state.rs:402+` | shipped TB-4 + TB-5 |
| `assert_total_ctf_conserved` 5-holding sum | `src/economy/monetary_invariant.rs` | shipped TB-7R |
| `assert_no_post_init_mint` exhaustive match (incl. TaskBankruptcy) | `src/economy/monetary_invariant.rs` | shipped TB-3+ |
| `EvidenceCapsule` + `write_evidence_capsule` writer | `src/runtime/evidence_capsule.rs` | shipped TB-11 (Atom 3) |
| `tb11_emit_terminal_summary_for_run` + `tb11_emit_expire_for_eligible` | `src/runtime/adapter.rs` | shipped TB-11 (Atom 4) |
| `TerminalSummaryTx` (≡ RunExhaustedTx) + `TaskBankruptcyTx` + `TaskExpireTx` dispatch arms | `src/state/sequencer.rs` | shipped TB-11 (Atom 2) |
| `audit_dashboard` §1-§12 (incl. §12 TB-11 Epistemic Exhaust) | `src/bin/audit_dashboard.rs` | shipped TB-11 (Atom 5) |
| `lean_market` 4 subcommands (run-task / view-task / view-wallet / view-replay) | `experiments/minif2f_v4/src/bin/lean_market.rs` | shipped TB-10 |

**Net-new in TB-12** (per architect §8 revised atom plan):

- **Atom 0.5** (TB-11 G3/G4 closure):
  - Evaluator binary on MAX_TX exhausted: build ExhaustionCounts from
    accumulated counters, call `evidence_capsule::write_evidence_capsule`,
    then `runtime::adapter::tb11_emit_terminal_summary_for_run` with
    capsule_cid.
  - `lean_market tick [--expiry-delta <ticks>]` subcommand — opens
    existing chaintape, calls `tb11_emit_expire_for_eligible`, prints
    count + refund total + sponsor balance.
  - `lean_market view-bankruptcy` read-only subcommand — lists tasks
    with `state == Bankrupt` from chaintape replay.
  - Real-LLM zeta-regularization rerun with TB-11 wired in producing a
    single self-contained `runtime_repo.tar.gz` — Atom 0.5 ship gate
    G4-closure (NOT TB-12 main gate).

- **Atom 1** (NodePosition schema):
  - `pub enum PositionSide { Long, Short }`.
  - `pub enum PositionKind { FirstLong, ChallengeShort }`.
  - `pub struct NodePosition { position_id, node_id, task_id, owner,
    side, kind, amount, source_tx, opened_at_round }`.
  - `pub struct NodePositionsIndex(pub BTreeMap<TxId, NodePosition>)`.
  - `EconomicState` 10 → 11 sub-fields with `+node_positions_t:
    NodePositionsIndex` (`#[serde(default)]` for backward-compat).
  - Doc-comment must say "exposure record / derived index, NOT Coin
    holding" (architect §10).

- **Atom 2** (dispatch wire — pure side-effect on existing accept arms):
  - WorkTx accept arm: after existing money/stake mutation, if
    `work.stake.micro_units() > 0`, insert
    `NodePosition { kind: FirstLong, side: Long, node_id =
    work.tx_id, source_tx = work.tx_id, position_id = work.tx_id,
    amount = work.stake, owner = work.agent_id, ... }` into
    `q_next.economic_state_t.node_positions_t`.
  - ChallengeTx accept arm: same shape with `kind: ChallengeShort,
    side: Short, node_id = challenge.target_work_tx, source_tx =
    challenge.tx_id, position_id = challenge.tx_id, amount =
    challenge.stake, owner = challenge.challenger_agent`.
  - VerifyTx accept arm: NO insertion (CR-12.8: bond is responsibility
    bond, not market side).
  - **No change** to `balances_t / stakes_t / challenge_cases_t /
    total_supply_micro`. State-root domain extended additively to fold
    NodePosition write (or kept in existing accept-arm domain — TBD
    Atom 2 design decision; locked by Atom 3 replay test).

- **Atom 3** (replay invariants):
  - 6 deterministic tests proving FR-12.1..8 + CR-12.1..8 + SG-12.4
    (CTF unchanged) + SG-12.5 (replay reconstructs).

- **Atom 4** (CLI / dashboard):
  - `lean_market view-positions [--node-id <tx>] [--owner <agent>]`
    read-only subcommand printing exposure records + per-node Long /
    Short totals.
  - `audit_dashboard` §13 — "Exposure records per node" sub-tables;
    label discipline per architect §8 Atom 4 ("Exposure records", NOT
    "Open market balances").

- **Atom 5** (deterministic integration tests):
  - SG-12.1..8 covered via integration test file
    `tests/tb_12_node_exposure_index.rs`.

- **Atom 6** (audit):
  - `RECURSIVE_AUDIT_TB_12_2026-05-XX.md` (4-clause).
  - Codex impl-paranoid audit doc.
  - Gemini architectural strategic audit doc (degraded label if
    exhausted).

- **Atom 7** (SHIP):
  - LATEST.md + TB_LOG.tsv row + ship commit. ONLY after audit verdict.

---

## §3 Deliverables (8 atoms — architect §8 revised plan)

### Atom 0 — Charter ratification (Class 0)

**Action**: this document IS the ratification draft. Per architect
§11: "Authorize TB-12, but revise charter before coding." Awaiting
user-architect explicit ratification command before Atom 0.5 begins.

§7 open questions auto-resolved per architect §1-§7 + §11
direct-execution口令; user-architect may override before ratify.

**Iter cap**: 1h.

---

### Atom 0.5 — TB-11 G3/G4 carry-forward closure (Class 2)

**Architect mandate** (§1.1 + §8 Atom 0.5): "把 TB-11 carry-forward 从
Atom 3 移到 Atom 0.5。原因：EvidenceCapsule / bankruptcy / exhausted
run 是 NodeMarket 的前置地基。如果失败任务不能被锚定，ShortPosition
就没有可靠的失败对象。"

**Action**:

(a) `experiments/minif2f_v4/src/bin/evaluator.rs`:
   - After main solver loop exits, if `final_run_outcome != OmegaAccepted`:
     - Build `ExhaustionCounts` from accumulated telemetry counters.
     - Read raw run log bytes (existing evaluator log).
     - Call `evidence_capsule::write_evidence_capsule(...)` to obtain
       `capsule.capsule_id`.
     - Call `runtime::adapter::tb11_emit_terminal_summary_for_run(
       sequencer, run_id, task_id, run_outcome,
       total_attempts, failure_class_histogram, last_logical_t,
       solver_agent, evidence_capsule_cid: Some(capsule_id))`.
     - Print receipt info to evaluator stdout for smoke evidence.

(b) `experiments/minif2f_v4/src/bin/lean_market.rs`:
   - New `tick` subcommand:
     - args: `--chaintape <path>` (required) `--expiry-delta <ticks>`
       (default 1000).
     - Opens existing chaintape via `runtime::build_chaintape_sequencer`
       (or attaches to existing repo with nonempty_repo_ok semantics).
     - Calls `tb11_emit_expire_for_eligible(seq, current_logical_t,
       expiry_delta)`.
     - Calls `seq.try_apply_one(&mut rx)` in a loop until queue
       drains.
     - Prints `(count, total_refunded)` + updated sponsor balances.
   - New `view-bankruptcy` subcommand (read-only):
     - args: `--chaintape <path>` (required).
     - Replays chaintape; for each `task_markets_t` entry with
       `state == Bankrupt`, prints task_id + bankruptcy_at_logical_t +
       sponsor + `evidence_capsule_cid` (best-effort lookup from any
       TaskBankruptcy L4 entry for that task_id).
   - **Do NOT add `view-positions` here** (architect §8 Atom 0.5
     explicit). view-positions lands in Atom 4.

(c) **Optional zeta-regularization real-LLM smoke**:
   - New script
     `handover/evidence/tb_11_carryforward_smoke_2026-05-XX/run_smoke.sh`:
     - Bootstraps fresh runtime_repo + agent_keystore.
     - `lean_market run-task --problem zeta_regularization --bounty
       500_000 --max-tx 10` (forced low to ensure exhaustion).
     - After evaluator exits, run `lean_market tick --expiry-delta 1`
       (force expiry).
     - Run `audit_dashboard --include-evidence` and capture §12
       output.
     - Run `verify_chaintape` and capture replay_report.json.
     - tar -czf the resulting runtime_repo + cas.

**Tests**:
- I-A0.5-1 evaluator end-to-end: forced MAX_TX=10 on
  zeta-regularization (deterministic; uses fixture solver mock IF
  real LLM unavailable in CI) → assert
  `economic_state_t.runs_t` non-empty + capsule file in CAS.
- I-A0.5-2 lean_market tick: open + escrow on a task; advance logical
  clock past delta; tick → assert refund + sponsor balance restored.
- I-A0.5-3 lean_market view-bankruptcy: emit TaskBankruptcyTx for a
  task; view-bankruptcy → assert task_id + reason + capsule_cid
  rendered.

**Ship gate (Atom 0.5)**:
- G-A0.5-a evaluator binary builds + runs end-to-end on smoke problem.
- G-A0.5-b `lean_market tick` exits 0 on populated chaintape; emits
  ≥1 TaskExpireTx if eligible.
- G-A0.5-c `lean_market view-bankruptcy` lists ≥0 tasks
  (deterministic empty-state OK if chaintape has no Bankrupt entries).
- G-A0.5-d (optional) zeta-regularization real-LLM run produces
  runtime_repo.tar.gz with EvidenceCapsule + RunExhausted L4 entry +
  TaskExpire post-tick.

**Iter cap**: 18h (evaluator binary modification + 2 new
subcommands + optional real-LLM smoke).

---

### Atom 1 — NodePosition schema (Class 2)

**Action**: pure additive schema; no dispatch logic.

(a) `src/state/typed_tx.rs` (or new `src/state/node_position.rs` if
size warrants — TBD at implementation):

```rust
/// TRACE_MATRIX TB-12 Atom 1 (architect 2026-05-03 ruling §3 + §8
/// Atom 1): position side discriminator. TB-12 only uses Long /
/// Short; Long is from accepted WorkTx.stake; Short is from accepted
/// ChallengeTx.stake. No "Tied" or "Both" — per CR-12.4 + CR-12.8.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize)]
#[repr(u8)]
pub enum PositionSide {
    Long = 0,
    Short = 1,
}

/// TRACE_MATRIX TB-12 Atom 1 (architect §8 Atom 1): position kind.
/// TB-12 only ships FirstLong + ChallengeShort. NO MarketBuy /
/// MarketSell — those are TB-13+ trading layer.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize)]
#[repr(u8)]
pub enum PositionKind {
    FirstLong = 0,
    ChallengeShort = 1,
}

/// TRACE_MATRIX TB-12 Atom 1 (architect §3 + §10): NodePosition is an
/// IMMUTABLE EXPOSURE RECORD. NOT a Coin holding (CR-12.1). NOT in
/// total_supply_micro (CR-12.2). NOT a tradable share balance.
/// Created on accept; never mutated post-creation in TB-12. TB-13
/// CompleteSet introduces actual YES/NO claims; TB-14 PriceIndex
/// introduces price; TB-16 controlled-arena introduces P&L; TB-12
/// just records "this Agent took directional risk on this node".
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Default)]
pub struct NodePosition {
    pub position_id: TxId,         // TB-12: == source_tx (one-source-tx-one-position)
    pub node_id: TxId,             // FirstLong: work_tx_id; ChallengeShort: challenge.target_work_tx
    pub task_id: TaskId,
    pub owner: AgentId,
    pub side: PositionSide,
    pub kind: PositionKind,
    pub amount: MicroCoin,         // == source_tx.stake at creation; immutable
    pub source_tx: TxId,
    pub opened_at_round: u64,
}
```

(b) `src/state/q_state.rs`:

```rust
/// TRACE_MATRIX TB-12 Atom 1 (architect §3 ruling): flat exposure
/// index. NOT NodeMarketEntry (architect §3.2: avoid second source
/// of truth; NodeMarketEntry is TB-14 derived view only).
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Default)]
pub struct NodePositionsIndex(pub BTreeMap<TxId, NodePosition>);
```

Add to `EconomicState`:
```rust
#[serde(default)]
pub node_positions_t: NodePositionsIndex,
```

`#[serde(default)]` for backward-compat with TB-11 chain snapshots
(empty index by default).

(c) Re-exports in `src/state/mod.rs`.

(d) Trust Root rehash for the 2 modified files.

**Tests**:
- U1: NodePosition default round-trips through canonical_encode.
- U2: PositionSide / PositionKind enum discriminants stable
  (`#[repr(u8)]` golden discriminant test).
- U3: NodePositionsIndex empty default serializes to empty BTreeMap.
- U4: EconomicState default has 11 sub-fields including
  `node_positions_t`.

**Forbidden in Atom 1**:
- ❌ Adding `MarketBuy` or `MarketSell` PositionKind variants.
- ❌ Adding `node_market_t: BTreeMap<TxId, NodeMarketEntry>` (architect
  §3 explicit forbid).
- ❌ Mutating WorkTx / ChallengeTx struct schemas.
- ❌ Touching `balances_t / stakes_t / challenge_cases_t`.

**Iter cap**: 4h.

---

### Atom 2 — Dispatch wire (Class 3)

**Action**: extend WorkTx + ChallengeTx accept arms with
NodePosition side-effect. **No change** to existing money/stake
mutation paths — purely additive index write.

(a) `src/state/sequencer.rs` WorkTx accept arm:
   - After existing `q_next` mutation (stakes_t insert + balances_t
     debit + total_escrow consumption), check `work.stake.micro_units() > 0`.
   - If yes, insert NodePosition into
     `q_next.economic_state_t.node_positions_t.0`:
     ```
     position_id: work.tx_id.clone(),
     node_id: work.tx_id.clone(),
     task_id: work.task_id.clone(),
     owner: work.agent_id.clone(),
     side: PositionSide::Long,
     kind: PositionKind::FirstLong,
     amount: work.stake.into(),  // StakeMicroCoin → MicroCoin coercion
     source_tx: work.tx_id.clone(),
     opened_at_round: q.next_logical_t (or work.timestamp_logical),
     ```
   - State-root domain unchanged (existing WORK_ACCEPT_DOMAIN_V1 hash
     of canonical-encoded tx already covers the side-effect because the
     NodePosition fields are deterministic projections of work.tx_id +
     work.stake; no new entropy).

(b) `src/state/sequencer.rs` ChallengeTx accept arm:
   - Same shape with side: Short / kind: ChallengeShort / node_id =
     challenge.target_work_tx / source_tx = challenge.tx_id /
     position_id = challenge.tx_id / owner = challenge.challenger_agent.

(c) VerifyTx accept arm: **no change** (FR-12.3 + CR-12.8).

**Critical invariant** (CR-12.2): NodePosition.amount NOT counted in
total_supply_micro. The 5-holding CTF sum stays:

```
total_supply_micro = Σ balances + Σ escrows + Σ stakes + Σ claims-active + Σ challenge_cases
                       (NodePosition NOT a holding)
```

`assert_total_ctf_conserved` is unchanged (no new holding term).
`assert_no_post_init_mint` is unchanged (NodePosition write is not a
mint — no money created).

(d) Re-emit existing accept-arm `state_root` advance unchanged —
NodePosition write is a deterministic Q-projection from tx fields,
state_root already captures it via canonical-encoded tx hash.

**Tests** (in `src/state/sequencer.rs` test module):
- U-D1: WorkTx with stake > 0 accept → NodePosition inserted with
  expected fields; balances_t / stakes_t / total_supply unchanged
  except via existing TB-3 logic.
- U-D2: WorkTx with stake == 0 → NO NodePosition inserted (per
  FR-12.1 "with stake").
- U-D3: ChallengeTx with stake > 0 accept → ChallengeShort inserted.
- U-D4: VerifyTx accept → node_positions_t unchanged.
- U-D5: assert_total_ctf_conserved passes (NodePosition not in sum).
- U-D6: assert_no_post_init_mint passes on every accept.

**Iter cap**: 8h.

---

### Atom 3 — Replay invariants + total-supply guard (Class 1-2)

**Architect mandate** (§8 Atom 3): "这一步很重要，不能省。"

**Action**: integration tests in `tests/tb_12_node_exposure_index.rs`
proving:

- I-T12-1 `replay_reconstructs_node_positions`: end-to-end
  TaskOpen → EscrowLock → WorkTx → ChallengeTx → VerifyTx → re-run
  `verify_chaintape` from disk → `node_positions_t` reconstructed
  bit-equal to live state.
- I-T12-2 `node_positions_do_not_change_total_supply`: pre/post
  assert_total_ctf_conserved with empty exempt list, with NodePosition
  delta > 0; total_supply_micro bit-equal.
- I-T12-3 `node_position_source_tx_exists_and_is_accepted_l4`:
  for every NodePosition, `source_tx ∈ L4 accepted tx_id set`.
- I-T12-4 `firstlong_node_id_equals_work_tx_id`: for every
  FirstLong, `node_id == source_tx == work.tx_id`.
- I-T12-5 `challengeshort_node_id_equals_target_work_tx`: for every
  ChallengeShort, `node_id == challenge.target_work_tx`.
- I-T12-6 `verifytx_does_not_create_position`: chain with VerifyTx
  but no Work/Challenge → `node_positions_t` empty.

**Iter cap**: 6h.

---

### Atom 4 — Dashboard + lean_market view-positions (Class 1)

**Action**:

(a) `src/bin/audit_dashboard.rs` §13:
   - 1 new audit-row struct `ExposureRecordRow` (position_id, node_id,
     task_id, owner, side, kind, amount, opened_at_round).
   - 1 new DashboardReport field `exposures: Vec<ExposureRecordRow>`.
   - Walk loop: when reading L4, NodePosition is a Q-state entry not
     a typed-tx; the dashboard reads it post-replay from the final
     QState `economic_state_t.node_positions_t`. (Different from §11
     /§12 which walk L4 entries directly; §13 reads post-replay Q.)
   - Render section labeled "§13 TB-12 Node exposure records (per
     architect 2026-05-03 ruling §10: immutable exposure record, NOT
     active market balance)" with sub-tables grouped by node_id and
     by owner.
   - Aggregate: per-node Long total + Short total + net.

(b) `experiments/minif2f_v4/src/bin/lean_market.rs`:
   - New `view-positions` subcommand (read-only):
     - args: `--chaintape <path>` (required) `--node-id <tx>`
       (filter; optional) `--owner <agent>` (filter; optional).
     - Replays chaintape; reads `node_positions_t`; prints filtered
       rows + per-node Long/Short totals.
     - Output header MUST say "Exposure records" (architect §8 Atom 4
       label discipline).

**Tests**:
- U-DASH1: §13 renders correctly with non-empty + empty cases.
- U-CLI1: view-positions subcommand argument parsing.
- I-CLI1: end-to-end view-positions filters by node_id.

**Iter cap**: 4h.

---

### Atom 5 — Smoke / tests (Class 1)

**Action**: `tests/tb_12_node_exposure_index.rs` covering SG-12.1..8.

```text
SG-12.1 accepted_worktx_creates_firstlong_position
SG-12.2 accepted_challengetx_creates_challengeshort_position
SG-12.3 verifytx_does_not_create_node_position
SG-12.4 node_positions_do_not_change_total_supply (Atom 3 I-T12-2)
SG-12.5 replay_reconstructs_node_positions (Atom 3 I-T12-1)
SG-12.6 dashboard_view_positions_works (Atom 4 I-CLI1)
SG-12.7 no_market_trading_variants_introduced (compile-time assert
        via grep diff)
SG-12.8 no_node_market_entry_as_canonical_state (grep
        EconomicState fields == 11; no `node_market_t` field)
```

**Optional carry-forward smoke** (Atom 0.5 G-A0.5-d): zeta
real-LLM rerun → produces single tar.gz at
`handover/evidence/tb_11_carryforward_smoke_2026-05-XX/`.

**Iter cap**: 6h.

---

### Atom 6 — Recursive self-audit + Codex + Gemini (Class 3)

**Action**: produce 3 audit documents.

#### 6.a `handover/audits/RECURSIVE_AUDIT_TB_12_2026-05-XX.md`

4-clause recursive self-audit:

- **Clause 1 — Constitutional preservation**:
  - 1.1 Anti-Oreo: no agent-callable surface added (Atom 2 is
    sequencer-derived).
  - 1.2 Tape Canonical: NodePosition canonical-encoded; deterministic
    from tx fields; replay-deterministic.
  - 1.3 No fake accepted: NodePosition created on accept ONLY; cannot
    influence predicate outcome (CR-12.3).
  - 1.4 1-Coin = 1-YES + 1-NO: NodePosition is NOT a YES or NO claim
    (TB-13 territory); CR-12.7 no automatic YES/NO injection.
  - 1.5 on_init unique mint: no mint added; CR-12.1 NodePosition not
    Coin.

- **Clause 2 — Replay-deterministic**:
  - 2.1 verify_chaintape replays full chain → node_positions_t
    bit-equal (Atom 3 I-T12-1).
  - 2.2 NodePosition fields are deterministic projections of tx
    fields; no environmental input.

- **Clause 3 — Conservation (CTF)**:
  - 3.1 Σ holdings unchanged at 5 (NodePosition NOT in sum) — CR-12.2.
  - 3.2 No mint, no burn, no transfer in NodePosition write.
  - 3.3 assert_no_post_init_mint passes; assert_total_ctf_conserved
    passes empty-exempt.

- **Clause 4 — Architecture-skeleton hygiene** (TB-12-unique):
  - 4.1 NO `node_market_t` as canonical state (architect §3 + SG-12.8).
  - 4.2 Position is immutable exposure record; no close / settle /
    transfer / mark-to-market in TB-12 (architect §10).
  - 4.3 NO trading variants (SG-12.7).
  - 4.4 NO price calculation (CR-12.4).
  - 4.5 VerifyTx.bond ≠ market position (FR-12.3 + CR-12.8).

11 ship gates checklist (G1-G11 mirroring TB-11):
- G1 cargo check passes
- G2 cargo test --workspace passes; workspace_count + delta vs TB-11
  baseline 747 reported
- G3 lean_market 7 subcommands accessible (TB-10 4 + TB-11
  carryforward 2 + TB-12 1 view-positions)
- G4 evaluator MAX_TX exhaust produces EvidenceCapsule + emits
  TerminalSummary in real-LLM smoke (TB-11 carry-forward gate)
- G5 audit_dashboard §13 renders correctly + view-positions CLI works
- G6 verify_chaintape green on smoke runtime_repo
- G7 no new typed_tx variant (grep confirmation)
- G8 no new dispatch arm OUTSIDE WorkTx/ChallengeTx accept-arm
  side-effect (no NodePosition arm; no NodeMarket arm)
- G9 no new TransitionError variant (additive only if any)
- G10 No `node_market_t` canonical EconomicState field (grep + Q field
  count == 11)
- G11 Conservation invariant: Σ holdings = total_supply preserved;
  NodePosition NOT in sum

#### 6.b `handover/audits/CODEX_TB_12_SHIP_AUDIT_2026-05-XX.md`

Class 3 Codex impl-paranoid. Specific questions per architect §8
Atom 6:
- Does NodePosition create a second money ledger?
- Does replay reconstruct positions deterministically?
- Does VerifyTx bond avoid market classification?
- Does NodePosition avoid total supply counting?
- Does TB-12 accidentally implement trading?
- Could a forged or replayed Work/Challenge accept create a position
  that bypasses the stake > 0 gate?

Round-1 verdict + remediation if VETO/CHALLENGE → round-2.

#### 6.c `handover/audits/GEMINI_TB_12_SHIP_AUDIT_2026-05-XX.md`

Class 3 Gemini architectural strategic. Same 6 questions + plus:
- Does the flat NodePositionsIndex extend cleanly to TB-13
  CompleteSet (which will introduce real YES/NO claims) without
  schema collision?
- Does the flat-not-nested decision (architect §3 ruling) hold up at
  TB-14 PriceIndex when long/short aggregation IS needed?

Gemini exhausted → degraded label per architect §5; **NO honest
deferral** (architect explicitly forbade it for TB-12).

**Iter cap**: 12h (3 docs + remediation).

---

### Atom 7 — SHIP (Class 0)

**Action**: ONLY after Atom 6 audit verdict PASS.

- LATEST.md TB-12 section at top.
- TB_LOG.tsv row 34.
- TRACE_FLOWCHART_MATRIX.md TB-12 row planned→shipped.
- handover/evidence/tb_11_carryforward_smoke + (optional) tb_12 dir
  README.
- ship commit:
  ```
  TB-12 SHIPPED — Node Exposure Index (Class 3 dual audit; PASS)
  ```

**Iter cap**: 4h.

---

## §4 Audit mode (Q4 + architect §5)

**Class 3 dual audit at Atom 6** — Codex + Gemini per architect §5.
Gemini exhausted → degraded label, NOT TB-11-style honest deferral
(architect §5 explicit: "do not call it full strategic dual audit").

Conservative verdict wins (VETO > CHALLENGE > PASS) per
`feedback_dual_audit_conflict`.

---

## §5 Forbidden in TB-12 (architect §9.4 verbatim + supplementary)

```text
NO   CompleteSet                                  (defer TB-13)
NO   MarketSeedTx                                 (defer TB-13)
NO   CPMM / AMM / CLOB                            (defer post-TB-14 product decision)
NO   MarketOrderTx / MarketTradeTx                (defer trading layer)
NO   price_yes / price_no calculation             (defer TB-14)
NO   automatic liquidity                          (architect "No automatic liquidity")
NO   ghost liquidity                              (architect "No ghost liquidity")
NO   automatic YES/NO injection                   (CR-12.7)
NO   NodeMarketEntry as canonical EconomicState field  (architect §3 explicit)
NO   settlement change                            (TB-8 settlement-spine unchanged)
NO   payout change                                (TB-8 invariant)
NO   slash change                                 (RSP-3.2 territory)
NO   VerifyTx market position                     (FR-12.3 + CR-12.8)
NO   constitution.md edit                         (sudo-only; Class 4)
NO   bare `cargo test` in TB-12 ship report       (workspace canonical only)
NO   NodePosition close / settle / transfer / mark-to-market  (architect §10: immutable record only)
NO   MarketBuy / MarketSell PositionKind variants (defer trading layer)
```

---

## §6 Ship gates (architect SG-12.1..8 + carry-forward G1..G11)

**Architect SG-12 mandatory** (per supplementary directive 2026-05-02
+ 2026-05-03 ruling §9.3):

```text
SG-12.1  accepted_worktx_creates_firstlong_position passes
SG-12.2  accepted_challengetx_creates_challengeshort_position passes
SG-12.3  verifytx_does_not_create_node_position passes
SG-12.4  node_positions_do_not_change_total_supply passes
SG-12.5  replay_reconstructs_node_positions passes
SG-12.6  dashboard_view_positions_works passes
SG-12.7  no_market_trading_variants_introduced passes
SG-12.8  no_node_market_entry_as_canonical_state passes
```

**Engineering carry-forward G1..G11** (TB-9/10/11 precedent):

```text
G1   cargo check (release) passes
G2   cargo test --workspace passes; workspace_count + delta vs TB-11 747 baseline
G3   lean_market 7 subcommands (TB-10 4 + TB-11 2 carry + TB-12 1 view-positions)
G4   evaluator forced-exhaust real-LLM produces EvidenceCapsule + L4 anchor
     (TB-11 carry-forward; Atom 0.5 G-A0.5-d)
G5   audit_dashboard §13 renders correctly
G6   verify_chaintape 7 indicators GREEN on smoke runtime_repo
G7   diff TB-11→TB-12: zero new typed_tx variant
G8   diff TB-11→TB-12: dispatch arms unchanged outside accept-side side-effect
G9   diff TB-11→TB-12: TransitionError variants additive only (or zero new)
G10  No `node_market_t` canonical EconomicState field; field count == 11
G11  Conservation invariant: 5-holding sum unchanged; NodePosition NOT a holding
```

---

## §7 Open questions (auto-resolved per architect §1-§7 + §11)

| # | Question | Auto-resolution |
|---|---|---|
| Q1 | Risk class | Class 3 envelope; Atom 1 = Class 2; Atom 2 = Class 3; Atom 3-5 = Class 1-2; Atom 6 audit = Class 3 dual (architect §2). |
| Q2 | NodePosition index shape | **FLAT** `node_positions_t: BTreeMap<TxId, NodePosition>`. NO `node_market_t`. NodeMarketEntry is TB-14 derived view (architect §3). |
| Q3 | node_id | FirstLong: work_tx_id; ChallengeShort: challenge.target_work_tx; position_id = source_tx (architect §4). |
| Q4 | Audit mode | Class 3 Codex + Gemini at Atom 6; Gemini exhausted = degraded label (architect §5). NO honest deferral. |
| Q5 | Smoke mode | Deterministic integration tests = TB-12 main gate (Atom 5). zeta real-LLM = Atom 0.5 G-A0.5-d carry-forward gate ONLY (architect §6). |
| Q6 | Sync mode | **(ii.5) — AMENDED 2026-05-03 by user "一直做到双审结束"**: charter ratify → continuous Atom 0.5..5 → continue through Atom 6 dual audit → STOP after audit verdict → SHIP at Atom 7 ONLY after explicit user authorization. (architect §7 + 2026-05-03 user amendment). |
| Q7 | NodePosition mutability | IMMUTABLE per architect §10. No close / settle / transfer / mark-to-market in TB-12. |
| Q8 | View label discipline | "Exposure records" NOT "Open market balances" (architect §8 Atom 4). |
| Q9 | Halting triggers | CTF conservation failure / WorkTx-Challenge position mismatch / NodePosition counted as Coin / replay divergence / Codex or Gemini VETO (architect §7). MUST stop immediately. |

---

## §8 Cross-references

- 2026-05-03 architect ruling lossless: `handover/directives/2026-05-03_TB12_NODE_EXPOSURE_INDEX_ARCHITECT_RULING.md`
- 2026-05-02 supplementary directive (TB-12 base spec): `handover/directives/2026-05-02_TB11_TO_TB17_SUPPLEMENTARY_DIRECTIVE.md`
- TB-11 SHIPPED reference: `handover/ai-direct/LATEST.md` (top section)
- TB-11 charter (precedent for Class 3 dispatch + dual audit shape): `handover/tracer_bullets/TB-11_charter_2026-05-02.md`
- 9-phase roadmap: `handover/architect-insights/ROADMAP_9_PHASE_2026-04-29.md` (now overlay-amended by 2026-05-02 + 2026-05-03 rulings)
- Constitution: `constitution.md` (Art. 0.2, I.1, III.4, V.1.3)
- Memory: `project_tb_12_node_exposure_index`, `project_tb11_to_tb17_roadmap`, `project_tb_11_epistemic_exhaust`, `feedback_o1_chain_on_auditability`, `feedback_kolmogorov_compression`, `feedback_no_retroactive_evidence_rewrite`, `feedback_dual_audit`, `feedback_iteration_cap_24h`, `feedback_workspace_test_canonical`, `feedback_smoke_evidence_naming`, `feedback_no_fake_menus`, `feedback_risk_class_audit`

---

**RATIFIED 2026-05-03 by user-architect** ("一直做到双审结束").

AI coder executes per Q6 (ii.5):
1. Atom 0.5 → Atom 5 continuous (no per-atom stop unless halting trigger fires).
2. Atom 6 dual audit (recursive self-audit + Codex + Gemini).
3. **STOP after audit verdict** — present Codex + Gemini round-1 (and round-2 if remediation occurred) to user.
4. Atom 7 SHIP ONLY after explicit user authorization on the verdict.

Halting triggers (architect §7) MUST stop execution immediately and report:
- CTF conservation failure
- WorkTx/ChallengeTx position mismatch
- NodePosition counted as Coin
- Replay divergence
- Codex / Gemini VETO

End of charter.
