# TB-16.x.2 — Multi-task Chain Continuation (umbrella)

**Date**: 2026-05-04 (charter only; implementation deferred to TB-16.x.2.{1..6} sub-atoms in subsequent sessions)
**Predecessor**: TB-16.x.1 SHIPPED `3f7535d`; TB-16.x.1.5 SHIPPED `3735484`
**Scope**: 6 sub-atoms unblocking the 4 missing tx kinds + Boltzmann RUNTIME exercise + AutopsyCapsule real-bankruptcy chain
**Estimated effort**: 3-5 days total across 6 sub-atoms

---

## §0 Architect-required declarations (per 2026-05-03 anti-drift directive §9)

| Field | Value |
|---|---|
| `phase_id` | **P6** (Permissioned ChainTape / Epistemic Lab — TB-16 conformance closure on real-LLM substrate) |
| `roadmap_exit_criteria_addressed` | TB-16 §7.3 FR-16.3 (challenge tx fired) + FR-16.5 (price update from positions) + FR-16.6 (Boltzmann mask event) + FR-16.7 (AutopsyCapsule generated); §7.5 SG-16.4 (failure evidence / bankruptcy anchor); architect §1.1 7-mechanism RUNTIME exercise (mechanism 5 = Boltzmann) |
| `kill_criteria_tested` | (a) total_supply_micro conservation across all 4 new system-emitted tx kinds; (b) sandbox-prefix invariant (Layer A id=3) honored — no production agent ids leak into emitted txs; (c) replay byte-identity preserved on every sub-atom's evidence chain; (d) no f64 introduced in any new emit path |
| `flowchart_trace` | FC1 (state-transition kernel — adds 4 system-emitted tx kinds to live runtime path) + FC2 (economic flow — TaskExpire refund / TaskBankruptcy state mutation / ChallengeResolve bond release / CompleteSetRedeem share burn) |
| `risk_class` | **Class 2** for sub-atoms 2.1 / 2.3 / 2.5 (env-var-gated arena hooks; no economic semantics change). **Class 3** for sub-atoms 2.2 / 2.4 (touches sequencer / scheduler logic with economic surface implications) |
| `forbidden` | (a) no f64 in economic/refund paths; (b) no L4 vs L4.E confusion (rejected emits go L4.E with `submit_id`, accepted emits go L4 with `state_root_t` advance); (c) no retroactive evidence rewrite (per `feedback_no_retroactive_evidence_rewrite`); (d) no agent-submitted system tx (system-emitted means `emit_system_tx` only); (e) no AMM/CPMM/price-as-truth introduction; (f) no `prediction_market.rs` import (excised in TB-14 Atom 6); (g) no removing existing 38+3 supplemental audit assertions; (h) no agent_id pattern outside `sandbox_prefix` (Layer A id=3 + id=41) |
| `halt_triggers` | HALT if: (i) any sub-atom changes total_supply_micro on accepted-chain; (ii) any sub-atom breaks replay byte-identity; (iii) any sub-atom introduces f64 in economic path; (iv) any sub-atom causes a fence-mechanism doom loop (per `feedback_audit_loop_roi_flip`); (v) any sub-atom skips the smoke evidence requirement (per `feedback_smoke_before_batch`); (vi) `cargo test --workspace` regresses below 907 pass |
| `STEP_B_PROTOCOL` | Sub-atoms 2.1 / 2.3 / 2.5: NOT triggered (env-var hooks in `evaluator.rs` only). Sub-atoms 2.2 / 2.4: TRIGGERED if touching `src/state/sequencer.rs` (challenge-window scheduler / Boltzmann parent selection — both restricted) — use parallel-branch A/B per `STEP_B_PROTOCOL.md`. |

---

## §1 Background

TB-16 R3 Round 2 conformance battery (commit `3cd22d4`) shipped 9-of-13
architect tx kinds across 8 chains, with **4 tx kinds NOT runtime-exercised**:

| Tx kind | Status at R3 | Gap reason |
|---|---|---|
| `ChallengeResolve` | Sequencer dispatch arm exists (sequencer.rs:1415); `emit_system_tx::ChallengeResolve` works (test at 4646); zero-window MVP per adapter.rs:560 — no challenge-window scheduler tick. | No live emit path. |
| `CompleteSetRedeem` | Dispatch arm at sequencer.rs:1736; agent-emitted tx; not wired in evaluator's normal flow. | No env-var trigger; no LLM agent decision flow. |
| `TaskExpire` | Adapter helper `tb11_emit_expire_for_eligible` exists (adapter.rs:659); evaluator does NOT call it. | No env-var trigger in evaluator.rs. |
| `TaskBankruptcy` | Wired via `TURINGOS_FORCE_BANKRUPTCY=1` (evaluator.rs:3059); fires only after MaxTxExhausted. | Works, but cannot fire AFTER an accepted WorkTx (R2 P4 SOLVED in 1 tx; bankruptcy never had a chance). |

Plus two RUNTIME-exercise gaps on already-existing mechanisms:

| Mechanism | Status at R3 | Gap reason |
|---|---|---|
| Boltzmann (mechanism 5) | Structurally fenced (id=26 + halt-triggers H1/H4); scheduler runs INSIDE evaluator's swarm logic; no chain transition. | Single-WorkTx-per-task chains; no parent-selection event. |
| AutopsyCapsule on real-bankruptcy chain | Substrate exists (TB-15 commit `2337381`); FORCE_BANKRUPTCY emits dispatch step 3.5. | R2 P4 SOLVED in 1 tx; never had bankruptcy after accepted WorkTx. |

## §2 Sub-atom plan (6 sub-atoms; ordered by ascending complexity)

### TB-16.x.2.1 — TaskExpire env-var trigger (~half day; Class 2)

**Goal**: After MaxTxExhausted run cleanup (where `TURINGOS_FORCE_BANKRUPTCY` already fires), add a parallel `TURINGOS_FORCE_EXPIRE=1` env-var that calls `tb11_emit_expire_for_eligible(seq, current_logical_t, expiry_delta=0)`.

**Files**:
- `experiments/minif2f_v4/src/bin/evaluator.rs` — insert env-var block after line 3091 (FORCE_BANKRUPTCY end). Use `bundle.sequencer.q_snapshot()` to get `current_logical_t`; pass `expiry_delta=0` to make every Open/Bankrupt task eligible.
- `genesis_payload.toml` — Trust Root rehash for evaluator.rs (R-014, non-sudo per R-018).
- `scripts/run_real_llm_arena.sh` — add `TURINGOS_FORCE_EXPIRE=1` to a new `P9_force_expire` problem profile (mirroring the existing P4_bankruptcy profile).

**Smoke evidence**: arena run with FORCE_EXPIRE=1 + a problem expected to MaxTxExhaust (reuse aime_1997_p9 from R2 P5). Verify chain contains ≥1 TaskExpireTx with `reason: ExpireReason::Deadline` (or `BankruptcyTriggered` if combined with FORCE_BANKRUPTCY).

**Ship gate**: SG-16.x.2.1 = "TaskExpire tx kind is in the union of arena-produced tx kinds" (raises 9-of-13 → 10-of-13).

**Audit assertion**: pre-existing assertions cover; no new assertion needed.

### TB-16.x.2.2 — ChallengeResolve via challenge-window scheduler (~1 day; Class 3)

**Goal**: Set `challenge_window_close_logical_t > 0` for ChallengeTx-targeted WorkTxs (currently always 0 = zero-window MVP per adapter.rs:560). Add a scheduler-tick (called at run cleanup or via separate trigger) that iterates open `challenge_cases_t`, finds those past their window, and emits `ChallengeResolveTx` with `Released` or `UpheldDeferred` based on policy.

**Files**:
- `src/runtime/adapter.rs` — new `tb16_emit_challenge_resolve_for_eligible(seq, current_logical_t, default_resolution=Released)`. Mirrors `tb11_emit_expire_for_eligible` shape.
- `src/state/sequencer.rs` — set `challenge_window_close_logical_t` field non-zero on ChallengeTx admission (~line 793). **STEP_B-PROTOCOL TRIGGERED**: edit via parallel-branch worktree.
- `experiments/minif2f_v4/src/bin/evaluator.rs` — add `TURINGOS_FORCE_CHALLENGE_RESOLVE=1` env-var calling new adapter.
- `genesis_payload.toml` — Trust Root rehashes.
- `scripts/run_real_llm_arena.sh` — add `P10_challenge_resolve` profile.

**Smoke evidence**: arena run with both `TURINGOS_FORCE_CHALLENGER=...` AND `TURINGOS_FORCE_CHALLENGE_RESOLVE=1`. Chain must contain Challenge → ChallengeResolve sequence (raises 10-of-13 → 11-of-13).

**Ship gate**: SG-16.x.2.2 = "Chain contains both ChallengeTx and ChallengeResolveTx with parent-child relationship".

**Audit assertion**: new id=42 `challenge_resolve_chain_to_challenge_tx` (Layer E) — every ChallengeResolveTx must reference a prior ChallengeTx in the same chain.

### TB-16.x.2.3 — CompleteSetRedeem env-var trigger (~half day; Class 2)

**Goal**: Add `TURINGOS_FORCE_REDEEM=<owner>:<event_id>:<outcome>:<share_amount>` env-var that emits a `CompleteSetRedeemTx` from the named owner against the resolved event.

**Files**:
- `experiments/minif2f_v4/src/bin/evaluator.rs` — insert env-var block (mirroring `TURINGOS_COMPLETE_SET_SEED` pattern).
- `genesis_payload.toml` — Trust Root rehash.
- `scripts/run_real_llm_arena.sh` — add `P11_complete_set_redeem` profile.

**Smoke evidence**: arena run with FORCE_REDEEM after FORCE_BANKRUPTCY (so resolution is Deadline-via-bankruptcy). Chain must contain CompleteSetRedeemTx with positive share_amount (raises 11-of-13 → 12-of-13).

**Ship gate**: SG-16.x.2.3 = "Chain contains CompleteSetRedeemTx with non-zero share_amount".

**Audit assertion**: pre-existing id=22 `conditional_shares_excluded_from_supply` covers; no new assertion needed.

### TB-16.x.2.4 — Multi-WorkTx-attempt scenario + Boltzmann RUNTIME (~1 day; Class 3)

**Goal**: Preseed a single task with multiple agents (5+); allow each to attempt the proof; Boltzmann scheduler picks parents from prior attempts. Verify mechanism 5 RUNTIME exercise.

**Files**:
- `experiments/minif2f_v4/src/bin/evaluator.rs` — modify `run_swarm` to allow multiple WorkTx attempts per logical task before TerminalSummary.
- `src/state/sequencer.rs` — verify `boltzmann_select_parent_v2` is called in WorkTx admission path. **STEP_B-PROTOCOL TRIGGERED**.
- `scripts/run_real_llm_arena.sh` — add `P12_boltzmann_runtime` profile.

**Smoke evidence**: arena run produces chain with ≥3 WorkTx attempts on same task, with non-trivial `parent_tx_id` graph (some attempts cite each other as parents, not all root).

**Ship gate**: SG-16.x.2.4 = "Chain contains ≥3 WorkTx with diverse parent_selection_entropy ≥ 0.5 (per Art II.2.1 alarm threshold 0.25)".

**Audit assertion**: new id=43 `boltzmann_parent_selection_diversity` (Layer E) — measures parent_tx_id entropy across WorkTx in same task; flags if < 0.25.

### TB-16.x.2.5 — AutopsyCapsule real-bankruptcy chain (~half day; Class 2)

**Goal**: Force a chain where `FORCE_BANKRUPTCY=1` fires AFTER an accepted WorkTx (the missing R2 P4 path). This generates a real AutopsyCapsule for the agent who proved → got accepted → then the task went bankrupt.

**Files**:
- `experiments/minif2f_v4/src/bin/evaluator.rs` — add a `TURINGOS_FORCE_BANKRUPTCY_AFTER_ACCEPTED=1` mode that delays FORCE_BANKRUPTCY until ≥1 accepted WorkTx (vs. only on MaxTxExhausted).
- `scripts/run_real_llm_arena.sh` — add `P13_autopsy_real` profile.

**Smoke evidence**: arena run produces chain with WorkTx-accepted → TaskBankruptcyTx → AutopsyCapsule emitted (verifiable via TB-15 dispatch step 3.5 path; capsule `loss_amount > 0` and `loss_reason_class != Default`).

**Ship gate**: SG-16.x.2.5 = "Chain contains AutopsyCapsule with non-default loss_reason_class".

**Audit assertion**: pre-existing id=14 `replay_autopsy_index_chains` + id=29 `autopsy_private_detail_creator_is_system` cover; no new assertion needed.

### TB-16.x.2.6 — Combined arena run (all 4 tx kinds + Boltzmann + Autopsy in one chain) (~half day; Class 2)

**Goal**: Single arena run exercising all of 2.1-2.5 in one chain. The "comprehensive R3 conformance" target.

**Files**:
- `scripts/run_real_llm_arena.sh` — `P14_comprehensive` profile combining all FORCE_* env vars.

**Smoke evidence**: arena_run produces chain with **13-of-13 architect tx kinds** + ≥3 WorkTx attempts + AutopsyCapsule. Replay byte-identical. Tamper 3/3 detect.

**Ship gate**: SG-16.x.2.6 = "Single chain covers 13-of-13 tx kinds + Boltzmann RUNTIME exercise + AutopsyCapsule real-bankruptcy".

## §3 Cross-references

- `feedback_iteration_cap_24h` — every sub-atom must produce evaluator signal in 24h; arena smoke is the canonical signal.
- `feedback_smoke_before_batch` — every sub-atom requires a smoke probe before claiming SHIP.
- `feedback_step_b_protocol` — sub-atoms 2.2 / 2.4 touch `src/state/sequencer.rs`; STEP_B-PROTOCOL parallel-branch required.
- `feedback_risk_class_audit` — Class 2 vs Class 3 declared per sub-atom in §2.
- `feedback_dual_audit` — Class 3 sub-atoms (2.2, 2.4) require dual audit (Codex + Gemini); Class 2 sub-atoms can self-audit.
- `feedback_no_retroactive_evidence_rewrite` — sub-atom evidence is forward-looking; do NOT rewrite TB-16 R3 Round 2 evidence.
- TB-16 charter: `handover/tracer_bullets/TB-16_charter_2026-05-04.md` (architect spec).
- TB-16.x.1 charter: `handover/tracer_bullets/TB-16.x.1_charter_2026-05-04.md` (predecessor).
- 2026-05-03 architect roadmap directive — `feedback_kolmogorov_compression` requires lossless archive; reference `project_tb11_to_tb17_roadmap` memory.

## §4 Why this is charter-only (this session)

Sub-atom 2.1 alone requires:
1. Code change in `evaluator.rs` (~30 LoC) + Trust Root rehash
2. Arena smoke run (~30 min on real LLM endpoint)
3. Audit verification (TaskExpire tx in chain + audit_tape PROCEED)
4. Commit + push

That's a full 1-2 hour cycle if everything goes smoothly. The user's
"继续" intent is forward progress, but per `feedback_iteration_cap_24h`
**every PR must produce evaluator pass/fail signal within 24h** —
shipping just the charter without smoke is "spec, not capability" and
default-rejects.

**Position taken**: Ship the umbrella charter as the next-session entry
point. Sub-atom 2.1 implementation + smoke + ship is the next session's
work, with this charter providing the definitive scope, file paths,
ship gates, and risk-class declarations. Architect anti-drift §9
declarations are pre-filled at the umbrella level so each sub-atom
inherits them.

This is consistent with `feedback_no_fake_menus` (state and execute
the recommendation) and `feedback_architect_deviation_stance` (take a
position rather than fence-sit). The charter IS the deliverable; the
sub-atom is the next step.

## §5 Next-session bootstrap

1. Read this charter top-to-bottom.
2. Pick a sub-atom (default: 2.1 — cheapest).
3. Implement per the file paths and ship gates in §2.
4. Run `cargo build --release --bin evaluator`.
5. Run arena: `bash scripts/run_real_llm_arena.sh <profile>`.
6. Verify chain contains the target tx kind via `audit_tape` + grep on `tx_kind_counts`.
7. Update LATEST.md with sub-atom closure block.
8. Commit + push. FC-trace tag `FC1-N<id>` or `FC2-N<id>` per the file edited.

If sub-atom 2.2 or 2.4 (Class 3) — dual audit (Codex + Gemini) BEFORE
ship per `feedback_dual_audit`. Charter declares these as Class 3 in
§0 to flag the requirement upfront.
