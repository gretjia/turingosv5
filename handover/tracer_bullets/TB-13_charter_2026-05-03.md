# TB-13 Charter — CompleteSet + MarketSeedTx (Conditional-Share Substrate)

**Status**: **DRAFT 2026-05-03 evening** (post-TB-12 SHIPPED `fa36eca`). Awaiting user-architect ratification before Atom 0.5 begins. Architect `proceed and recursively check at the best place you decide` reads as ratification + checkpoint authority delegated to AI coder.
**Date**: 2026-05-03 evening (drafted same day as TB-12 SHIPPED + 2026-05-03 architect post-TB-12 ruling).
**Predecessor**: TB-12 SHIPPED 2026-05-03 (`fa36eca`).
**Authority**: 2026-05-03 architect post-TB-12 ruling lossless archive at `handover/directives/2026-05-03_TB13_TO_TB17_POST_TB12_ARCHITECT_RULING.md` (operative spec, §4 TB-13 + §11 loop-mode master instruction) + 2026-05-02 supplementary directive at `handover/directives/2026-05-02_TB11_TO_TB17_SUPPLEMENTARY_DIRECTIVE.md` (TB-13 base spec) + 2026-05-02 lossless integrated edition Part C §9.4 (TB-13 forbidden list canonical).
**Phase**: **P3 carry-forward** (RSP-M Polymarket collateral substrate per 2026-05-02 directive Part C §9.4) + **P4 primary** (CompleteSet is the conditional-share substrate that TB-15 MarkovEvidenceCapsule will compress into Information Loom rollups; first canonical YES_E + NO_E claim accounting).
**Risk class envelope**: **Class 3** (per architect §4.8). Per-atom: Atom 0.5 = Class 1 (forward-fence + label, no production deletion). Atom 1 = Class 2 (additive schema). Atom 2 = **Class 3** (sequencer money path — debits `balances_t`, credits new `conditional_collateral_t`, issues `conditional_share_balances_t`). Atom 3 = Class 3 (extends `monetary_invariant`; touches CTF). Atom 4 = Class 1 (dashboard read-only). Atom 5 = Class 1 (tests). Ship audit (Atom 6) = **Class 3 dual** (Codex + Gemini, no honest-deferral; conservative-verdict-wins).
**Iteration cap**: **72h with 24h checkpoints** (production wire-up exception per `feedback_iteration_cap_24h`; conservation surface).

**Operating mode** (mirrors TB-12 Q6 ii.5 sync mode + architect §4.8 + user 2026-05-03 evening "proceed and recursively check at the best place you decide"):
- Atoms 0.5 → 5 continuous; AI-coder-decided micro-checkpoints at:
  - **CP-A** after Atom 0.5 (cargo test --workspace baseline preserved; forward fence in place).
  - **CP-B** after Atom 2 (sequencer dispatch; STEP_B-protocol; conservation pre-check via assert_total_ctf_conserved).
  - **CP-C** after Atom 3 (monetary_invariant extended; this is the THE money-safety gate).
- Atom 6 dual audit runs continuously; **STOP after audit verdict**.
- Atom 7 SHIP ONLY after explicit user authorization on the verdict.

**FC-trace**: `Art. 0.2` (Tape Canonical — `CompleteSetMintTx` / `CompleteSetRedeemTx` / `MarketSeedTx` canonical-encoded; replay-deterministic) + `Art. I.1` (5-step compile loop — conditional collateral and share accounting are part of the proposal-pricing substrate, but DO NOT affect predicate outcome; CR-13.6 explicit) + `Art. III.4` (no fake accepted preserved — Redeem requires system-resolved outcome reference; agent-callable Redeem rejected pre-resolution) + `Art. V.1.3` (Anti-Oreo — `MarketSeedTx` requires explicit provider funds, NO automatic seed; `CompleteSetMintTx` is agent-driven balance debit, no money created) + `WP-§14.1` (typed_tx variant additive bumps; 3 NEW typed-tx variants; CompleteSetRedeem additionally accepts a `ResolutionRef` from system-emitted resolution; not a unilateral agent variant since pre-resolution rejection is sequencer-side gate) + `WP-§19` (RSP-M collateral substrate — conditional collateral is the YES_E + NO_E claim foundation that TB-14 PriceIndex will derive prices from).

**Flowchart-trace**: **Flowchart 1** (runtime — `CompleteSetMintTx` debits `balances_t`, credits `conditional_collateral_t`, increments equal `conditional_share_balances_t[(owner, event, Yes/No)]`; `CompleteSetRedeemTx` requires system-resolution-reference, debits `conditional_share_balances_t`, credits `balances_t`; `MarketSeedTx` debits `balances_t` of provider, credits `conditional_collateral_t` + records protocol-owned share inventory) + **Flowchart 2** (boot — runtime preseed unchanged; EconomicState 11→13 sub-fields with backward-compat `#[serde(default)]` empty maps) + **Flowchart 3** (meta — conditional shares are TB-14 PriceIndex price-derivation substrate + TB-15 MarkovEvidenceCapsule compression input; TB-13 lays bytes only; no price, no AMM, no orderbook).

**Phase declarations** (per `feedback_tb_phase_tag_required`):

```text
phase_id: P3 carry-forward (RSP-M collateral substrate per 2026-05-02 §9.4)
          + P4 primary (CompleteSet conditional-share substrate; first
                        canonical YES_E + NO_E claim accounting; TB-15
                        Information Loom compression input)
roadmap_exit_criteria_addressed:
  P3:RSP-M  1 locked Coin = 1 YES_E + 1 NO_E mathematical core (architect
            §4.4 FR-13.1 / FR-13.2 / FR-13.3); CTF conserved across mint
            (architect SG-13.1)
  P3:RSP-M  Redeem requires system-resolved outcome (architect FR-13.4
            + SG-13.5); after-YES outcome pays YES not NO (FR-13.5 + SG-13.6)
  P3:RSP-M  MarketSeedTx requires explicit provider funds (FR-13.6 +
            SG-13.3); no auto-seed (forbidden list Part A §4.7); no
            quote/trade/price (FR-13.7)
  P4:1      First canonical YES_E + NO_E claim accounting (replaces
            BinaryMarket f64 CPMM book; EconomicState +2 sub-fields)
  P4:2      Forward-binding fence: TB-13 modules cannot import legacy
            prediction_market.rs (forbidden-token grep ship-gate)
  P4:3      Conditional collateral as Coin holding; conditional shares
            as claims (architect CR-13.3 + CR-13.4); 6-holding CTF sum
            (extends 5→6 holdings; conditional_collateral_t IS a holding)
kill_criteria_tested:
  P3:1      No post-init mint — CompleteSetMintTx is balance-DEBIT-then-
            collateral-CREDIT pair; total Coin conserved bit-for-bit
            (architect SG-13.1; assert_total_ctf_conserved unchanged-output
            after extending exempt list with conditional_collateral_t)
  P3:2      No stake-less write — CompleteSetMintTx with amount==0
            either rejected upstream or NO conditional state mutation;
            MarketSeedTx with collateral==0 rejected (SG-13.4 cannot
            create liquidity without collateral)
  P3:3      Payout sum ≤ collateral — CompleteSetRedeemTx after YES
            redeems at most conditional_share_balances_t[(owner, event,
            Yes)] units against conditional_collateral_t[event]; CTF
            preserved across redeem (no payout > collateral)
  P3:4      No agent-self-reported reward — Redeem requires
            ResolutionRef pointing to a system-emitted resolution (no
            agent unilateral close pre-resolution); SG-13.5 enforced
  P3:5      No full payout pre-challenge-window — Redeem rejected if
            ResolutionRef.outcome state != Resolved or if challenge
            window for the resolving event is still open
  P3:8      Settlement ≤ escrow — TB-8 invariant unchanged; TB-13 does
            NOT touch escrows_t / claims_t finalize path; CompleteSet
            collateral is its own pool
  P4:4      Conditional shares NOT in total_supply_micro — CR-13.3
            verified via SG-13.2 (assert_total_ctf_conserved with
            conditional_share_balances_t in EXEMPT list, not in sum)
flowchart_trace:
  Flowchart 1 (runtime): CompleteSetMintTx → balances_t debit +
                         conditional_collateral_t credit + conditional_
                         share_balances_t equal-YES-NO mint;
                         CompleteSetRedeemTx → conditional_share_balances_t
                         debit + balances_t credit (only after system-
                         resolved outcome);
                         MarketSeedTx → provider balances_t debit +
                         conditional_collateral_t credit + protocol-
                         owned share inventory record (NO trading).
  Flowchart 2 (boot):    EconomicState additive +conditional_collateral_t
                         +conditional_share_balances_t (11→13).
  Flowchart 3 (meta):    Conditional shares are TB-14 PriceIndex price-
                         derivation substrate + TB-15 Information Loom
                         compression input; TB-13 lays bytes only.
```

---

## §0 Why TB-13 exists (architect post-TB-12 ruling §4.1)

**Architect spec verbatim** (lossless archive Part A §4.1):

```text
TB-13 = CompleteSet + MarketSeedTx
引入 Polymarket / CTF 的数学核心：
  1 locked Coin = 1 YES_E + 1 NO_E

但 TB-13 仍然不做：
  AMM / CPMM router / orderbook / MarketOrderTx / MarketTradeTx /
  PriceIndex / DPMM / pro-rata / automatic liquidity

TB-13 只做抵押与份额会计。
```

**Driver context** (architect Part A §1.3 + §4.1):
- TB-12 SHIPPED Node Exposure Index (`fa36eca`); WorkTx.stake → FirstLong, ChallengeTx.stake → ChallengeShort EXPOSURE RECORDS now exist.
- NodePosition is exposure record, NOT YES/NO share, NOT tradable balance, NOT collateral.
- TB-13 introduces the FIRST canonical YES/NO claim accounting; closes the gap between "exposure recorded" (TB-12) and "directional claim against collateral" (TB-13).
- Constitutional invariants preserved: 1 Coin = 1 YES + 1 NO mathematical identity; no automatic injection; on_init unique mint.
- Forward fence: TB-13 modules MUST NOT import legacy `src/prediction_market.rs` f64 CPMM scaffolding (architect §4.2 SG-13.0.1 + halting trigger 1).

---

## §1 One-line goal

```text
Goal: Introduce conditional-share accounting per Polymarket / CTF math
      "1 locked Coin = 1 YES_E + 1 NO_E" via 3 NEW typed_tx variants
      (CompleteSetMintTx, CompleteSetRedeemTx, MarketSeedTx) and 2 NEW
      EconomicState sub-fields (conditional_collateral_t,
      conditional_share_balances_t). CompleteSetMint debits Coin balance,
      credits conditional collateral, mints equal YES_E + NO_E shares.
      CompleteSetRedeem requires system-resolved outcome; after YES
      outcome pays YES, after NO outcome pays NO; pre-resolution
      rejected. MarketSeedTx uses explicit provider funds; no auto-seed;
      no quote/trade/price. CTF conserved (1 Coin → 1 YES + 1 NO);
      shares NOT in total Coin supply. NO AMM. NO CPMM. NO orderbook.
      NO PriceIndex (TB-14). NO trading.

Pre-gate (Atom 0.5): forward fence + label. NEW TB-13 modules cannot
      import src/prediction_market.rs; forbidden-token grep ship-gate
      enforces this. Existing kernel.rs / bus.rs / evaluator.rs CPMM
      wiring is documented as legacy and carry-forward to TB-14 SHIP
      prerequisite (per OBS_TB_12_LEGACY_CPMM_QUARANTINE_2026-05-03;
      retroactive deletion would break 759 passing tests + the
      production evaluator and is OUT OF SCOPE per
      feedback_no_retroactive_evidence_rewrite).
```

---

## §2 What's already shipped (TB-3..TB-12 foundations + TB-13 dependencies)

| Foundation | Source | Status |
|---|---|---|
| `MicroCoin` integer math (no f64 in money path) | `src/economy/money.rs` | shipped TB-3 |
| `BalancesIndex`, `EscrowsIndex`, `StakesIndex`, `ClaimsIndex` | `src/state/q_state.rs:158-162` | shipped TB-3..TB-7R |
| `EconomicState` 11 sub-fields (incl. node_positions_t added by TB-12) | `src/state/q_state.rs:158-192` | shipped TB-3..TB-12 |
| `assert_total_ctf_conserved` 5-holding sum (now 5; conditional_collateral_t will become 6th holding) | `src/economy/monetary_invariant.rs` | shipped TB-7R |
| `assert_no_post_init_mint` exhaustive match (incl. TaskBankruptcy + TB-12 typed-tx unchanged) | `src/economy/monetary_invariant.rs` | shipped TB-3+ |
| `TypedTx` enum + canonical signing payload + state-root domain prefix | `src/state/typed_tx.rs` | shipped TB-3..TB-12 |
| Sequencer dispatch arm pattern (additive variants safe; agent-ingress fail-closed for system-emitted variants) | `src/state/sequencer.rs` | shipped TB-11 + TB-12 |
| `audit_dashboard.rs` §1-§13 (incl. §13 TB-12 Node exposure records) | `src/bin/audit_dashboard.rs` | shipped TB-12 |
| OBS_TB_12_LEGACY_CPMM_QUARANTINE (forward-fence rationale + TB-14 SHIP prerequisite) | `handover/alignment/OBS_TB_12_LEGACY_CPMM_QUARANTINE_2026-05-03.md` | open OBS |

**Net-new in TB-13** (per architect §4.2 + §4.3 + §4.4 + §4.5 + §4.6 + §4.7):

- **Atom 0.5** (legacy CPMM forward-fence + label):
  - Forbidden-token grep ship-gate test (`tests/tb_13_legacy_cpmm_forward_fence.rs`) failing if NEW TB-13 module references `prediction_market::` or `BinaryMarket` or `buy_yes` or `buy_no` or `f64` in a money-path context.
  - Doc-comment "LEGACY: not constitutional, not RSP-M, not production market path; carry-forward to TB-14 SHIP prerequisite per OBS_TB_12_LEGACY_CPMM_QUARANTINE" added to `src/prediction_market.rs` module header + `src/kernel.rs` market field doc-comments.
  - OBS_TB_12_LEGACY_CPMM_QUARANTINE updated to reflect TB-13 forward-fence in place + still carrying TB-14 SHIP prerequisite.
  - **NO retroactive deletion** of `src/prediction_market.rs` or `src/kernel.rs` market scaffolding (production evaluator + 10+ test files would break; out-of-scope per `feedback_no_retroactive_evidence_rewrite`; TB-14 SHIP prerequisite per OBS).

- **Atom 1** (typed_tx schemas):
  - `EventId` newtype (`pub struct EventId(pub TaskId)` — for TB-13 we resolve events 1:1 with task_id; future TB-14+ may decouple).
  - `OutcomeSide` enum `{Yes = 0, No = 1}` with `#[repr(u8)]`.
  - `ShareAmount` newtype `pub struct ShareAmount { pub units: u128 }` (u128 not i128 — shares are non-negative claims; mint creates positive, redeem decreases positive; never negative).
  - `ResolutionRef` newtype referencing the system-emitted resolution (TB-13 reuses `TaskBankruptcyTx.tx_id` for NO outcome + reuses `FinalizeRewardTx.tx_id` for YES outcome — both already system-emitted; this is the "system resolution reference" architect §4.3 mentions).
  - `CompleteSetMintTx { tx_id, parent_state_root, event_id, owner, amount: MicroCoin, signature }` agent-callable.
  - `CompleteSetRedeemTx { tx_id, parent_state_root, event_id, owner, outcome: OutcomeSide, share_amount: ShareAmount, resolution_ref: ResolutionRef, signature }` agent-callable but pre-resolution rejected at sequencer.
  - `MarketSeedTx { tx_id, parent_state_root, event_id, provider, collateral_amount: MicroCoin, signature }` agent-callable; provider == seed_owner.
  - All 3 added to `TypedTx` enum + `CanonicalMessage::*Signing` + 3 new `DOMAIN_*_V1` prefixes + `TxKind::*` discriminants.
  - Round-trip + deterministic-digest unit tests; STEP_B_PROTOCOL parallel-branch since touches `src/state/typed_tx.rs` restricted file.

- **Atom 2** (sequencer dispatch + EconomicState extension):
  - `EconomicState` 11 → 13 sub-fields with `+conditional_collateral_t: ConditionalCollateralIndex` + `+conditional_share_balances_t: ConditionalShareBalances`. Both `#[serde(default)]` for backward-compat with TB-12 chain snapshots.
  - `ConditionalCollateralIndex(BTreeMap<EventId, MicroCoin>)` — locked Coin per event.
  - `ConditionalShareBalances(BTreeMap<(AgentId, EventId, OutcomeSide), ShareAmount>)` — share holdings per (owner, event, side).
  - 3 new dispatch arms in `src/state/sequencer.rs`:
    - `CompleteSetMintTx`: validate balance >= amount; debit `balances_t[owner]` by amount; credit `conditional_collateral_t[event_id]` by amount; credit equal share_amount=amount.units to `conditional_share_balances_t[(owner, event, Yes)]` AND `conditional_share_balances_t[(owner, event, No)]`. (Architect FR-13.1 + FR-13.2 + FR-13.3.)
    - `CompleteSetRedeemTx`: validate `resolution_ref` resolves to either a `TaskBankruptcyTx` (NO outcome) or a `FinalizeRewardTx` (YES outcome) for `event_id`; verify outcome matches Tx outcome field; debit `conditional_share_balances_t[(owner, event, outcome)]` by share_amount; credit `balances_t[owner]` by share_amount.units (1:1 per architect FR-13.5: "after YES outcome pays YES shares"); debit `conditional_collateral_t[event_id]` by share_amount.units. Pre-resolution rejected (architect FR-13.4 + SG-13.5).
    - `MarketSeedTx`: validate `balances_t[provider] >= collateral_amount`; debit `balances_t[provider]`; credit `conditional_collateral_t[event_id]` by collateral_amount; credit `conditional_share_balances_t[(provider, event, Yes)]` AND `(provider, event, No)` each by collateral_amount.units (provider owns BOTH sides; explicit provider-funded; NO trading; architect FR-13.6 + FR-13.7).
  - State-root domain prefixes: `COMPLETE_SET_MINT_DOMAIN_V1`, `COMPLETE_SET_REDEEM_DOMAIN_V1`, `MARKET_SEED_DOMAIN_V1`.
  - 3 new `TransitionError` variants (additive): `InsufficientBalanceForMint`, `RedeemBeforeResolution`, `InsufficientCollateral`.
  - STEP_B_PROTOCOL parallel-branch since touches `src/state/sequencer.rs` restricted file.

- **Atom 3** (monetary_invariant extension):
  - Extend `assert_total_ctf_conserved` to add `conditional_collateral_t` as the 6th Coin holding (architect CR-13.4: "Locked collateral is Coin holding"); EXCLUDE `conditional_share_balances_t` from the sum (architect CR-13.3 + SG-13.2: "shares are not Coin").
  - New invariant `assert_complete_set_balanced`: for every event_id, `Σ conditional_share_balances_t[(*, event, Yes)] == Σ conditional_share_balances_t[(*, event, No)] == conditional_collateral_t[event].units` (1 Coin = 1 YES + 1 NO mathematical identity post-mint and post-redeem).
  - New invariant `assert_no_negative_share_balance`: u128 type-level guarantee + sequencer-side underflow check.

- **Atom 4** (dashboard §14):
  - `audit_dashboard.rs` §14 — "Conditional collateral and share balances" sub-tables; label discipline: "claims, not balances" (architect CR-13.3); "locked collateral, not free coin" (architect CR-13.4).
  - Per-event totals: collateral, YES depth, NO depth, completeness check (depth_yes == depth_no == collateral.units).
  - Per-owner share holdings filtered by event.
  - Render-fn unit-testable per TB-12 §13 precedent.

- **Atom 5** (integration tests SG-13.1..8 + halting-trigger guards):
  - `tests/tb_13_complete_set.rs` covering all SG-13.x with EXACT architect-named functions + halting-trigger guard tests.

- **Atom 6** (audit):
  - `RECURSIVE_AUDIT_TB_13_2026-05-XX.md` (4-clause + complete-set hygiene clause).
  - `CODEX_TB_13_SHIP_AUDIT_2026-05-XX.md`.
  - `GEMINI_TB_13_SHIP_AUDIT_2026-05-XX.md`.

- **Atom 7** (SHIP):
  - LATEST.md + TB_LOG.tsv row + ship commit. ONLY after audit verdict.

---

## §3 Deliverables (8 atoms)

### Atom 0 — Charter ratification (Class 0)

**Action**: this document IS the ratification draft. Architect 2026-05-03 evening "proceed and recursively check at the best place you decide" reads as ratification + checkpoint authority delegation; user-architect may override before Atom 0.5.

**Iter cap**: 0.5h.

---

### Atom 0.5 — Legacy CPMM forward-fence + label (Class 1)

**Architect mandate** (Part A §4.2 + §4.7 forbidden list + §4.8 halting triggers):

```text
FR-13.0.1  src/prediction_market.rs legacy f64 CPMM must be quarantined.
FR-13.0.2  New CompleteSet/MarketSeedTx code must not import legacy
           prediction_market.rs.
FR-13.0.3  No f64 in CompleteSet/MarketSeedTx/market accounting path.
FR-13.0.4  Legacy CPMM must be clearly labeled:
           legacy / not used by RSP-M / not constitutional /
           not production market path.
```

**Action**:

(a) Forward-fence test `tests/tb_13_legacy_cpmm_forward_fence.rs`:
   - Read all NEW TB-13 module sources (`src/state/typed_tx.rs` *additions* — identified by git-blame against TB-12 ship `fa36eca`; `src/economy/conditional_market.rs` if created; `src/state/sequencer.rs` *additions*).
   - Forbidden-token grep on those additions: fails if any of `prediction_market::`, `BinaryMarket`, `buy_yes`, `buy_no`, ` f64 ` (in money-path context — exclude unrelated f64 use like timestamps if any), `MarketError` (legacy variant), `open_bounty_market`, `bounty_market`, `bounty_lp_seed` appear.
   - Implementation: read git-tree `fa36eca:src/state/sequencer.rs` vs HEAD; diff additions; grep additions only.

(b) Label discipline:
   - Update `src/prediction_market.rs` module header doc-comment with:
     ```rust
     //! # LEGACY — TB-3..TB-10 Phase-3A Hayek bounty-market scaffolding
     //!
     //! TRACE_MATRIX TB-13 Atom 0.5 (architect 2026-05-03 ruling Part A §4.2):
     //! This module is **legacy / not constitutional / not RSP-M / not
     //! production market path** in the post-2026-05 architect roadmap. It is:
     //! - **NOT** the canonical YES/NO claim system (TB-13 introduces
     //!   `CompleteSetMintTx` + `ConditionalShareBalances` for that).
     //! - **NOT** the canonical price index (TB-14 introduces `PriceIndex`
     //!   derived from `node_positions_t` long/short interest).
     //! - **NOT** authorized for extension or new use sites.
     //!
     //! ## Carry-forward
     //! Removal is a TB-14 SHIP prerequisite per
     //! `handover/alignment/OBS_TB_12_LEGACY_CPMM_QUARANTINE_2026-05-03.md`.
     //! Retroactive deletion in TB-13 would break the production evaluator
     //! (`experiments/minif2f_v4/src/bin/evaluator.rs:1323` calls
     //! `bus.kernel.market_ticker(5)`) + 10+ test files; that is out-of-scope
     //! per `feedback_no_retroactive_evidence_rewrite` and architect §4.2
     //! halting-trigger semantics (which target NEW TB-13 code, not existing
     //! scaffolding).
     ```
   - Update `src/kernel.rs` `markets` / `bounty_market` / `bounty_lp_seed` field doc-comments with `LEGACY: f64 CPMM scaffolding; see prediction_market.rs module header for migration path`.

(c) OBS update:
   - Append new section to `handover/alignment/OBS_TB_12_LEGACY_CPMM_QUARANTINE_2026-05-03.md` titled "## TB-13 Atom 0.5 update (forward-fence + label in place)" documenting that:
     - Forward-fence ship-gate `tests/tb_13_legacy_cpmm_forward_fence.rs` is in place + passes.
     - Module-header + field-level labels added to `src/prediction_market.rs` and `src/kernel.rs`.
     - Hard removal carries forward to TB-14 SHIP prerequisite (unchanged).

**Tests**:
- I-A0.5-1 `legacy_cpm_api_not_imported_by_complete_set` (architect SG-13.0.1 EXACT name): forbidden-token grep on TB-13 additions passes.
- I-A0.5-2 `no_f64_in_complete_set_or_market_seed` (architect SG-13.0.2 EXACT name): grep ` f64 ` in TB-13 additions fails (zero matches).
- I-A0.5-3 `prediction_market_legacy_quarantined` (architect SG-13.0.3 EXACT name): module header doc-comment contains "legacy" + "not constitutional" + "not RSP-M" tokens; verified via `include_str!`.

**Ship gate (Atom 0.5)**:
- G-A0.5-a SG-13.0.1..3 (3 EXACT-named tests) pass.
- G-A0.5-b OBS_TB_12_LEGACY_CPMM_QUARANTINE updated with TB-13 status (architect SG-13.0.4 — "OBS either closed or explicitly carried as non-importable legacy"; we carry).
- G-A0.5-c `cargo test --workspace` count baseline preserved at 759 (TB-12 baseline) plus the 3 new SG-13.0.x tests = 762; failed=0; ignored=150 unchanged.

**Halting triggers** (Part A §4.2):
- HALT if a NEW TB-13 module imports legacy `prediction_market`.
- HALT if `f64` appears in NEW CompleteSet / MarketSeed code.
- HALT if any AMM / CPMM router function is INTRODUCED in TB-13.

**Iter cap**: 4h.

**Checkpoint CP-A**: After Atom 0.5, AI coder pauses to verify forward-fence is robust before proceeding to Atom 1 typed-tx schemas.

---

### Atom 1 — TB-13 typed_tx schemas (Class 2)

**Action**: pure additive schema; no dispatch logic.

(a) `src/state/typed_tx.rs` additions (STEP_B_PROTOCOL parallel-branch):

```rust
/// TRACE_MATRIX TB-13 Atom 1 (architect 2026-05-03 ruling Part A §4.3):
/// Event identifier for conditional shares. TB-13 maps EventId 1:1 to
/// TaskId; future TB-14+ may decouple (e.g., per-node events).
#[derive(Debug, Clone, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize)]
pub struct EventId(pub TaskId);

/// TRACE_MATRIX TB-13 Atom 1 (architect §4.3): outcome side discriminator.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize)]
#[repr(u8)]
pub enum OutcomeSide {
    Yes = 0,
    No = 1,
}

/// TRACE_MATRIX TB-13 Atom 1 (architect §4.3): non-negative share count.
/// Architect spec uses `units: i128`; we use u128 since shares can never
/// go negative (mint creates positive; redeem decreases positive; no debt
/// model in TB-13). Underflow is sequencer-side error (RedeemMoreThanOwned).
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize, Default)]
pub struct ShareAmount {
    pub units: u128,
}

/// TRACE_MATRIX TB-13 Atom 1 (architect §4.3): system-resolution
/// reference. References either a `TaskBankruptcyTx` (NO outcome) or a
/// `FinalizeRewardTx` (YES outcome) for the event. Sequencer validates
/// the reference exists in L4 + outcome matches before allowing redeem.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct ResolutionRef {
    pub resolution_tx_id: TxId,
    pub claimed_outcome: OutcomeSide,
}

/// TRACE_MATRIX TB-13 Atom 1 (architect §4.3 + §4.4 FR-13.1..3):
/// CompleteSetMintTx — agent debits Coin balance, locks as conditional
/// collateral, receives equal YES_E + NO_E shares.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct CompleteSetMintTx {
    pub tx_id: TxId,
    pub parent_state_root: Hash,
    pub event_id: EventId,
    pub owner: AgentId,
    pub amount: MicroCoin,
    pub signature: AgentSignature,
}

/// TRACE_MATRIX TB-13 Atom 1 (architect §4.3 + §4.4 FR-13.4..5):
/// CompleteSetRedeemTx — agent claims winning shares post-resolution.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct CompleteSetRedeemTx {
    pub tx_id: TxId,
    pub parent_state_root: Hash,
    pub event_id: EventId,
    pub owner: AgentId,
    pub outcome: OutcomeSide,
    pub share_amount: ShareAmount,
    pub resolution_ref: ResolutionRef,
    pub signature: AgentSignature,
}

/// TRACE_MATRIX TB-13 Atom 1 (architect §4.3 + §4.4 FR-13.6..7):
/// MarketSeedTx — provider explicitly funds collateral + protocol-owned
/// share inventory. NO trading. NO quoting. NO pricing.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct MarketSeedTx {
    pub tx_id: TxId,
    pub parent_state_root: Hash,
    pub event_id: EventId,
    pub provider: AgentId,
    pub collateral_amount: MicroCoin,
    pub signature: AgentSignature,
}
```

(b) `TypedTx` enum extended with 3 new variants; `TxKind::*` extended with 3 new discriminants; `CanonicalMessage::*Signing` extended with 3 new variants; 3 new `DOMAIN_*_V1` prefix consts; `TxId::compute` gains 3 new domain matches.

(c) `src/state/mod.rs` re-exports updated.

(d) Trust Root rehash for `src/state/typed_tx.rs`.

**Tests**:
- U1 `complete_set_mint_round_trips_canonical`: round-trip canonical encode + decode.
- U2 `complete_set_redeem_round_trips_canonical`: same.
- U3 `market_seed_round_trips_canonical`: same.
- U4 `outcome_side_repr_u8_stable`: discriminant golden test (Yes=0, No=1).
- U5 `share_amount_default_zero_units`.
- U6 `event_id_round_trip`.
- U7 `tb_13_typed_tx_deterministic_digest`: same payload twice → same `tx_id`.

**Forbidden in Atom 1**:
- ❌ Adding `MarketBuyTx` / `MarketSellTx` / `MarketOrderTx` / `MarketTradeTx`.
- ❌ Adding `price_yes` / `price_no` fields to MarketSeedTx (TB-14 territory).
- ❌ f64 anywhere in new structs.
- ❌ `i128` in `ShareAmount` (use u128; shares are non-negative).
- ❌ Removing or modifying any TB-3..TB-12 typed_tx variant.

**Iter cap**: 6h.

---

### Atom 2 — Sequencer dispatch + EconomicState 11→13 (Class 3)

**Action**: STEP_B_PROTOCOL parallel-branch since touches `src/state/sequencer.rs` and `src/state/q_state.rs` restricted files.

(a) `src/state/q_state.rs` extension:

```rust
/// TRACE_MATRIX TB-13 Atom 2 (architect Part A §4.3): conditional
/// collateral per event. Locked Coin held against outstanding YES_E +
/// NO_E share inventory. CR-13.4: locked collateral IS a Coin holding;
/// included in 6-holding CTF sum (extends 5→6).
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Default)]
pub struct ConditionalCollateralIndex(pub BTreeMap<EventId, MicroCoin>);

/// TRACE_MATRIX TB-13 Atom 2 (architect §4.3): conditional share
/// balances per (owner, event, side). CR-13.3: shares are claims, NOT
/// Coin; EXCLUDED from total Coin supply.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Default)]
pub struct ConditionalShareBalances(
    pub BTreeMap<(AgentId, EventId, OutcomeSide), ShareAmount>,
);
```

Add to `EconomicState`:
```rust
#[serde(default)]
pub conditional_collateral_t: ConditionalCollateralIndex,
#[serde(default)]
pub conditional_share_balances_t: ConditionalShareBalances,
```

Update field-count test: 11 → 13.

(b) `src/state/sequencer.rs` 3 new dispatch arms:

**CompleteSetMintTx accept arm**:
```text
1. Verify signature against pinned_pubkeys[owner].
2. Verify balances_t[owner] >= amount → InsufficientBalanceForMint else.
3. Debit balances_t[owner] by amount.
4. Credit conditional_collateral_t[event_id] by amount.
5. Credit conditional_share_balances_t[(owner, event_id, Yes)] by amount.units.
6. Credit conditional_share_balances_t[(owner, event_id, No)] by amount.units.
7. Advance state_root via COMPLETE_SET_MINT_DOMAIN_V1.
```

**CompleteSetRedeemTx accept arm**:
```text
1. Verify signature against pinned_pubkeys[owner].
2. Verify resolution_ref.resolution_tx_id ∈ L4 accepted set; lookup the
   referenced tx.
3. If TaskBankruptcyTx for event_id.0 (== task_id): claimed_outcome must
   be No; else InvalidResolutionRef.
4. If FinalizeRewardTx for event_id.0: claimed_outcome must be Yes; else
   InvalidResolutionRef.
5. Else (no resolution found / wrong type): RedeemBeforeResolution.
6. Verify conditional_share_balances_t[(owner, event_id, outcome)] >=
   share_amount.units; else RedeemMoreThanOwned.
7. Verify conditional_collateral_t[event_id] >= share_amount.units; else
   InsufficientCollateral (defensive; should never fire if invariant holds).
8. Debit conditional_share_balances_t[(owner, event_id, outcome)] by
   share_amount.units.
9. Debit conditional_collateral_t[event_id] by share_amount.units.
10. Credit balances_t[owner] by share_amount.units (1:1 winning-share
    redemption).
11. Advance state_root via COMPLETE_SET_REDEEM_DOMAIN_V1.
```

**MarketSeedTx accept arm**:
```text
1. Verify signature against pinned_pubkeys[provider].
2. Verify collateral_amount > 0; else InsufficientCollateral.
3. Verify balances_t[provider] >= collateral_amount; else
   InsufficientBalanceForMint (architect SG-13.3).
4. Debit balances_t[provider] by collateral_amount.
5. Credit conditional_collateral_t[event_id] by collateral_amount.
6. Credit conditional_share_balances_t[(provider, event_id, Yes)] by
   collateral_amount.units.
7. Credit conditional_share_balances_t[(provider, event_id, No)] by
   collateral_amount.units.
8. Advance state_root via MARKET_SEED_DOMAIN_V1.
```

(c) Agent-ingress fail-closed: extend `submit_agent_tx` ingress matcher to accept the 3 new variants; verify signature; reject if signing payload mismatch.

(d) `TransitionError` additive: `InsufficientBalanceForMint`, `RedeemBeforeResolution`, `RedeemMoreThanOwned`, `InsufficientCollateral`, `InvalidResolutionRef`.

(e) Trust Root rehash for `src/state/sequencer.rs` + `src/state/q_state.rs`.

**Tests** (in `src/state/sequencer.rs` test module):
- U-D1 `mint_debits_balance_credits_collateral_and_shares`: balances_t -= amount, conditional_collateral_t[event] += amount, both YES_E and NO_E shares += amount.units.
- U-D2 `mint_fails_with_insufficient_balance`.
- U-D3 `redeem_before_resolution_rejected`.
- U-D4 `redeem_after_yes_pays_yes_not_no`: with FinalizeRewardTx resolution_ref + outcome=Yes; YES share balance -= amount; NO share balance unchanged; balances_t += amount.
- U-D5 `redeem_after_no_pays_no_not_yes`: with TaskBankruptcyTx resolution_ref + outcome=No.
- U-D6 `redeem_with_wrong_outcome_for_resolution_rejected`.
- U-D7 `market_seed_requires_balance`.
- U-D8 `market_seed_zero_collateral_rejected`.
- U-D9 `market_seed_provider_owns_both_sides`.

**Iter cap**: 12h.

**Checkpoint CP-B**: After Atom 2, AI coder runs `cargo test --workspace` and verifies field-count test (11→13), all U-D1..9 PASS, no regression in TB-3..TB-12 tests. Conservation invariant test still passes (until Atom 3 extends it). Then proceeds to Atom 3.

---

### Atom 3 — Conservation invariant extension (Class 3)

**Action**: extend `src/economy/monetary_invariant.rs`.

(a) `assert_total_ctf_conserved`:
   - 5-holding sum → 6-holding sum: `total_supply_micro = Σ balances_t + Σ escrows_t + Σ stakes_t + Σ claims_t + Σ challenge_cases_t + Σ conditional_collateral_t`.
   - `conditional_share_balances_t` EXCLUDED (architect CR-13.3 + SG-13.2: "shares are not Coin").

(b) `assert_complete_set_balanced` NEW:
   - For every `event_id ∈ conditional_collateral_t.keys()`:
     - `Σ_{owner} conditional_share_balances_t[(owner, event_id, Yes)].units == conditional_collateral_t[event_id].units`
     - AND `Σ_{owner} conditional_share_balances_t[(owner, event_id, No)].units == conditional_collateral_t[event_id].units`
   - Holds post-mint (1 Coin → 1 YES + 1 NO equal mint).
   - Holds post-redeem (debit equal in YES side and collateral OR equal in NO side and collateral).

(c) `assert_no_post_init_mint`:
   - Add 3 new TypedTx variants to exhaustive match: CompleteSetMint, CompleteSetRedeem, MarketSeed. None create money (mint = balance debit + collateral credit; redeem = collateral debit + balance credit; seed = balance debit + collateral credit). Match arms re-assert via assertion that no balance increased net of debit+credit.

(d) Re-emit `EconomicState` field count test: 13.

**Tests**:
- I-T13-1 `mint_preserves_total_supply`: pre/post mint, total_supply 6-holding sum bit-equal.
- I-T13-2 `redeem_preserves_total_supply`.
- I-T13-3 `market_seed_preserves_total_supply`.
- I-T13-4 `complete_set_balanced_post_mint`.
- I-T13-5 `complete_set_balanced_post_redeem`.
- I-T13-6 `shares_excluded_from_total_supply`: assert `conditional_share_balances_t` units NOT in total_supply_micro sum.

**Iter cap**: 6h.

**Checkpoint CP-C**: After Atom 3, AI coder runs full `cargo test --workspace` + verifies CP-C kill criteria:
- assert_total_ctf_conserved passes pre/post every TB-13 typed_tx
- assert_complete_set_balanced holds at every state transition
- assert_no_post_init_mint passes (no money created)
- TB-3..TB-12 baseline tests unchanged (no regression on the 759+3 baseline)
**This is THE money-safety gate**. If any of these fails, HALT immediately and report.

---

### Atom 4 — Dashboard §14 (Class 1)

**Action**: extend `src/bin/audit_dashboard.rs` with §14 conditional-shares view.

(a) New audit-row structs:
   - `ConditionalCollateralRow { event_id, locked_collateral, share_depth_yes, share_depth_no, balanced: bool }`.
   - `ConditionalShareRow { owner, event_id, side, share_amount }` filtered.

(b) New `DashboardReport` field `conditional_shares: ConditionalSharesSection { collaterals: Vec<...>, holdings: Vec<...> }`.

(c) Walk: post-replay reads from `economic_state_t.conditional_collateral_t` + `.conditional_share_balances_t`; aggregates per-event YES depth + NO depth + balanced flag.

(d) Render section: "§14 TB-13 Conditional collateral and share balances (per architect 2026-05-03 ruling Part A §4: claims, NOT Coin balance; locked collateral, NOT free coin)".

(e) Pure-fn helper `render_section_14(report: &DashboardReport) -> String` for unit-testability per TB-12 §13 precedent.

**Tests**:
- U-DASH1 `sg_13_dashboard_renders_conditional_shares`: 4 cases (empty / mint-only / redeem-after-yes / market-seed-only) with forbidden-token grep ("market price" / "tradable" / "AMM" must NOT appear).

**Iter cap**: 4h.

---

### Atom 5 — Integration tests SG-13.1..8 (Class 1)

**Action**: `tests/tb_13_complete_set.rs` covering SG-13.1..8 with EXACT architect-named functions.

```text
sg_13_1_mint_one_coin_yields_one_yes_plus_one_no_total_coin_conserved
sg_13_2_yes_no_shares_not_in_total_coin_supply
sg_13_3_market_seed_fails_if_provider_lacks_balance
sg_13_4_market_seed_cannot_create_liquidity_without_collateral
sg_13_5_redeem_unavailable_before_outcome_resolution
sg_13_6_redeem_after_yes_outcome_pays_yes_not_no
sg_13_7_no_f64_in_new_complete_set_or_market_seed_path
sg_13_8_no_import_or_use_of_legacy_cpmm_in_tb13_modules
```

Plus halting-trigger guards (architect §4.8):
- `halt_total_supply_micro_unchanged_across_mint_redeem`
- `halt_shares_not_counted_as_coin`
- `halt_market_seed_zero_balance_provider_rejected`
- `halt_no_legacy_cpmm_import_in_tb13_modules` (delegates to SG-13.0.x grep)
- `halt_no_f64_in_new_market_modules` (delegates to SG-13.0.2)
- `halt_no_price_amm_or_trade_logic` (forbidden-token grep on TB-13 additions: `price_yes`, `price_no`, `AMM`, `CPMM`, `MarketOrder`, `MarketTrade` — zero matches in TB-13 additions).

**Iter cap**: 6h.

---

### Atom 6 — Recursive self-audit + Codex + Gemini dual audit (Class 3)

**Action**: produce 3 audit documents.

#### 6.a `handover/audits/RECURSIVE_AUDIT_TB_13_2026-05-XX.md`

5-clause recursive self-audit:

- **Clause 1 — Constitutional preservation** (Anti-Oreo, Tape Canonical, no fake accepted, on_init unique mint, 1 Coin = 1 YES + 1 NO).
- **Clause 2 — Replay-deterministic** (verify_chaintape replays with conditional_collateral_t + conditional_share_balances_t bit-equal).
- **Clause 3 — Conservation (CTF)** (6-holding sum; complete-set balanced; assert_no_post_init_mint).
- **Clause 4 — Resolution gating** (Redeem cannot fire pre-resolution; resolution_ref ↔ outcome match enforced; system-emitted resolution required).
- **Clause 5 — Forward-fence + label** (Atom 0.5 ship-gates pass; legacy CPMM out of TB-13 module surface; OBS_TB_12_LEGACY_CPMM_QUARANTINE updated).

11 ship gates checklist (G1-G11):
- G1 cargo check passes
- G2 cargo test --workspace passes; workspace_count + delta vs TB-12 baseline 759
- G3 lean_market 7 subcommands accessible (TB-12 baseline; TB-13 does not add CLI)
- G4 evaluator MAX_TX exhaust still produces EvidenceCapsule (TB-11 carry-forward unchanged)
- G5 audit_dashboard §14 renders correctly
- G6 verify_chaintape green on TB-13 smoke runtime_repo
- G7 typed_tx variants: 3 NEW (CompleteSetMint, CompleteSetRedeem, MarketSeed) — additive only
- G8 dispatch arms: 3 NEW additive
- G9 TransitionError variants: 5 NEW additive
- G10 EconomicState fields == 13 (was 11; +conditional_collateral_t +conditional_share_balances_t)
- G11 Conservation: 6-holding sum preserved; complete-set balanced; shares NOT in sum

#### 6.b `handover/audits/CODEX_TB_13_SHIP_AUDIT_2026-05-XX.md`

Class 3 Codex impl-paranoid. Specific questions:
- Does CompleteSetMint create or destroy money? (must be balance↔collateral migration only)
- Can Redeem fire without a system-emitted resolution? (must be sequencer-rejected)
- Can Redeem with `outcome=Yes` and a TaskBankruptcy resolution_ref bypass the outcome check?
- Does the 6-holding sum hold across all TB-13 typed_tx?
- Does `assert_complete_set_balanced` hold after every transition?
- Can MarketSeedTx create liquidity without provider balance? (must be rejected)
- Are conditional shares anywhere counted as Coin? (must be excluded)
- Could a malformed ShareAmount underflow? (u128 type guarantee)
- Forward-fence: any new TB-13 module file references legacy `prediction_market`?

#### 6.c `handover/audits/GEMINI_TB_13_SHIP_AUDIT_2026-05-XX.md`

Class 3 Gemini architectural strategic. Same 9 questions + plus:
- Does CompleteSet schema extend cleanly to TB-14 PriceIndex (long/short interest derivable from `conditional_share_balances_t` aggregates)?
- Does the EventId == TaskId 1:1 simplification hold up under TB-14+ multi-event-per-task scenarios?
- Is the `ResolutionRef` model robust to multi-resolver scenarios in TB-15+?

Conservative-verdict-wins on disagreement (`feedback_dual_audit_conflict`).

**Iter cap**: 12h (3 docs + remediation).

**STOP-AT-VERDICT**: Atom 6 is the user-checkpoint gate. AI coder produces all 3 audit docs + remediation rounds (if any) and STOPS. User reviews verdict and explicitly authorizes Atom 7 SHIP.

---

### Atom 7 — SHIP (Class 0)

**Action**: ONLY after Atom 6 audit verdict PASS + explicit user authorization.

- LATEST.md TB-13 section at top.
- TB_LOG.tsv row 35.
- TRACE_FLOWCHART_MATRIX.md TB-13 row planned→shipped.
- handover/evidence/tb_13_complete_set_smoke_2026-05-XX/ if smoke ran.
- ship commit:
  ```
  TB-13 SHIPPED — CompleteSet + MarketSeedTx (Class 3 dual audit; PASS)
  ```

**Iter cap**: 4h.

---

## §4 Audit mode

**Class 3 dual audit at Atom 6** — Codex + Gemini per architect §11 + `feedback_dual_audit`.
Gemini exhausted → degraded label per TB-12 precedent; conservative-verdict-wins (VETO > CHALLENGE > PASS).

---

## §5 Forbidden in TB-13 (architect Part A §4.7 verbatim + supplementary)

```text
NO   AMM                                          (architect §4.7; defer TB-16+ explicitly scoped)
NO   CPMM router                                  (architect §4.7)
NO   orderbook / CLOB                             (architect §4.7)
NO   MarketOrderTx / MarketTradeTx                (architect §4.7)
NO   PriceIndex / price_yes / price_no calc       (defer TB-14)
NO   DPMM / pro-rata payout                       (architect §4.7)
NO   automatic liquidity                          (architect §4.7; CR-13.1)
NO   ghost liquidity                              (architect CR-13.1)
NO   automatic per-node 100 YES + 100 NO          (architect §4.7)
NO   Treasury magic seed unless Treasury debited  (architect §4.7)
NO   price oracle                                 (architect §4.7)
NO   NodeMarketEntry as canonical state           (architect §4.7; TB-14 derived view)
NO   f64 in money / collateral / share path       (architect SG-13.0.2 + halt trigger 2)
NO   import of src/prediction_market.rs in TB-13  (architect SG-13.0.1 + halt trigger 1)
NO   shares counted as Coin                       (architect CR-13.3 + SG-13.2)
NO   constitution.md edit                         (sudo-only; Class 4)
NO   bare `cargo test` in TB-13 ship report       (workspace canonical only)
NO   retroactive deletion of legacy prediction_market.rs in TB-13
     (production wiring + 10+ test files; TB-14 SHIP prerequisite per
      OBS_TB_12_LEGACY_CPMM_QUARANTINE; out-of-scope per
      feedback_no_retroactive_evidence_rewrite)
```

---

## §6 Ship gates

**Architect SG-13.0.x mandatory (Atom 0.5 forward-fence)**:

```text
SG-13.0.1  legacy_cpm_api_not_imported_by_complete_set passes
SG-13.0.2  no_f64_in_complete_set_or_market_seed passes
SG-13.0.3  prediction_market_legacy_quarantined passes
SG-13.0.4  OBS_TB_12_LEGACY_CPMM_QUARANTINE either closed or explicitly
           carried as non-importable legacy → carried (TB-14 SHIP
           prerequisite preserved)
```

**Architect SG-13.1..8 mandatory (Atoms 1-5)**:

```text
SG-13.1  Mint 1 Coin -> 1 YES + 1 NO, total Coin conserved
SG-13.2  YES/NO shares are not counted in total Coin supply
SG-13.3  MarketSeedTx fails if provider lacks balance
SG-13.4  MarketSeedTx cannot create liquidity without collateral
SG-13.5  Redeem unavailable before outcome resolution
SG-13.6  Redeem after YES outcome pays YES, not NO
SG-13.7  No f64 in new CompleteSet/MarketSeed path
SG-13.8  No import/use of legacy CPMM in TB-13 modules
```

EXACT-name discipline (per TB-12 architect §1.2 ruling):
test names = traceability contract; SG-13.x test functions MUST be named per the architect's "passes" string verbatim or in the canonical-named form documented above (Atom 5).

**Engineering carry-forward G1..G11**:

```text
G1   cargo check (release) passes
G2   cargo test --workspace passes; workspace_count + delta vs TB-12 759 baseline
     (target: 759 + Atom 0.5 SG-13.0.x (3) + Atom 1 unit (7) + Atom 2 unit (9)
      + Atom 3 invariant (6) + Atom 4 dashboard (1) + Atom 5 integration (8 SG +
      6 halt) ≈ 800; failed=0; ignored=150 unchanged)
G3   lean_market 7 subcommands (unchanged from TB-12)
G4   evaluator MAX_TX exhaust → EvidenceCapsule (unchanged from TB-11)
G5   audit_dashboard §14 renders correctly
G6   verify_chaintape 7 indicators GREEN on smoke runtime_repo
G7   diff TB-12→TB-13: 3 new TypedTx variants additive only
G8   diff TB-12→TB-13: 3 new dispatch arms additive
G9   diff TB-12→TB-13: 5 new TransitionError variants additive
G10  EconomicState field count == 13 (was 11; +conditional_collateral_t
     +conditional_share_balances_t)
G11  Conservation: 6-holding sum preserved; complete-set balanced post-
     transition; shares NOT in sum
```

---

## §7 Open questions (auto-resolved per architect §4 + §11)

| # | Question | Auto-resolution |
|---|---|---|
| Q1 | Risk class | Class 3 envelope; Atoms per-class above (§Risk-class envelope). |
| Q2 | EventId scope | TB-13: 1:1 with TaskId via `EventId(pub TaskId)`. Future TB-14+ may decouple. |
| Q3 | ShareAmount signedness | u128 (non-negative; underflow = sequencer error). Architect spec uses i128; we tighten to u128. |
| Q4 | Audit mode | Class 3 Codex + Gemini at Atom 6; conservative-verdict-wins. NO honest deferral (architect Part A §4.8 implicit; TB-12 precedent). |
| Q5 | Smoke mode | Deterministic integration tests = TB-13 main gate (Atom 5). No real-LLM smoke required (no LLM-driven path added in TB-13). |
| Q6 | Sync mode | Continuous Atoms 0.5 → 5 with AI-coder-decided micro-checkpoints CP-A/CP-B/CP-C → continue Atom 6 dual audit → STOP after audit verdict → SHIP at Atom 7 ONLY after explicit user authorization. |
| Q7 | Resolution reference shape | `ResolutionRef { resolution_tx_id, claimed_outcome }`; references TaskBankruptcyTx (NO) or FinalizeRewardTx (YES); validated via L4 lookup + outcome match in sequencer. |
| Q8 | View label discipline | "claims, not balances" (CR-13.3) + "locked collateral, not free coin" (CR-13.4). |
| Q9 | Halting triggers | total_supply_micro mutation incorrect, shares counted as Coin, MarketSeed without balance, legacy CPMM import in TB-13, f64 in new modules, any AMM/CPMM router introduced. MUST stop immediately. |
| Q10 | Atom 0.5 scope | Forward-fence + label only; NO retroactive deletion of legacy `src/prediction_market.rs` (TB-14 SHIP prerequisite per OBS). |

---

## §8 Cross-references

- 2026-05-03 architect post-TB-12 ruling lossless: `handover/directives/2026-05-03_TB13_TO_TB17_POST_TB12_ARCHITECT_RULING.md`
- 2026-05-02 supplementary directive (TB-13 base): `handover/directives/2026-05-02_TB11_TO_TB17_SUPPLEMENTARY_DIRECTIVE.md`
- 2026-05-03 architect TB-12 ruling: `handover/directives/2026-05-03_TB12_NODE_EXPOSURE_INDEX_ARCHITECT_RULING.md`
- TB-12 SHIPPED reference: `handover/ai-direct/LATEST.md` (top section) + commit `fa36eca`
- TB-12 charter (precedent for Class 3 dispatch + dual audit shape): `handover/tracer_bullets/TB-12_charter_2026-05-03.md`
- OBS legacy CPMM: `handover/alignment/OBS_TB_12_LEGACY_CPMM_QUARANTINE_2026-05-03.md`
- 9-phase roadmap: `handover/architect-insights/ROADMAP_9_PHASE_2026-04-29.md` (overlay-amended by 2026-05-02 + 2026-05-03 rulings)
- Constitution: `constitution.md` (Art. 0.2, I.1, III.4, V.1.3)
- Memory: `feedback_kolmogorov_compression`, `feedback_no_retroactive_evidence_rewrite`, `feedback_tb_phase_tag_required`, `feedback_dual_audit`, `feedback_iteration_cap_24h`, `feedback_workspace_test_canonical`, `feedback_step_b_protocol`, `feedback_smoke_before_batch`, `feedback_no_fake_menus`, `feedback_risk_class_audit`, `feedback_chaintape_externalized_proposal`

---

**Ratification path**: User-architect 2026-05-03 evening "proceed and recursively check at the best place you decide" reads as ratification + checkpoint authority delegation.

AI coder executes per Q6:
1. Atom 0.5 → CP-A → Atom 1 → Atom 2 → CP-B → Atom 3 → CP-C → Atom 4 → Atom 5.
2. Atom 6 dual audit (recursive self-audit + Codex + Gemini), STOP after verdict.
3. Atom 7 SHIP ONLY after explicit user authorization on the verdict.

Halting triggers (Part A §4.8 + halting-trigger guards in Atom 5) MUST stop execution immediately and report:
- total_supply_micro mutation incorrect (CTF conservation failure)
- Conditional shares counted as Coin (architect SG-13.2 violation)
- MarketSeedTx succeeds without balance debit (architect SG-13.3 violation)
- Legacy `prediction_market::` import in NEW TB-13 module (architect SG-13.0.1 violation)
- f64 appears in NEW market modules (architect SG-13.0.2 violation)
- Any AMM / CPMM router / price / trade logic introduced in TB-13 (forbidden list)
- Codex / Gemini VETO at Atom 6

End of charter.
