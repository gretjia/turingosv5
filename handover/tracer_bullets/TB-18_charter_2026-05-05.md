# TB-18 Charter — Formal Benchmark Scale-Up

**Status**: **RATIFIED-WITH-AMENDMENTS 2026-05-05** — architect ruling at `handover/directives/2026-05-05_TB18_CHARTER_RATIFICATION_ARCHITECT_RULING.md`. 17 amendments applied as Atom 0 (this revision; commit followup). TB-18 may start at Atom E once user grants source-change authorization. Architect did NOT grant blanket TB-18-ship autonomous-execution authority (no equivalent of TB-17's "由你负责执行，一直到TB-17 ship"); per-atom user authorization expected for Class 2 / Class 3 / Class 4 source work.
**Date**: 2026-05-05 (charter draft `d58af31`); 2026-05-05 (architect ratification + Atom 0 amendment).
**Predecessor**: TB-17 SHIPPED FINAL 2026-05-05 (commit `8e3d5cc`); 20/20 SG GREEN; §8 verdict CONDITIONAL with 5 caveats (L1 OBS_R023 / L2 PRE-17.5 / L3 PRE-17.6 / L4 PRE-17.7 / L5 sandbox-only smoke); P7 entry NOT authorized; per architect §B.10.2 verbatim "TB-17 后第一件事 = TB-18 Formal Benchmark Scale-Up". Workspace baseline: 939/0/150 @ `8e3d5cc`.

**Authority** (in order of precedence; later overrides earlier on conflict):
  - **2026-05-05 architect TB-18 charter ratification ruling** (this charter's binding amendment source; 17-point execution command in §A.6) — `handover/directives/2026-05-05_TB18_CHARTER_RATIFICATION_ARCHITECT_RULING.md`.
  - **2026-05-05 architect TB-17 audit verdict** §B.9 (M-ladder M0..M4) + §B.10.2 (TB-18 = first thing after TB-17 ship) — `handover/directives/2026-05-05_TB17_AUDIT_VERDICT_ARCHITECT_RULING.md`.
  - **PRE-17.6 deviation §6 forward-binding** (atoms A-H envelope source; ratified 2026-05-05) — `handover/proposals/TB-17_PRE_17_6_COMPREHENSIVE_ARENA_DEVIATION_2026-05-05.md` §6.
  - **OBS_M0_DEEPSEEK_DRIFT §5.1** (per-LLM-call budget binding for atom A) — `handover/alignment/OBS_M0_DEEPSEEK_DRIFT_2026-05-05.md`.
  - **OBS_R023 §5** (hardcoded `RunOutcome::MaxTxExhausted` evaluator path; Q4 deferral cap = TB-18) — `handover/alignment/OBS_R022_TB_16_X_2_2_FIX_EVIDENCE_CAPSULE_HARDCODED_MAXTX_2026-05-05.md`.
  - **PRE-17.5 design ratified** (Boltzmann ENFORCE — **EXCLUDED** from TB-18 default scope per Q3 ratification) — `handover/proposals/TB-17_PRE_17_5_BOLTZMANN_ENFORCE_DESIGN_2026-05-05.md`.
  - **PRE-17.7 design ratified** (β-A feasibility verification co-located with atom B; β-D → TB-19+) — `handover/proposals/TB-17_PRE_17_7_INTAPE_MARKOV_DESIGN_2026-05-05.md`.
  - **MARKOV_INHERITANCE_POLICY §4** (α → β migration target) — `handover/markov_capsules/MARKOV_INHERITANCE_POLICY.md`.
  - **TB-7R/TB-8/TB-9 evidence packaging precedent** (`runtime_repo.dotgit.tar.gz` + `cas.dotgit.tar.gz` per run; resolves Codex VETO on missing `.git/objects`) — `handover/tracer_bullets/TB-8_charter_2026-05-02.md` + `handover/tracer_bullets/TB-9_charter_2026-05-02.md`.

**Phase**: **P6 closure validation** per `ROADMAP_9_PHASE_2026-04-29.md` (P6 = multi-org permissioned ChainTape + Epistemic Lab v0). TB-18 produces full-scale formal-benchmark evidence on the Lean Proof problem set; **does NOT execute P7 real-world tasks**; supplies the empirical pre-condition for the P7-authorization decision in TB-19+ per architect §B.10.3.

**Risk class envelope** (architect Q2 hardened):
  - **Class 0** atom: 0 (this charter amendment).
  - **Class 2** atom: E (close OBS_R023; refactor; no canonical state surface change).
  - **Class 3** atoms: A, B, C, F, H (re-entrant evaluator API + budget; substantive arena; deferred-finalize; single-chain 13/13 evidence; M-ladder execution).
  - **Class 3** evidence + audit atoms: H0 (M0 preflight), G0 (Codex micro-audit pre-H), G1 (full Codex+Gemini ship audit).
  - **Class 3 OR Class 4** atom (HARD GATE): D — **Class 3 ONLY IF** atom only changes runtime lifecycle ordering policy without canonical schema/signing-payload mutation; **Class 4 STOP** if atom touches: sequencer admission semantics / `WorkTx` or `TerminalSummaryTx` canonical payload / signing payload / challenge-finalize admission predicates / canonical tx ABI. Per architect Q2 verbatim — this is a ship gate, not a comment.
  - **Class 4** atom (EXCLUDED from TB-18 default scope): PRE-17.5 Boltzmann ENFORCE per Q3 EXCLUDE verdict; carry-forward to TB-19+ as separate TB requiring Class 4 ratification + Phase Z′ + no-price-as-truth proof.

**STEP_B_PROTOCOL flag** (per CLAUDE.md restricted-file rule):
  - Atoms C and D touch `src/state/sequencer.rs` — **STEP_B_PROTOCOL applies**: parallel-branch A/B preflight required, no direct edit on `main`. Step-B preflight doc filed under `handover/ai-direct/TB-18_<atom>_STEP_B_<topic>.md` per `feedback_step_b_protocol`.
  - Atom A and Atom E touch `experiments/minif2f_v4/src/bin/evaluator.rs` — **NOT in STEP_B restricted set**; direct edit OK.
  - Atom B touches `experiments/minif2f_v4/src/bin/comprehensive_arena.rs` — **NOT restricted**; direct edit OK.

**Iteration cap** (per `feedback_iteration_cap_24h` production wire-up exception):
  - Class 0 atom (Atom 0) = single-pass; no iteration.
  - Class 2 atom (E) = 24h.
  - Class 3 atoms (A, B, C, F, H) = 72h-to-feedback-loop.
  - Class 3 evidence (H0) = 8h timebox (small preflight).
  - Class 3 audit atoms (G0, G1) = external auditor turnaround (Codex + Gemini SLA).
  - Class 4 atom (if D escalates) = no AI-coder iteration cap; gated on architect ratification + Phase Z′ rerun. **STOP at design step.**
  - **Total TB-18 envelope**: 10-14 days timebox (extended from charter draft's 7-10 by architect amendments adding H0 + G0 + G1 + manifest/packaging artifacts).

**FC-trace** (per `feedback_fc_first_problem_handling`):
  - `Art.0.1` (Append-Only DAG) — **atom D MUST preserve append-only semantics**; lifecycle-order configurable does NOT mean state-overwrite (architect §2.7); `TaskLifecycle = set / history / enum-with-history`, not single covering field.
  - `Art.0.2` (Tape Canonical) — atom F single-chain 13/13 evidence MUST be replay-byte-identical on chain-derived state alone; PRE-17.7 β-A feasibility resolves `previous_capsule_cid` from in-tape `TerminalSummaryTx → EvidenceCapsule.markov_capsule_cid` per `MARKOV_INHERITANCE_POLICY §4`. **TB-18 cannot use α CLI sidecar to fake β-A success** (architect Q4 added ship gate).
  - `Art.0.3` (Replay Determinism) — atom A re-entrant `drive_task` API MUST be deterministic given canonical chain + task spec + (logical_t, seed); per-LLM-call budget enforcement is a halt path emitting EvidenceCapsule, NOT a chain-state mutation source.
  - `Art.0.4` (Q_t version-controlled, path B chain continuation) — atom B + PRE-17.7 β-A together close path B for single-chain multi-task continuation. β-D (full pipeline replacing α) → TB-19+.
  - `Art.III.4` (no fake accepted) — **`RunOutcome::DegradedLLM` MUST emit EvidenceCapsule + TerminalSummary + budget counters; no payout; no fake accepted** (architect §2.5 — DegradedLLM cannot become evidence-skip backdoor).
  - `Art.IV` (terminal-state distinction `OmegaAccepted` / `MaxTxExhausted` / `WallClockCap` / `ComputeCapViolated` / `ErrorHalt`) — atom A adds **NEW** `RunOutcome::DegradedLLM` variant per OBS_M0 §5.1; atom E plumbs the actual `RunOutcome` discriminant into `EvidenceCapsule.outcome` (closes OBS_R023 hardcoded literal).
  - `Art.V.1` (三权分立) — atom G0 + G1 dual external audit (Codex + Gemini) discharges Generator≠Evaluator separation per `feedback_dual_audit`. **G1 final ship audit MUST cover H** (architect §2.1 audit-order amendment).

**Flowchart-trace**:
  - **Flowchart 1 (runtime)**:
    - `FC1-N36` = `evaluator::drive_task(chain, task_spec) -> RunOutcome` re-entrant API (atom A; preserves chain across tasks).
    - `FC1-N37` = per-LLM-call budget enforcement (atom A; new halt path → `RunOutcome::DegradedLLM` with mandatory EvidenceCapsule + TerminalSummary emission).
    - `FC1-N38` = sequencer deferred-finalize path (atom C; FinalizeReward re-emit post-ChallengeResolve; **idempotent payout**).
    - `FC1-N39` = lifecycle-order configurable resolution (atom D; constraint #2; **append-only history, NOT covering field**; class TBD).
    - `FC1-N40` = `EvidenceCapsule.outcome` propagation from caller (atom E; closes OBS_R023 hardcoded literal).
  - **Flowchart 2 (signal)**:
    - `FC2-N31` (TB-17 declared; substantive build in TB-18) = `comprehensive_arena.rs` substantive multi-task driver (atom B; ≥6 engineered Lean tasks in **ONE evaluator process + ONE runtime_repo + ONE CAS + ONE chain + ONE Sequencer**; subprocess fork to per-task evaluator processes is FORBIDDEN per architect §2.8).
    - `FC2-N32` = M-ladder M0/M1/M2 batch evaluation pipeline (atom H + H0).
  - **Flowchart 3 (meta)**:
    - `FC3-N46` = TB-18 ship doc + benchmark report as canonical formal-benchmark evidence; supplies P7-admission empirical pre-condition for TB-19+. **Report MUST include real-world-readiness disclaimer + benchmark contamination disclosure** (architect §2.9 + §2.10).

**Phase declarations** (per `feedback_tb_phase_tag_required`):

```text
phase_id: P6 closure validation (Multi-Org Epistemic Lab v0;
          full-scale formal benchmark on Lean Proof problem set).
          NOT P7 (no real-world execution). Supplies P7-admission
          empirical pre-condition consumed by TB-19+.

roadmap_exit_criteria_addressed:
  P6-Exit1   Multi-task single-chain coverage:
             atom B + atom F → ≥6 engineered Lean tasks in ONE
             evaluator process + ONE runtime_repo + ONE CAS + ONE
             chain; 13/13 architect tx kinds present in single-chain
             shape (replaces TB-16.x.2.6 multi-chain UNION as
             canonical evidence).
  P6-Exit2   Full-scale formal benchmark capacity validated:
             atom H → M2 (≥100 problems, n5, Boltzmann observe-only,
             no enforce, no price mask) per architect §B.9.3 +
             BenchmarkManifest pinned + EvidencePackagingPolicy
             satisfied.
  P6-Exit3   Per-LLM-call budget enforcement live:
             atom A → drift-induced silent hang (OBS_M0 §3) becomes
             clean `RunOutcome::DegradedLLM` halt with EvidenceCapsule
             + TerminalSummary + budget counter emission; external
             `timeout` becomes safety-net only.
  P6-Exit4   In-tape Markov inheritance β-A feasibility verified:
             PRE-17.7 §3.5 β-A provisional branch → `evaluator.rs`
             resolves `previous_capsule_cid` via in-tape
             `TerminalSummaryTx → EvidenceCapsule.markov_capsule_cid`
             on substrate produced by atom B; **β-A evidence MUST
             come from chain-derived TerminalSummary / EvidenceCapsule
             relation, NOT from α CLI sidecar** (Q4 added ship gate).
             β-D (full pipeline) carries forward to TB-19+.
  P6-Exit5   OBS_R023 closure (architect Q4 deferral cap):
             atom E → `EvidenceCapsule.outcome` propagated from
             caller's actual `RunOutcome`; hardcoded
             `RunOutcome::MaxTxExhausted` literal at evaluator.rs:3033
             eliminated.

  P7-PRE-empirical (TB-18 supplies; TB-19 consumes):
             Empirical evidence that TuringOS chain substrate
             can sustain full-scale Lean Proof benchmark without
             chain-integrity violation, used as pre-condition input
             into TB-19 low-risk real-world pilot design (architect
             §B.10.3 sequencing). NOT a real-world readiness claim.

kill_criteria_tested:
  P6-Forbidden:per-LLM-call budget absent (NEW from OBS_M0)
             SG-18.2 — atom A re-entrant API + per-call wall-clock
             budget + token-floor check fail-closed test asserting
             `RunOutcome::DegradedLLM` halt within configured budget;
             aggregate per-run wall-clock cap enforced internally,
             NOT only via external `timeout`.
  P6-Forbidden:DegradedLLM as evidence-skip backdoor
             SG-18.2 enriched (architect §2.5) — DegradedLLM halt
             MUST emit EvidenceCapsule + TerminalSummary + budget
             counters; payout = none; fake accepted = none.
  P6-Forbidden:hardcoded terminal-state literal (OBS_R023 closure)
             SG-18.3 — atom E regression test constructs synthetic
             non-`MaxTxExhausted` exit (e.g. `WallClockCap`) and
             asserts written `EvidenceCapsule.outcome` matches the
             propagated `RunOutcome` discriminant; literal
             `MaxTxExhausted` string scan in evaluator.rs:2940-3137
             region returns zero matches.
  P6-Forbidden:double payout via deferred-finalize (NEW architect §2.6)
             SG-18.4 — atom C idempotency: same WorkTx cannot
             receive double FinalizeReward; deferred FinalizeReward
             after ChallengeResolve fires once; pre-ChallengeResolve
             FinalizeReward rejected/deferred; re-emitted preserves
             original reward amount.
  P6-Forbidden:lifecycle state overwrite (NEW architect §2.7)
             SG-18.5 — atom D MUST NOT overwrite earlier lifecycle
             marker with later one; `TaskLifecycle = set / history /
             enum-with-history`; later marker does NOT erase earlier.
  P6-Forbidden:multi-evaluator-process per-task chain split (NEW architect §2.8)
             SG-18.6 — atom B substantive `comprehensive_arena.rs`
             MUST run ≥6 tasks in ONE process + ONE runtime_repo +
             ONE CAS + ONE chain + ONE Sequencer; per-task
             subprocess fork creating separate chains is FORBIDDEN.
  P6-Forbidden:multi-chain UNION as single-chain claim
             SG-18.7 — atom F single-chain evidence audit MUST
             cite ONE `runtime_repo` + ONE CAS dir; tx-kind
             coverage = 13/13 in that single chain; no
             cross-chain UNION accepted as substitute.
  P6-Forbidden:全局平行账本 (carry-forward TB-17 SG-17.10)
             SG-18.8 — atom H batch run MUST NOT introduce any
             `LATEST*` filesystem pointer; β-A in-tape resolution
             is the ONLY canonical inheritance path.
  P6-Forbidden:M0 too late (TB-17 复盘 §3.1 mandate; architect §1 Q5)
             SG-18.9 — H0 M0 preflight MUST run AFTER E+A and
             BEFORE B/F/H; if H0 fails, do NOT enter B/F/H.
  P6-Forbidden:Benchmark without manifest (NEW architect §2.2)
             SG-18.12 — `BenchmarkManifest` exists pinning
             problem_ids / split / model_name / model_version /
             temperature / max_tx / per_llm_call_budget / n_agents /
             seed / Lean version / mathlib commit / TuringOS commit;
             without manifest = incomparable evidence = ship blocked.
  P6-Forbidden:Evidence packaging gap (NEW architect §2.3; TB-7R precedent)
             SG-18.13 — `EvidencePackagingPolicy` satisfied:
             full restorable for M0; sampled (random + failure-heavy +
             solved + unsolved) for M1/M2; aggregate manifest for all
             runs; `runtime_repo.dotgit.tar.gz` + `cas.dotgit.tar.gz`
             per packaged run per TB-7R/TB-8/TB-9 precedent.
  P6-Forbidden:real-world readiness claim from MiniF2F
             SG-18.14 — TB-18 ship doc + benchmark report MUST
             contain explicit "Formal benchmark capacity only.
             Not real-world readiness. No real-world domain. No
             real funds. No public settlement." disclaimer per
             architect §2.9 + memory `feedback_minif2f_scaling_policy`.
  P6-Forbidden:Audit-before-evidence (NEW architect §2.1; AI-coder blind spot)
             SG-18.15 + SG-18.16 — G0 micro-audit AFTER F BEFORE H;
             G1 final dual audit AFTER H. Final ship audit MUST
             cover H (M-ladder evidence); G-before-H = wrong-target
             audit and is forbidden.
  P6-Forbidden:Class 4 surface hiding inside Class 3 atom
             Q2 hardened (architect §1 Q2) — atom D MUST stop for
             separate Class 4 ratification on sequencer admission /
             canonical payload / signing payload / challenge-finalize
             predicates / canonical tx ABI mutation; not a comment,
             a ship gate.
```

---

## §0 Why TB-18 exists (architect §B.10.2 + §B.9.3 verbatim)

```text
Architect §B.10.2 verbatim:
  TB-17 后第一件事 不是直接真实世界，而是：
    TB-18 — Formal Benchmark Scale-Up
  这可以是大规模 MiniF2F：
    full controlled benchmark
    chain-backed
    no real-world domain
    no real money
```

```text
Architect §B.9.3 M-ladder verbatim:
  M0 — Benchmark harness audit:
        20 known problems; chain-backed; no market;
        prove no fake accepted.
  M1 — Full heldout subset:
        50–100 problems; n1 / n3;
        all failures produce EvidenceCapsule;
        dashboard batch report.
  M2 — Multi-agent market-disabled:
        100+ problems; n5;
        Boltzmann observe only; no enforce; no price mask.
  M3 — Controlled market-enabled:    [DEFERRED to TB-19+]
        same problem set; NodePosition / PriceIndex / Autopsy;
        no real funds; sandbox only.
  M4 — Public benchmark report:      [DEFERRED post-TB-19]
        Only after replay reproducibility + no unresolved evidence
        gaps + workspace tests green + audit pass.
```

```text
Architect §7 final judgment verbatim:
  TB-18 = 不是刷题；不是 real-world；不是 market trading；
  而是证明 TuringOS 可以在大规模 formal benchmark 下保持：
    ChainTape 连续性，预算控制，terminal-state 区分，
    single-chain evidence，replay determinism，no fake accepted，
    no unresolved evidence gaps。

  这才是进入 TB-19 low-risk real-world pilot design 前应该拥有的证据。
```

---

## §1 One-line goal

```text
Goal: Land the substantive multi-task arena substrate (atoms A+B+C
      [+D-if-Class3]) and the formal benchmark up to architect §B.9.3
      M2 (100+ problems / n5 / Boltzmann observe-only / no market) on
      single-chain 13-of-13 shape. CLOSE three TB-17 forward triggers
      (OBS_R023 / OBS_M0 budget / PRE-17.7 β-A feasibility). PRODUCE
      benchmark report (with BenchmarkManifest + EvidencePackagingPolicy
      + real-world-readiness disclaimer + contamination disclosure)
      as P7-admission empirical pre-condition for TB-19+. NO production
      real-world task. NO real funds. NO public settlement. NO
      Boltzmann ENFORCE.

Key inputs:
  - TB-17 SHIPPED FINAL @ 8e3d5cc (workspace 939/0/150 baseline)
  - architect TB-18 ratification ruling 2026-05-05 (17-point
    execution command in §A.6 of ruling archive)
  - PRE-17.6 §6 atoms A-H (envelope source; pre-bound by architect)
  - OBS_M0_DEEPSEEK_DRIFT §5.1 (per-LLM-call budget spec)
  - OBS_R023 §5 (RunOutcome propagation remediation)
  - PRE-17.5 / PRE-17.7 design docs (RATIFIED 2026-05-05; PRE-17.5
    EXCLUDED from TB-18 per Q3; PRE-17.7 β-A only per Q4)
  - TB-7R/TB-8/TB-9 evidence packaging precedent

Key code surfaces:
  Atom 0   handover/tracer_bullets/TB-18_charter_2026-05-05.md
            (this revision — Class 0 charter amendment).
  Atom E   experiments/minif2f_v4/src/bin/evaluator.rs:2940-3137
            (RunOutcome propagation; closes OBS_R023). Class 2.
  Atom A   experiments/minif2f_v4/src/bin/evaluator.rs (4498 lines)
            — re-entrant drive_task(chain, task_spec) -> RunOutcome
            API + per-LLM-call budget + new RunOutcome::DegradedLLM
            variant + DegradedLLM-emits-EvidenceCapsule+TerminalSummary
            gate. Class 3.
  Atom H0  experiments/minif2f_v4/src/bin/evaluator.rs (consumer)
            + handover/tests/scripts/run_m0_minif2f_harness_2026-05-05.sh
            (small M0 preflight; ≤20 problems; validates A+E end-to-end).
            Class 3 evidence.
  Atom D-design
           src/state/sequencer.rs (read-only design step;
           CLASSIFIES Class 3 vs Class 4; **STOP at Class 4 gate**).
           Class 0 design step.
  Atom C   src/state/sequencer.rs (FinalizeReward re-emit
            post-ChallengeResolve; constraint #1 in PRE-17.6 §2.1;
            **idempotency ship gates per architect §2.6**).
            Class 3 (STEP_B_PROTOCOL).
  Atom D-impl
           src/state/sequencer.rs (constraint #2; lifecycle-order
           configurable; **append-only history, NOT covering field**;
           Class 3 ONLY; **STOPPED if Class 4 escalation**).
           Class 3 (STEP_B_PROTOCOL).
  Atom B   experiments/minif2f_v4/src/bin/comprehensive_arena.rs
            (436 lines scaffold → substantive multi-task driver
            using atom A's API; ONE process + ONE runtime_repo +
            ONE CAS + ONE chain + ONE Sequencer per architect §2.8).
            Class 3.
  Atom F   evidence: handover/evidence/tb_18_single_chain_13_of_13/
            (single-chain canonical evidence; replaces TB-16.x.2.6
            multi-chain UNION). Class 3 evidence + β-A feasibility.
  Atom G0  handover/audits/CODEX_MICRO_AUDIT_TB_18_PRE_H_*.md
            (Codex implementation-substrate micro-audit AFTER F;
            BEFORE expensive M-ladder). Class 3 audit.
  Atom H   handover/evidence/tb_18_minif2f_m2_2026-05-XX/ +
            handover/whitepapers/MINIF2F_M2_BENCHMARK_REPORT.md +
            handover/manifests/TB-18_BENCHMARK_MANIFEST.json
            (M-ladder M0 retry → M1 → M2; chain-backed; manifest +
            packaging policy satisfied). Class 3 evidence + ship doc.
  Atom G1  handover/audits/DUAL_AUDIT_TB_18_VERDICT_*.md
            (final Codex + Gemini external audit covering ALL atoms
            INCLUDING H). Class 3 audit.
```

## §1.1 PRE-18 hard preconditions

| PRE | Source | Status (2026-05-05) | Closure evidence |
|---|---|---|---|
| **PRE-18.1** TB-17 SHIPPED FINAL with §8 sign-off | TB-17 §8 | ✅ CLOSED | commit `8e3d5cc`; 20/20 SG GREEN; 939/0/150 baseline |
| **PRE-18.2** PRE-17.6 §6 forward-binding (atoms A-H) | PRE-17.6 §6 | ✅ RATIFIED | 2026-05-05 architect TB-17 ruling §B.8 atom 8 + Q5 |
| **PRE-18.3** OBS_M0_DEEPSEEK_DRIFT filed with budget spec | OBS_M0 §5.1 | ✅ FILED | `handover/alignment/OBS_M0_DEEPSEEK_DRIFT_2026-05-05.md` |
| **PRE-18.4** PRE-17.5 / PRE-17.7 design docs ratified | PRE-17.5 / .7 | ✅ RATIFIED | DESIGN-ONLY ship; PRE-17.5 EXCLUDED from TB-18 per Q3; PRE-17.7 β-A only per Q4 |
| **PRE-18.5** MARKOV_INHERITANCE_POLICY.md authoritative | OBS_R022 §A.5 | ✅ AUTHORITATIVE | `handover/markov_capsules/MARKOV_INHERITANCE_POLICY.md` filed 2026-05-05 |
| **PRE-18.6** TB-18 charter ratified with amendments | TB-18 charter ratification ruling | ✅ RATIFIED 2026-05-05 | this charter at `d58af31` + Atom 0 amendment commit (followup) |

**Opening rule**: all PRE-18 closed at TB-18 charter Atom 0 commit time. Atom E execution requires user "go" signal (architect did not grant blanket TB-18 autonomous-execution authority).

---

## §1.2 Functional Requirements FR-18.1..FR-18.10 (architect §4 verbatim)

```text
FR-18.1
drive_task(chain, task_spec) is re-entrant and uses the same
ChainTape across tasks.

FR-18.2
Per-LLM-call budget is enforced and emits RunOutcome::DegradedLLM,
not silent timeout.

FR-18.3
DegradedLLM emits EvidenceCapsule and TerminalSummary.

FR-18.4
EvidenceCapsule.outcome is propagated from actual RunOutcome,
no hardcoded MaxTxExhausted.

FR-18.5
Deferred FinalizeReward after ChallengeResolve is idempotent
and cannot double-pay.

FR-18.6
Lifecycle ordering avoids destructive overwrite of earlier
lifecycle facts.

FR-18.7
comprehensive_arena runs ≥6 engineered Lean tasks in one
evaluator process and one chain.

FR-18.8
Single-chain 13/13 tx-kind evidence exists in one runtime_repo
+ one CAS.

FR-18.9
M-ladder runs M0/M1/M2 under chain-backed benchmark manifest.

FR-18.10
Benchmark report is generated with reproducible manifest and
evidence packaging.
```

## §1.3 Constitutional Requirements CR-18.1..CR-18.10 (architect §4 verbatim)

```text
CR-18.1   No real-world execution.
CR-18.2   No real funds.
CR-18.3   No public settlement.
CR-18.4   No ChainTape bypass.
CR-18.5   All proposal/proof/failure evidence enters ChainTape/CAS
          or EvidenceCapsule.
CR-18.6   Dashboard / benchmark report is materialized view, not
          source of truth.
CR-18.7   No Class 4 surface hidden inside Class 3 atom.
CR-18.8   No multi-chain union claimed as single-chain.
CR-18.9   No hardcoded terminal state.
CR-18.10  No Boltzmann enforce unless separately ratified.
```

## §1.4 Ship Gates SG-18.1..SG-18.16 (architect §4 verbatim)

```text
SG-18.1   drive_task re-entrant API passes deterministic tests.

SG-18.2   DegradedLLM budget cap produces EvidenceCapsule.

SG-18.3   Hardcoded MaxTxExhausted literal removed; non-MaxTx
          outcome propagates.

SG-18.4   Deferred-finalize idempotency: no double payout.

SG-18.5   Lifecycle-order does not erase prior lifecycle facts.

SG-18.6   comprehensive_arena produces ≥6 tasks in one process
          and one chain.

SG-18.7   Single-chain 13/13 tx-kind evidence exists.

SG-18.8   No global Markov pointer introduced.

SG-18.9   M0 preflight passes before M1/M2.

SG-18.10  M1 50–100 problems completes with chain-backed evidence.

SG-18.11  M2 100+ n5 completes with Boltzmann observe-only,
          no enforce.

SG-18.12  BenchmarkManifest exists and pins problem set / model /
          seed / Lean / mathlib / commit.

SG-18.13  EvidencePackagingPolicy satisfied.

SG-18.14  Benchmark report contains "formal benchmark only,
          not real-world readiness."

SG-18.15  Codex micro-audit after F passes or issues are
          remediated before H.

SG-18.16  Final Codex + Gemini audit passes under VETO > CHALLENGE
          > PASS.
```

**Auxiliary ship gates** (charter-internal; not in architect §4 numbered list):

- **SG-18.A** workspace tests green: `cargo test --workspace --release` baseline 939/0/150 + new TB-18 tests; net delta ≥ +20. Per `feedback_workspace_test_canonical`: bare `cargo test` forbidden in TB ship report.
- **SG-18.B** atom B substantive build: `comprehensive_arena.rs` LOC ≥ 800 (substantive, not scaffold); contains multi-task loop calling `drive_task` directly (no per-task subprocess fork creating separate chains).
- **SG-18.C** PRE-17.7 β-A in-tape resolution test: substrate produced by atom B exercises in-tape `previous_capsule_cid` resolution from `TerminalSummary → EvidenceCapsule.markov_capsule_cid`; α CLI sidecar fallback retained but NOT used to fake β-A success.
- **SG-18.D** atom D risk-class status flag: ship-time status declared = "Class-3-additive-config-shipped" / "Class-4-stopped-pending-ratification" / "Class-4-ratified-and-implemented". No silent absorption.
- **SG-18.E** architect § sign-off: TB-18 ship status PROVISIONAL until architect signs off on benchmark report + caveats per TB-17 §8 pattern.

---

## §2 Atom-by-atom plan (architect §3 verbatim ordering)

Sequence: **Atom 0 → E → A → H0 → D-design → C → D-if-Class3 → B → F → G0 → H → G1 → ship**.

| # | Atom | Class | Surface | Iter cap | Tests / evidence added | Gate / stop-condition |
|---|---|---|---|---|---|---|
| 1 | **0** | 0 | this charter rev | single-pass | n/a | charter amendment commit |
| 2 | **E** | 2 | `evaluator.rs:2940-3137` `RunOutcome` propagation; closes OBS_R023 | 24h | `tb_18_evidence_capsule_outcome_propagation.rs` | literal `MaxTxExhausted` scan returns zero in target region |
| 3 | **A** | 3 | `evaluator.rs` re-entrant `drive_task` API + per-LLM-call budget + `RunOutcome::DegradedLLM` + DegradedLLM-emits-evidence | 72h | `tb_18_drive_task_api.rs` + `tb_18_per_llm_call_budget.rs` + `tb_18_degraded_llm_evidence_emission.rs` | drift smoke produces clean DegradedLLM halt within budget; EvidenceCapsule + TerminalSummary emitted; budget counters present; payout = none |
| 4 | **H0** | 3 evidence | small M0 preflight (≤20 problems) post-E+A using budget-enforced runtime | 8h | `handover/evidence/tb_18_h0_m0_preflight_*/` + `M0_PREFLIGHT_REPORT.md` | exercises: solved + unsolved + LLM-degraded/budget-cap + Lean-failure + EvidenceCapsule emission + no fake accepted (architect §2.4); **STOP if H0 fails — do NOT enter B/F/H** |
| 5 | **D-design** | 0 | `src/state/sequencer.rs` read-only design (constraint #2 lifecycle-order) | 24h | `handover/proposals/TB-18_ATOM_D_DESIGN_*.md` | classifies Class 3 vs Class 4 per architect Q2; **STOP at Class 4 gate** for separate ratification |
| 6 | **C** | 3 (STEP_B) | `sequencer.rs` deferred-finalize (constraint #1; idempotent payout) | 72h | `tb_18_deferred_finalize_idempotency.rs` covering: same-WorkTx-no-double-FinalizeReward + deferred-fires-once + pre-ChallengeResolve-rejected/deferred + re-emit-preserves-amount | architect §2.6 idempotency ship gates green |
| 7 | **D-impl** | 3 (STEP_B) IF design = Class 3; SKIP IF Class 4 | `sequencer.rs` lifecycle-order (constraint #2; append-only history) | 72h | `tb_18_lifecycle_no_overwrite.rs` covering: later marker does NOT erase earlier; `TaskLifecycle = set/history/enum-with-history` | architect §2.7 append-only invariant green; if SKIP, atom D status = "Class-4-stopped-pending-ratification" + TB-18 ship claim narrows per architect Q2 ("formal benchmark substrate partially closed; lifecycle-order constraint remains Class 4 forward trigger") |
| 8 | **B** | 3 | `comprehensive_arena.rs` substantive (≥6 tasks ONE chain) | 72h | `tb_18_single_chain_multi_task.rs` | one process + one runtime_repo + one CAS + one chain + one Sequencer; no per-task subprocess fork (architect §2.8) |
| 9 | **F** | 3 evidence | single-chain 13/13 smoke + β-A feasibility | 24h | `handover/evidence/tb_18_single_chain_13_of_13/README.md` + audit_tape verdict | PROCEED + 13/13 + tamper 3/3 + replay-byte-identical; β-A in-tape resolution exercised (NOT α CLI sidecar) |
| 10 | **G0** | 3 audit | Codex micro-audit on substrate (atoms A+B+C+E+F[+D-impl]) BEFORE H | external SLA | `handover/audits/CODEX_MICRO_AUDIT_TB_18_PRE_H_*.md` | architect §2.1 audit-order amendment: G0 BEFORE H to avoid wasting M-ladder compute on broken substrate |
| 11 | **H** | 3 evidence + report | M-ladder M0 retry → M1 (50-100, n1/n3) → M2 (100+, n5, observe-only) + BenchmarkManifest + EvidencePackagingPolicy + benchmark report | 5-7d (external compute) | `handover/evidence/tb_18_minif2f_m2_*/` + `MINIF2F_M2_BENCHMARK_REPORT.md` + `TB-18_BENCHMARK_MANIFEST.json` | SG-18.10/.11/.12/.13/.14 green; CLAUDE.md Report Standard (ΣPPUT + Mean PPUT solved + 95% CI Wilson + reputation_distribution + halt_reason_distribution + parent_selection_entropy + pairwise_payload_diversity_mean) |
| 12 | **G1** | 3 audit | full Codex + Gemini ship audit covering ALL atoms incl. H | external SLA | `handover/audits/DUAL_AUDIT_TB_18_VERDICT_*.md` | conservative VETO > CHALLENGE > PASS per `feedback_dual_audit_conflict` |
| 13 | **Ship** | — | TB-18 ship doc + architect § sign-off | gated on G1 + architect | ship commit | TB-18 SHIPPED FINAL gate per TB-17 §8 pattern |

**Hard sequencing rules** (architect-binding):
- **E before A**: E enables A's RunOutcome propagation through the same evaluator path.
- **A before H0**: H0 cannot validate budget enforcement before A lands it.
- **H0 STOP gate**: If H0 fails, do NOT enter B/F/H. Re-cycle through A debug; do not paper over with M-ladder retry.
- **D-design before C and D-impl**: design step decides risk class. **Class 4 STOPS atom D**; C still proceeds (independent of D).
- **B AFTER all of A+C+D-impl-or-skip**: substantive arena driver depends on all three for true single-chain 13/13.
- **F AFTER B**: F cannot exist without atom B's substrate.
- **G0 AFTER F BEFORE H**: micro-audit on substrate; saves M-ladder compute on broken substrate.
- **H AFTER G0**: only if G0 verdict is PASS or remediated CHALLENGE.
- **G1 AFTER H**: final ship audit MUST cover M-ladder evidence (architect §2.1 audit-order amendment).

---

## §3 Atom 0 closure log (this charter rev)

This rev applies the 17-point architect amendment per `handover/directives/2026-05-05_TB18_CHARTER_RATIFICATION_ARCHITECT_RULING.md` §A.6 verbatim. Diff vs `d58af31`:

1. **Status**: `DRAFT` → `RATIFIED-WITH-AMENDMENTS 2026-05-05`.
2. **Atoms**: added Atom 0 + H0 + D-design + D-impl-conditional + G0 + G1 (was: implicit single G).
3. **Sequencing**: was `E → A → C → D-design → B → F → G → H`; now `0 → E → A → H0 → D-design → C → D-if-Class3 → B → F → G0 → H → G1 → ship`.
4. **Atom D risk class**: was "Class 3 OR 4 (TBD)"; now Q2 hard ship gate: Class 3 ONLY IF no sequencer admission / canonical payload / signing payload / challenge-finalize predicates / canonical tx ABI mutation; **STOP for separate Class 4 ratification otherwise**.
5. **Q3 PRE-17.5**: was "AI recommendation EXCLUDE (Q3 still open)"; now ratified `EXCLUDE`. Carry-forward to TB-19+ as separate TB.
6. **Q4 PRE-17.7**: was "AI recommendation β-A only (Q4 still open)"; now ratified `β-A only` + NEW SG: TB-18 cannot use α CLI sidecar to fake β-A success; β-D → TB-19+.
7. **Q5 M0**: was "M0 retry as Atom H sub-stage 1"; now H0 early preflight (after E+A; before B/F/H) + M0 retry as part of H ladder.
8. **Q6 M-ladder**: ratified `M0 + M1 + M2`; `M3 + M4 → TB-19+`.
9. **Q7 dual audit**: was "batched at ship (single G)"; now G0 Codex micro-audit AFTER F BEFORE H + G1 full Codex+Gemini AFTER H.
10. **NEW §7 BenchmarkManifest** appendix.
11. **NEW §8 EvidencePackagingPolicy** appendix.
12. **FR**: 10 → 10 (re-numbered + 4 new clauses on idempotency, append-only lifecycle, one-chain enforcement, manifest+packaging).
13. **CR**: 8 → 10 (CR-18.8/.9/.10 added).
14. **SG**: 12 → 16 (SG-18.13/.14/.15/.16 added).
15. **§3 ratification questions**: removed (already ratified); replaced with Atom 0 closure log (this section).
16. **STEP_B_PROTOCOL flag** added for atoms C + D-impl (sequencer.rs).
17. **AI-coder blind spot**: §2.1 audit-before-evidence acknowledged in `feedback_audit_after_evidence` memory (NEW); §2.2 + §2.3 acknowledged in `feedback_benchmark_manifest_required` + `feedback_evidence_packaging_policy_required` memories (NEW; per architect ruling §C.3).

**Atom 0 verdict**: SHIPPED at commit followup to `d58af31`. TB-18 unblocked from atom E onward subject to user "go" per non-blanket authority.

---

## §4 Forward triggers (TB-19+)

| Trigger | Source | Carry-forward TB |
|---|---|---|
| **PRE-17.5** (Boltzmann ENFORCE Class 4 schema bump + Phase Z′ rerun + no-price-as-truth proof) | TB-18 Q3 EXCLUDE | TB-19+ separate TB |
| **PRE-17.7 β-D** (full in-tape pipeline replacing α as default) | TB-18 Q4 default scope | TB-19+ |
| **M3** controlled market-enabled benchmark | TB-18 Q6 default scope | TB-19+ pilot design |
| **M4** public benchmark report | TB-18 Q6 default scope | post-TB-19 |
| **TB-19** Low-Risk Real-World Pilot Design (T2-like; oracle-backed; verifiable) | architect §B.10.3 | TB-19 charter |
| **OBS_M0 §5.2** P02 tamper-detection degrades (2/3 vs 3/3) | OBS_M0 §5.2 | TB-19+ atom-side investigation |
| **Atom D Class 4 escalation** (if D-design verdict = Class 4) | architect Q2 hard gate | TB-19+ separate Class 4 ratification + Phase Z′ rerun |

---

## §5 Cross-references

- **Architect rulings**:
  - **TB-18 charter ratification**: `handover/directives/2026-05-05_TB18_CHARTER_RATIFICATION_ARCHITECT_RULING.md` (this charter's binding amendment source)
  - TB-17 audit verdict: `handover/directives/2026-05-05_TB17_AUDIT_VERDICT_ARCHITECT_RULING.md` §B.9 + §B.10 + Q4
  - TB-16 OBS_R022: `handover/directives/2026-05-04_TB16_OBS_R022_ARCHITECT_RULING.md` §B.6 + §B.7
  - TB-13→TB-17 post-TB-12: `handover/directives/2026-05-03_TB13_TO_TB17_POST_TB12_ARCHITECT_RULING.md` §8
- **PRE-17 design / deviation docs (RATIFIED 2026-05-05)**:
  - `handover/proposals/TB-17_PRE_17_5_BOLTZMANN_ENFORCE_DESIGN_2026-05-05.md` (EXCLUDED from TB-18 per Q3)
  - `handover/proposals/TB-17_PRE_17_6_COMPREHENSIVE_ARENA_DEVIATION_2026-05-05.md` §6 (atom envelope source)
  - `handover/proposals/TB-17_PRE_17_7_INTAPE_MARKOV_DESIGN_2026-05-05.md` §3.5 (β-A co-location)
- **OBS docs (forward triggers)**:
  - `handover/alignment/OBS_M0_DEEPSEEK_DRIFT_2026-05-05.md` §5.1 (atom A budget spec)
  - `handover/alignment/OBS_R022_TB_16_X_2_2_FIX_EVIDENCE_CAPSULE_HARDCODED_MAXTX_2026-05-05.md` (OBS_R023; atom E)
  - `handover/alignment/OBS_R024_TB_16_X_2_4_BOLTZMANN_OBSERVE_VS_ENFORCE.md` (PRE-17.5 source; carry-forward)
- **Policy + roadmap**:
  - `handover/markov_capsules/MARKOV_INHERITANCE_POLICY.md` §4 (α/β migration)
  - `handover/architect-insights/ROADMAP_9_PHASE_2026-04-29.md` (P6 closure validation)
- **TB-7R/TB-8/TB-9 evidence packaging precedent** (architect §2.3 cite):
  - `handover/tracer_bullets/TB-8_charter_2026-05-02.md` (`runtime_repo.dotgit.tar.gz` + `cas.dotgit.tar.gz` codification)
  - `handover/tracer_bullets/TB-9_charter_2026-05-02.md` (cross-run pair pattern)
- **TB-17 ship state**:
  - `handover/whitepapers/REAL_WORLD_READINESS_REPORT.md` §8 (CONDITIONAL with 5 caveats)
  - commit `8e3d5cc` (TB-17 SHIPPED FINAL)
- **Memory bindings (binding)**:
  - `feedback_tb_phase_tag_required` (this charter declares phase_id + roadmap_exit_criteria_addressed + kill_criteria_tested)
  - `feedback_minif2f_scaling_policy` (M0-M4 ladder; full benchmark = TB-18-after-TB-17-ship)
  - `feedback_class4_cannot_hide_in_class3` (atom D Q2 hard gate)
  - `feedback_iteration_cap_24h` (Class 3 production wire-up exception 72h)
  - `feedback_workspace_test_canonical` (`cargo test --workspace` ship-gate canonical)
  - `feedback_dual_audit` + `feedback_dual_audit_conflict` + `feedback_audit_loop_roi_flip` + `feedback_audit_obs_bias` (G0 + G1 envelope)
  - `feedback_step_b_protocol` (atoms C + D-impl)
  - `feedback_no_workarounds_strict_constitution` (no 凑活; concrete TB-19+ closure for forward triggers)
  - `feedback_no_retroactive_evidence_rewrite` (TB-18 evidence going-forward; old chains preserved)
  - `feedback_kolmogorov_compression` (architect ruling archived verbatim)
  - `feedback_no_fake_menus` (architect 17-point command executed; no menu)
  - `feedback_architect_deviation_stance` (Q1-Q7 each had explicit AI position; ratified or amended)
  - **NEW** `feedback_audit_after_evidence` (architect §2.1 G-before-H bug)
  - **NEW** `feedback_benchmark_manifest_required` (architect §2.2)
  - **NEW** `feedback_evidence_packaging_policy_required` (architect §2.3 + TB-7R precedent)

---

## §6 What this charter does NOT authorize

Per `feedback_no_fake_menus` + `feedback_class4_cannot_hide_in_class3` + architect ruling §C.4:

- ❌ No source-code changes from Atom E onward without explicit user "go" signal (architect did NOT grant blanket TB-18-ship autonomous-execution authority).
- ❌ No PRE-17.5 implementation in TB-18 (Q3 EXCLUDE).
- ❌ No PRE-17.7 β-D implementation in TB-18 (Q4 β-A only).
- ❌ No M3 / M4 execution in TB-18 (Q6 M0+M1+M2 only).
- ❌ No real-world execution; no real funds; no public settlement (CR-18.1/2/3).
- ❌ No silent Class 4 absorption in atom D (Q2 hard gate).
- ❌ No retroactive rewrite of historical chains (TB-13/14/16 evidence preserved as-is).
- ❌ No claim of real-world readiness from M-ladder results (SG-18.14).
- ❌ No M-ladder run without `BenchmarkManifest` (SG-18.12).
- ❌ No M2 commit without `EvidencePackagingPolicy` satisfaction (SG-18.13).
- ❌ No `RunOutcome::DegradedLLM` halt without EvidenceCapsule + TerminalSummary + budget counters (FR-18.3 + Art.III.4).
- ❌ No deferred-finalize implementation without idempotency tests (SG-18.4).
- ❌ No lifecycle-order implementation that overwrites earlier markers (SG-18.5; Art.0.1).
- ❌ No comprehensive_arena per-task subprocess fork creating separate chains (SG-18.6 + architect §2.8).
- ❌ No use of α CLI sidecar to fake β-A success in atom F (Q4 added ship gate).
- ❌ No G1 final dual audit run before H completes (architect §2.1 audit-order rule).
- ❌ No ship without architect § sign-off (SG-18.E; per TB-17 §8 pattern).

---

## §7 BenchmarkManifest specification (NEW; architect §2.2)

`handover/manifests/TB-18_BENCHMARK_MANIFEST.json` — REQUIRED artifact for atom H. Without manifest, M-ladder evidence is **incomparable** and ship is BLOCKED (SG-18.12).

### §7.1 Required fields

```json
{
  "manifest_version": "tb-18.v1",
  "created_at_utc": "2026-05-XX",
  "phase": "TB-18 Formal Benchmark Scale-Up",
  "ladder_stage": "M0|M1|M2",
  "problem_set": {
    "source": "MiniF2F | heldout-49 | other",
    "split": "valid | test | other",
    "problem_ids": ["mathd_algebra_107", "..."],
    "problem_count": 0
  },
  "model": {
    "name": "deepseek-chat | siliconflow-qwen3-... | other",
    "version_pin": "model-id-or-hash-as-of-batch-start",
    "temperature": 0.2,
    "max_output_tokens": 8000,
    "additional_params": {}
  },
  "runtime": {
    "max_tx": 20,
    "per_llm_call_wallclock_seconds": 60,
    "per_llm_call_token_floor": 30,
    "consecutive_trivial_response_cap": 10,
    "aggregate_per_run_wallclock_seconds": 600,
    "n_agents": 1
  },
  "seeds": {
    "rng_seed": 42,
    "boltzmann_seed": "...",
    "additional_seeds": {}
  },
  "lean_environment": {
    "lean_version": "4.x.y",
    "mathlib_commit": "...",
    "lake_manifest_sha": "..."
  },
  "turingosv4_commit": "<head sha at batch start>",
  "boltzmann_mode": "observe-only",
  "market_state": "disabled",
  "price_index_state": "disabled-or-sandbox-only",
  "real_funds": false,
  "public_settlement": false,
  "real_world_domain": false
}
```

### §7.2 Anti-drift contract

The manifest is **frozen** at batch start. If any field changes mid-batch (e.g. model version drift, mathlib re-fetch), batch is **invalidated** and must restart with new manifest (new `created_at_utc` + new `turingosv4_commit`).

### §7.3 Per-run linking

Each problem run within the batch records `manifest_id = sha256(canonical_json(manifest))` in its `EvidenceCapsule.batch_manifest_id` field (NEW field on EvidenceCapsule for TB-18 per atom A; OR sidecar JSON if EvidenceCapsule schema bump deferred). Replay tooling cross-checks every run's `batch_manifest_id` matches the committed manifest.

---

## §8 EvidencePackagingPolicy specification (NEW; architect §2.3 + TB-7R precedent)

REQUIRED artifact: `handover/policies/TB-18_EVIDENCE_PACKAGING_POLICY.md` declaring per-stage strategy. Without satisfaction, ship is BLOCKED (SG-18.13). Default packaging unit per TB-7R/TB-8/TB-9 codification: `runtime_repo.dotgit.tar.gz` + `cas.dotgit.tar.gz` per packaged run.

### §8.1 Per-stage strategy

| Stage | Volume | Strategy | Rationale |
|---|---|---|---|
| **H0 M0 preflight** (≤20 problems) | small | **FULL** restorable evidence: every problem committed with `runtime_repo.dotgit.tar.gz` + `cas.dotgit.tar.gz` + `replay_report.json` + `audit_tape` verdict | small enough to commit fully; preflight evidence is high-leverage for budget/DegradedLLM verification |
| **M0 retry** (20 problems; in atom H) | small | **FULL** restorable | per architect §B.9.3 M0 spec ("prove no fake accepted"); FULL restorable enables tamper-detection audit |
| **M1** (50-100 problems; n1/n3) | medium | **SAMPLED restorable** + aggregate manifest | full commit infeasible at 50-100 × 2 runs; sample composition: 10 random + 5 failure-heavy + 5 solved + 5 unsolved + first-failure-of-each-failure-class |
| **M2** (100+ problems; n5) | large | **SAMPLED restorable** + aggregate manifest + `EVIDENCE_INDEX.json` enumerating ALL runs (committed + uncommitted, with `local_evidence_path` for uncommitted) | full commit infeasible at 100+ × 5 runs; sample composition: 20 random + 10 failure-heavy + 10 solved + 10 unsolved + first-DegradedLLM-of-batch + first-WallClockCap-of-batch (if any) |

### §8.2 Sample selection determinism

Sample selection MUST be deterministic given `(manifest_id, sample_strategy_version, sample_seed)`. The sample seed is committed in `TB-18_EVIDENCE_PACKAGING_POLICY.md`. This means a future auditor can re-derive the same sample (modulo source code drift) and confirm what was committed matches the policy.

### §8.3 Local-only evidence pointer

For uncommitted-but-extant runs (M1/M2 non-sampled subset), `EVIDENCE_INDEX.json` records:

```json
{
  "run_id": "...",
  "problem_id": "...",
  "outcome": "OmegaAccepted|MaxTxExhausted|DegradedLLM|...",
  "local_evidence_path": "/local/host/path/...",
  "host_id": "<machine identifier>",
  "kept_until_utc": "<retention deadline>"
}
```

This satisfies architect §2.3 ("如果你打算不 commit all CAS for 100+ problems，需要明确：where full evidence lives; how auditor can reproduce; what subset is committed; how subset was selected.").

### §8.4 Replay integrity check

For every committed `runtime_repo.dotgit.tar.gz` + `cas.dotgit.tar.gz` pair:
1. `git -C runtime_repo fsck --strict` MUST exit 0.
2. `git -C cas fsck --strict` MUST exit 0.
3. `audit_tape --runtime-repo runtime_repo --cas-dir cas` MUST verdict PROCEED.
4. Replay byte-equivalence test MUST pass.

This closes the TB-7R Codex VETO precedent (missing `.git/objects` blobs).

---

**End of charter (Atom 0 amended).** AI-coder filed under user-architect autonomous-execution authority for charter drafting only (Class 0 doc). Atom E onward = user "go" required per non-blanket TB-18-ship authority.
