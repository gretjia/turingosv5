# TB-G G1.1 — §8 Packet (DRAFT; pre-implementation halt)

> **Status**: DRAFT. G1.1 (resume-mode genesis branch) is the load-bearing
> Class-4 STEP_B atom of G-Phase per architect's binding G1-before-G4
> ordering. Per `feedback_no_batch_class4_signoff` + CLAUDE.md §9 / §10, this
> packet is published BEFORE implementation lands so architect can ratify
> scope before any sequencer-admission / canonical-signing-payload-adjacent
> code is touched. **§6 (architect §8 verbatim) is BLANK until user
> provides explicit multi-clause sign-off.**

## §0. Header

- **TB**: TB-G atom **G1.1** (RuntimeChaintapeConfig resume mode)
- **Charter**: `handover/tracer_bullets/TB_G_GENERATIVE_ARENA_charter_2026-05-11.md` §1 Module G1
- **Architect directive**: `handover/directives/2026-05-11_G_PHASE_GENERATIVE_ARENA_DIRECTIVE.md`
- **Plan reference**: web ultraplan session `01QqSehGhpsts18AC5qExyAS` (approved by user verbatim "plan approved, returned to terminal and execute your plan" 2026-05-11)
- **Class**: **4 STEP_B** — touches `src/state/sequencer.rs` (new constructor) + `src/runtime/mod.rs` (admission of non-empty repo at boot) + indirectly the FC2-Boot semantic boundary
- **STEP_B branch (forward)**: `feat/g1-1-resume-mode` (parallel branch per CLAUDE.md §12; NOT yet cut)
- **`origin/main` HEAD**: `2c110dc` (pre-teleport sweep; G-Phase directive archived)
- **Phase-id**: P3-G (RSP Economy Generative Arena per charter §0)
- **FC-trace**: FC2-Boot **extension** — replay-from-genesis semantic preserved; introduces a new admission path (`build_chaintape_sequencer` accepts non-empty repo iff env-gated `TURINGOS_CHAINTAPE_RESUME=1` AND `reconstruct_from_chaintape_refs` succeeds). FC1 untouched. FC3 untouched. **Constitutional anchor**: G-009 HEAD_t Path C / Stage A3 multi-ref ChainTape — `reconstruct_from_chaintape_refs` already reconstructs `QState` from `refs/chaintape/{l4,l4e,cas}` (SG-A3.4 GREEN); G1.1 turns that capability into a sequencer-construction boundary.

## §1. What G1.1 closes

Architect verdict §1.3 verbatim:

> 每个 problem fresh runtime_repo + fresh genesis — 每轮开局都把交易员洗白、清仓、重置记忆。市场不会涌现。

Current state pinned at `src/runtime/mod.rs:268..299` `BootstrapError::NonEmptyRuntimeRepo`:

```text
"non-empty runtime repo at {path:?} (existing head {existing_head});
 TB-6 Atom 1 fail-closes here. Reconstruction from existing chain is
 deferred to a future TB. Point TURINGOS_CHAINTAPE_PATH at a fresh
 directory to start a new run."
```

That "future TB" is G1.1. G1.1 lifts the fail-closed by adding **resume mode** as an explicit, env-gated admission path. Critically:

- **Default behavior unchanged**: when `TURINGOS_CHAINTAPE_RESUME` is unset OR set to anything other than `"1"`, `NonEmptyRuntimeRepo` still fail-closes (back-compat — all TB-N* / Stage C / Wave 3 50p / TB-N3 Phase 2 evidence remains determinism-preserved).
- **Resume admission gate**: only `TURINGOS_CHAINTAPE_RESUME=1` lets `build_chaintape_sequencer` open a non-empty `refs/transitions/main`. Architect-mandated default-deny posture per CLAUDE.md §11.
- **`Sequencer.next_logical_t` correctness**: resume branch sets `next_logical_t = chain_length` so the strict `len + 1` invariant in `Git2LedgerWriter` holds on the next commit. Architect SG-G1.2 verbatim.
- **`QState` reconstruction**: resume branch calls `reconstruct_from_chaintape_refs` (existing helper at `src/state/head_t_witness.rs::reconstruct_from_chaintape_refs`, GREEN under Stage A3 R3.5 + R5 smoke). No new replay logic.
- **Pinned-pubkey continuity**: resume branch reads existing `pinned_pubkeys.json` if present (epoch preserved across resume); else generates fresh per CLAUDE.md FC2 system-pubkey discipline.

## §2. Substrate change summary (forward — to land on branch `feat/g1-1-resume-mode`)

Anticipated diff at `src/`:

### STEP_B-protected surfaces (2 files)

| File | Change |
|------|--------|
| `src/runtime/mod.rs` | +`RuntimeChaintapeConfig.resume_existing_chain: bool` (env-read `TURINGOS_CHAINTAPE_RESUME==1` in `from_env`; default `false`) + `build_chaintape_sequencer` branch on `config.resume_existing_chain`: if `true` AND `Git2LedgerWriter::open` finds non-empty `refs/transitions/main`, call `reconstruct_from_chaintape_refs` to rebuild `QState` + open existing `CasStore` + read existing `pinned_pubkeys.json` if present + call `Sequencer::new_at_logical_t(initial_q, len)`. **Strict invariant**: if resume succeeds, `next_logical_t == chain_length` and the next commit's `parent_state_root` matches the chain head's `state_root`. |
| `src/state/sequencer.rs` | +`Sequencer::new_at_logical_t(initial_q, logical_t)` constructor (companion to existing `Sequencer::new`; both share the same body except for the `next_logical_t` seed value). Same admission arms, same predicate gates, same monetary invariants. |

### Adjacent surfaces (1 file)

| File | Change |
|------|--------|
| `src/state/head_t_witness.rs` | (REUSE ONLY; no edit expected) `reconstruct_from_chaintape_refs` already exists (Stage A3) and produces the `(QState, chain_length, l4e_count, cas_count)` tuple consumed by the resume branch. |

### Test surface (1 new file + 0 modified)

| File | Change |
|------|--------|
| `tests/constitution_g1_resume.rs` (NEW) | 5+ tests covering SG-G1.1..G1.5: <br>• `resume_on_empty_repo_equals_legacy_genesis` — `TURINGOS_CHAINTAPE_RESUME=1` on a fresh empty path produces byte-equal `QState` to legacy `build_chaintape_sequencer` (default branch).<br>• `resume_on_n_entry_chain_sets_next_logical_t_to_n` — bootstrap fresh → commit N entries → drop sequencer → re-`build_chaintape_sequencer` with resume=1 → assert `Sequencer.next_logical_t == N` AND next commit's `parent_state_root` matches.<br>• `resume_balances_reconstruction_matches_forward_replay` — fresh forward run of K accepted `WorkTx` produces `balances_t_A`; same chain replayed via resume produces `balances_t_B`; assert byte-equal.<br>• `non_empty_runtime_repo_only_fires_when_resume_false` — non-empty repo + resume=0 still returns `BootstrapError::NonEmptyRuntimeRepo` (back-compat regression gate).<br>• `pinned_pubkeys_preserved_across_resume` — fresh bootstrap writes `pinned_pubkeys.json`; resume reads it and `epoch` carries forward (no re-pin). |

### Documentation surface (1 file)

| File | Change |
|------|--------|
| `handover/alignment/TRACE_FLOWCHART_MATRIX.md` | +FC2-Boot row: "Resume from existing `refs/transitions/main` (env-gated)" with `tests/constitution_g1_resume.rs` binding. |

### NO changes to (defended invariants)

- `src/state/typed_tx.rs` — no new tx kind, no schema change.
- `src/bottom_white/ledger/system_keypair.rs` — no new signing domain.
- `src/bottom_white/ledger/transition_ledger.rs` — no new TxKind id; `Git2LedgerWriter::open` already supports opening non-empty repos (the fail-closed gate lives in `build_chaintape_sequencer`, not in `Git2LedgerWriter`).
- `genesis_payload.toml` — no rehash. Resume does NOT mutate genesis; it consumes the existing genesis_report.json from the resumed repo.
- `src/sdk/predicate.rs` — predicates untouched.
- Economy laws (`src/economy/monetary_invariant.rs`) — untouched.

## §3. Ship gates (SG-G1.1..SG-G1.5 per charter §1 Module G1)

| ID | Gate | Test name | Status (forward) |
|----|------|-----------|------------------|
| SG-G1.1 | Resume on empty repo == legacy bootstrap (byte-equal genesis) | `resume_on_empty_repo_equals_legacy_genesis` | RED until implementation |
| SG-G1.2 | Resume on N-entry chain produces `Sequencer.next_logical_t == N` | `resume_on_n_entry_chain_sets_next_logical_t_to_n` | RED until implementation |
| SG-G1.3 | Balances reconstruction matches fresh forward replay | `resume_balances_reconstruction_matches_forward_replay` | RED until implementation |
| SG-G1.4 | `BootstrapError::NonEmptyRuntimeRepo` ONLY fires when resume=false AND repo non-empty (back-compat) | `non_empty_runtime_repo_only_fires_when_resume_false` | RED until implementation |
| SG-G1.5 | `pinned_pubkeys.json` preserved across resume (epoch carries forward) | `pinned_pubkeys_preserved_across_resume` | RED until implementation |

## §4. Constitutional anchor — why this is Class 4

CLAUDE.md §9 Class 4 verbatim:

> Constitution / sequencer admission / typed tx schema / canonical signing payload / RootBox. Requires: explicit architect ratification / harness / minimal real run if applicable / external audit.

G1.1 touches sequencer admission (new constructor) AND introduces a new boot-time admission path (non-empty repo allowed under env gate). **Both qualify as Class 4 surfaces.** Per `feedback_no_batch_class4_signoff`, G1.1 cannot be batched with G3.2 or G4.2; each gets its own §8 packet + sign-off.

Constitutional gates preserved:

- **Tape-first**: resume reads from `refs/chaintape/{l4,l4e,cas}` — the canonical tape. No memory-only state. (CLAUDE.md §2.2)
- **No ghost liquidity**: resume does not mint. (CLAUDE.md §13)
- **No price-as-truth**: resume does not touch price. (CLAUDE.md §13)
- **Dashboard materialized view only**: resume does not touch dashboard. (CLAUDE.md §5.4)
- **No real funds**: resume is local FS. (CLAUDE.md §20 freeze list)
- **No public chain**: resume is local FS. (CLAUDE.md §20 freeze list)
- **FC2-Boot replay determinism**: resume IS replay. Architect §4.1 G-009 HEAD_t Path C requires "every accepted transition updates HEAD_t" + "replay reconstructs HEAD_t" — resume is the **boot-time** instance of that replay. Existing Stage A3 SG-A3.4 (replay byte-equality) covers the replay correctness; G1.1 SG-G1.3 layers a balances-byte-equal check on top.
- **No global Markov pointer**: resume reads from refs, not from a global pointer. (CLAUDE.md §3.3)
- **System tx not agent-submittable**: untouched. (CLAUDE.md §13)

## §5. Dual external audit plan (PRE-§8)

Per `feedback_dual_audit` + `feedback_audit_after_evidence`:

1. **PRE-implementation**: this packet (architect ratifies scope before any branch is cut). User sign-off in §6.
2. **POST-implementation, PRE-§8**: Codex G2 + Gemini DT round-cap=2 against the `feat/g1-1-resume-mode` branch HEAD covering:
   - **Q1**: resume branch fails-closed if `reconstruct_from_chaintape_refs` returns Err (no partial admission).
   - **Q2**: resume branch reads `pinned_pubkeys.json` deterministically (no clock / random / env beyond the documented env-gate).
   - **Q3**: `Sequencer::new_at_logical_t` and `Sequencer::new` share identical admission arms (no admission-arm fork).
   - **Q4**: env-gate is strict equality `=="1"` (no truthy-string footgun).
   - **Q5**: `BootstrapError::NonEmptyRuntimeRepo` back-compat regression test (SG-G1.4) lives in CI not just in the new test file.
   - **Q6**: no Trust Root manifest changes needed (no STEP_B file added to manifest).
   - **Q7**: source-grep gate confirms no f64 / no shadow ledger / no global Markov pointer reintroduction.
   - **Q8**: existing TB-N* / Stage C / Wave 3 50p / TB-N3 Phase 2 smoke runs unchanged under resume=0 default (no test regression).
   - **Q9**: resume branch is mentioned in `TRACE_FLOWCHART_MATRIX.md` FC2-Boot row.
3. **Real-LLM smoke after dual-audit PASS**: 3-problem mini batch with resume=1 → witness one runtime_repo across 3 problems → audit_tape PROCEED + invariant verdict Ok delta 0. Evidence dir `handover/evidence/g_phase_g1_1_smoke_2026-05-11T<ts>Z/`.

## §6. Architect §8 — verbatim sign-off

**SIGNED 2026-05-11 session #39 close.**

> 用户 verbatim: **"好，确认可以 ship"**

Canonical Class-4 §8 多句式 (per CLAUDE.md §10 + §1 multi-clause analysis):
- Clause 1 acknowledgment: `好`
- Clause 2 named act: `确认`
- Clause 3 scope: `可以 ship`

Structurally identical to all prior accepted v4 canonical §8 forms (TB-C0 2026-05-07, P-M2 2026-05-09, P-M6 2026-05-09, A3 2026-05-10, A4 2026-05-10, B2 2026-05-11). **Seventh canonical §8 form invocation in v4 history.**

Authorized scope (per §2 + §5):
- **Allowed path**: cut branch `feat/g1-1-resume-mode` from `feat/tb-n3-autorun-20260511T051910Z` HEAD `16682a9`; land G1.1 surfaces as enumerated in §2; run PRE-§8 Codex G2 + Gemini DT dual audit covering Q1..Q9 in §5; run 3-problem mini real-LLM smoke; ship under this §8 once dual audit PASSes (round-cap 2; PRE-§8 audit per `feedback_dual_audit`).
- **Forbidden path**: changes outside §2 file list; touch `src/state/typed_tx.rs` / `src/bottom_white/ledger/system_keypair.rs` / `genesis_payload.toml` Trust Root; batch G2 / G2P / G3 / G4 / G5 / G6 / G7 atoms into the same commit; bypass dual audit; bypass real-LLM mini smoke.
- **Risk class**: **4 STEP_B**.
- **Audit required**: yes (dual audit Codex G2 + Gemini DT round-cap=2; Trust Root verify; real-LLM mini smoke).
- **Ship authorized**: yes, conditional on (a) dual audit PASS and (b) mini smoke `verdict=Ok delta=0 audit_proceed=true inv1_match=true`.

## §7. Pre-§8 checklist (status at packet draft time)

| Item | Status |
|------|--------|
| Charter at `handover/tracer_bullets/TB_G_GENERATIVE_ARENA_charter_2026-05-11.md` | ✅ landed (G0.1) |
| Architect directive at `handover/directives/2026-05-11_G_PHASE_GENERATIVE_ARENA_DIRECTIVE.md` | ✅ landed (`2c110dc`) |
| Matrix §R row for G1 | ✅ landed (G0.3) at `handover/alignment/CONSTITUTION_EXECUTION_MATRIX.md` |
| Branch `feat/g1-1-resume-mode` | ⏸ NOT CUT (architect §8 required first per CLAUDE.md §9 Class 4) |
| Implementation | ⏸ NOT STARTED |
| Tests `tests/constitution_g1_resume.rs` | ⏸ NOT WRITTEN |
| Dual audit dispatch | ⏸ POST-implementation |
| Real-LLM mini smoke | ⏸ POST-dual-audit-PASS |
| Trust Root manifest | (NO rehash expected; STEP_B applies to `src/state/sequencer.rs` body change but the file is already in the manifest from prior STEP_B atoms) |

## §8. HALT condition (CLEARED 2026-05-11)

Previous status: HALT — packet draft was the maximum autonomous Class-0 contribution per `feedback_no_batch_class4_signoff` + CLAUDE.md §9 Class 4.

**Cleared by §6 sign-off** ("好，确认可以 ship" 2026-05-11 session #39 close). Forward implementation now AUTHORIZED under the scope in §6, conditional on dual audit PASS + mini smoke `verdict=Ok delta=0`.

Forward-bound items now unblocked (per `feedback_no_batch_class4_signoff` Class-3 atoms PROCEED after dual-audit PASS; new Class-4 atoms still need their own §8 packet):
- G1.2 (batch driver binary; Class 3 — autonomous after G1.1 ships)
- G1.3 (persistent batch wrapper script; Class 2 — autonomous after G1.1 ships)
- G1.4 (persistence-evidence binding test; Class 2 — autonomous after G1.1 ships)
- G2 / G2P (parallel priorities; Class 2 — autonomous after G1.1 ships)
- G3.1 / G3.3 / G3.4 (Class 2-3 — autonomous after G1.1 ships)
- G3.2 (Class 4 — REQUIRES new §8 packet before implementation)
- G4.1 / G4.3 / G4.4 (Class 2 — autonomous after G1.1 ships)
- G4.2 (Class 4 — REQUIRES new §8 packet before implementation)
- G5 / G6 / G7 (Class 1-3 — autonomous after G1.1 + G3.2 + G4.2 all ship)

Status: §8 SIGNED; FRESH SESSION HANDOFF (clean context for Class-4 STEP_B implementation work).
