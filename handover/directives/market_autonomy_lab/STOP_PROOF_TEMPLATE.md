# STOP_PROOF Template

Create `STOP_PROOF.md` only when Level 3 Constitutional Hard Stop is reached.

Required fields:

```text
date:
goal_id:
mode:
envelope_id:
stop_level: Level 3 Constitutional Hard Stop
exact_stop_clause:
touched_or_required_surface:
forbidden_mechanism_if_any:
evidence_contamination_if_any:
trust_root_status:
constitution_gate_status:
in_envelope_alternatives_considered:
why_each_alternative_failed:
last_clean_evidence_dir:
last_audit_tape_verdict:
next_required_user_or_architect_action:
```

Forbidden stop reasons:

```text
no E2 candidate
clean-negative
all abstain
PolicyTrader abstains
allowed Trust Root rehash required
hard10 produced too few trades
CHALLENGE that is fixable inside the envelope
```

