# Auto Research Plan — 2026-04-14 Session

## Context (已完成)
- Mathlib 安装 + oracle 前检就位（C-012）
- Baseline 测量诚信：n=50 分层抽样
  - oneshot: 34/50 = 68%, ΣPPUT=29.71
  - n3: 39/50 = 78%, ΣPPUT=35.60
  - **Δ: +5 OMEGA, +19.8% PPUT — 架构价值已经量化确认**

## Known Findings (审计基础)
1. **9 n3 救援 / 4 n3 回归**：救援分布广（7 类别），回归集中在"oneshot 险胜"边界题
2. **induction 回归**（2/2 题 n3 慢 60-78%）：inductive proofs oneshot 强，n3 探索三次税重
3. **gp_node_count=0 on n3 complete**：token 计数失真（payload 走 complete 不走 tape）
4. **imo/advanced 有突破**：n3 首解 imo_1959_p1（架构触及新能力边界）

## Proposed Next Steps (按价值/风险排序)

### Step 1 — 修度量：gp_token_count 对 complete 正确计数
**问题**：n3 直接 `complete` 时 `gp_tokens=0`，PPUT 没算 LLM tokens 成本。
**修复**：complete 分支记录 response.completion_tokens 作为 gp_token_count。
**宪法对齐**：
- **C-012** 度量正确性 > 被度量对象优化：现有度量有漏计
- **Art. I.2** 统计信号：PPUT 应反映真实资源消耗
**风险**：极小，~5 行代码
**预期收益**：PPUT 分数更真实（可能下调 n3 绝对值，但对比 oneshot 依然成立）

### Step 2 — 修架构税：n3 hybrid（oneshot-first + swarm-fallback）
**问题**：n3 在"oneshot 能单打赢"的题上浪费 2-3 次探索（+ -0.72 induction，+ 4 回归）
**修复**：run_swarm 增加 tx_-1（预热）：先用 oneshot 式 prompt 试一次，失败才进 swarm loop
**宪法对齐**：
- **Art. II.2.1** 探索/利用：先利用已知最优路径，再探索
- **不违 C-005 贪婪路由**：oneshot 一次是固定预算，非贪婪
- **不违 Art. III.1 屏蔽错误**：失败会触发 swarm，错误不被吃掉
**风险**：中等。需改 evaluator.rs run_swarm 结构。
**预期收益**：
- 最佳：回收 4 回归（+1.02 PPUT）+ induction PPUT 追上 oneshot（+0.72 PPUT）= **+1.74 PPUT**
- 最差：退化为 oneshot 行为（保底不劣化）

### Step 3 — 扩分子：prompt 加 Mathlib tactic hints
**问题**：当前 prompt 极简，DeepSeek 缺 tactic 线索；某些 mathlib-specific 技巧（`omega`, `decide`, `norm_num`, `polyrith`）未激活
**修复**：prompt 加 "Common tactics: simp, ring, linarith, norm_num, omega, decide, polyrith" 提示
**宪法对齐**：
- **Art. II.1** 广播典型错误（反向：广播典型解法）
- **V3L-40** 反锚点：tactic 名是策略空间而非数值锚点，不违反
- **V3L-17** 不截断推理：只加提示，不裁推理深度
**风险**：小，仅改 prompt.rs 和 oneshot prompt
**预期收益**：+3-10% OMEGA（特别是 imo/hard algebra 可能受益）

### Step 4 — 全 244 题权威基线
**前置**：Step 1-3 完成后执行，避免在次优系统上烧钱
**宪法对齐**：**Art. I.2** 统计信号需要完整样本；**C-012** 基线必须在冻结度量下测
**预算**：~24h 双条件运行，~$60-100 API
**收益**：可发布的 MiniF2F 成绩、架构完整验证

## 执行边界（Art. V.2）
- Step 1-3 可单线程 auto 执行，每步完成后回 Step 4 复查
- Step 4 启动前需再次 dual-audit（双阶段审计）
- 任何 cargo test 失败 → 立即停止，不自主诊断深入修改
- 若度量工具（oracle）异常 → 立即停止 + 报告（C-012 红线）

## 审计请求
本 plan 需要 **Codex** + **Gemini** 双审，焦点：
1. 每步的宪法对齐是否站得住？
2. Step 2 hybrid 是否违反 n3 纯净性？
3. Step 3 tactic hints 是否构成 V3L-40 锚点？
4. 优先顺序是否合理（OMEGA 门槛 vs PPUT 增益）？
