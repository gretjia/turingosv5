# Auto Research Plan v2 — 2026-04-14 (Post-Audit)

## Audit Dissent Resolution
- **Gemini**: 4/4 PASS, suggest +Step 5 RCA
- **Codex**: Step 1 PASS, Step 2 CHALLENGE, **Step 3 VETO**, Step 4 CHALLENGE; RCA as blocker not addendum
- **Resolution**: Codex vetoes binding (constitutional citations precise; conservative wins per C-017). Plan rebuilt.

## Plan v2

### Step 1 (UNCHANGED from v1) — Fix gp_token_count measurement
Record `response.completion_tokens` when agent takes `complete` action directly.
**Constitutional**: C-012, Art. I.2 (measurement correctness is prerequisite).
**Status**: Both auditors PASS. Execute.

### Step 2 (PROMOTED from v1 Gemini addendum) — Regression RCA (BLOCKER)
Analyze mechanism of:
- 4 regressions: amc12_2000_p20, amc12_2001_p21, numbertheory_2pownm1prime_nprime, numbertheory_3pow2pownm1...
- induction category −0.72 PPUT drop
Method: examine stderr per problem, identify whether n3 tx 0-2 parse OK but verify fail, or timeout via exploration thrash.
**Constitutional**: C-031 ("制度正确时调参才有意义"), C-033 (因果透明), C-017 (不静默).
**Output**: root cause + recommendation (mechanism fix OR conditionally accept tax).

### Step 3 (CONDITIONAL from v1 Step 2) — Hybrid condition IF AND ONLY IF RCA justifies
Rename to `hybrid_v1` (not `n3`) to preserve causal attribution (C-032, C-033).
tx_-1 uses oneshot prompt (no swarm state); on failure, swarm loop begins fresh.
**Constitutional**: Art. II.2.1 exploit-then-explore, C-005 (not greedy since swarm always fires on fail), C-021 (不能因能力强就不约束).
**Execution gate**: only if RCA identifies tax as unavoidable; if mechanism fixable (e.g., agent coordination), prefer fix.

### v1 Step 3 — REMOVED (Codex VETO)
Adding Mathlib tactic hints `omega/decide/norm_num/...` VIOLATES C-011 ("decide/omega/native_decide 应在 forbidden_patterns 拦截"). Also violates C-031/C-034 (prompt steering is soft control, inferior to mechanism).

**Optional alternative** (separate plan): fix `omega`/`decide` enforcement in `bus.rs forbidden_patterns` per C-011. This is **constitutional debt**, not a research step. Out of scope here.

### Step 4 (UNCHANGED in spirit, reframed) — Full 244 baseline
Execution gate:
1. Step 1 (metric fix) merged + tested
2. Step 2 (RCA) completed with decision recorded
3. Conditions frozen: separate `oneshot` and `n3` (and `hybrid_v1` if adopted) as distinct runs
4. Same prompt for same condition across all problems (no mid-run changes)

Budget: ~24h × 2-3 conditions, ~$60-100.

## Execution Protocol
1. Each step completion → log in handover
2. Any cargo test fail or oracle anomaly → halt, alert, no autonomous fix
3. Step 2 (RCA) results may **further modify** Step 3/4 — re-plan allowed
4. Post-Step-4 → new plan with fresh dual audit

## Constitutional Debt Registered (not in plan scope but logged)
- `bus.rs forbidden_patterns` missing `decide`, `omega` per C-011
- `gp_node_count` and related tape-based PPUT accounting assumptions for non-tape wins (complete tool)
