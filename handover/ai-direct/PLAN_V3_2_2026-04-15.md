# Plan v3.2 — Chat-Model Comparison (post-M4)

## Background
v3.1 (reasoner) complete. n1 +7 STRICT WIN over oneshot on N=50. User thesis: TuringOS scaffold is externalized CoT; reasoner + scaffold is double-reasoning. Test: chat-model on same sample + conditions.

## Hypothesis (restated narrowly)
**Primary**: chat + TuringOS scaffold achieves ≥80% of reasoner + scaffold SolveRate at ~10× lower cost/latency.
**Secondary**: chat + scaffold drives higher `append` usage than reasoner + scaffold (mechanism activation signal).

## Design (minimal change from v3.1)

- **Sample**: identical — `analysis/sample_N50_S74677.txt` (fp=796ead6c40351ae9)
- **Conditions**: oneshot, n1, n3 (same three)
- **Model**: `ACTIVE_MODEL=deepseek-chat` (was `deepseek-reasoner`)
- **Prompt/schema**: unchanged
- **Per-problem timeout**: 900s
- **Abort gate**: 10-problem / 3-timeout per condition
- **Interleaving**: 3-rotation per problem (same as v3.1)
- **Frozen metric**: `experiments/minif2f_v4/metrics.yaml` — FIRST committed in `513966f` (v3.2 pre-reg). Applies prospectively to v3.2 and retrospectively documents v3.1 decision rules (which lived in v3.1 plan doc).
- **Frozen analyzer**: `frozen_analysis.py` unchanged since commit `1fa9b32` (v3.1 re-freeze)

## Expected outcomes (precise, using concrete numbers)

**v3.1 reference (commit e58e021)**: oneshot 23/50, n1 30/50, n3 7/50 (abort).

Per `metrics.yaml` decision rules applied PAIRWISE (v3.2 condition X vs v3.1 condition X):

| Pairwise outcome | Thesis verdict |
|---|---|
| `n1_chat` − `n1_reasoner` ≥ +3 | **CONFIRMED PLUS** — chat+scaffold outperforms reasoner+scaffold |
| `|n1_chat − n1_reasoner|` ≤ 1 | **CONFIRMED** — chat ≈ reasoner; scaffold is the work-horse |
| difference of exactly 2 | **GRAY** — inconclusive |
| `n1_reasoner − n1_chat` ≥ 3 | **FALSIFIED** — reasoner carries performance |

Same rule applied to oneshot and n3 (paired-subset) independently.

**Concrete pre-registered numbers** (derived from v3.1 + decision rule):
- CONFIRMED requires: `n1_chat` ∈ [29, 31] (within 1 of 30) OR `n1_chat` ≥ 33 (strict win)
- FALSIFIED when: `n1_chat` ≤ 27

## Pre-registered decision rules (frozen per metrics.yaml commit)

- **Primary**: SolveRate per condition; pairwise Δ vs v3.1 values
- **Secondary**: Aggregate_PPUT; tie-break only if primary EQUIVALENT
- **Tertiary (informational only, NEVER decision-relevant)**:
  - `gp_node_count` (tape depth at OMEGA) — descriptive
  - `gp_token_count` (completion tokens) — cost accounting
  - These are reported in the analysis output but DO NOT confirm/falsify the thesis. Any later thesis using these MUST be pre-registered in a new plan (v3.3+) with primary-metric status.

**Notice of retraction from prior doc**: `HYPOTHESIS_CHAT_MODEL_2026-04-15.md` §Testable prediction mentioned `append_rate(chat) > append_rate(reasoner)` as "thesis confirmed" test. That framing is RETRACTED. Append-rate is informational-only under v3.2. The v3.2 verdict is determined solely by SolveRate pairwise rule above.

## Cost/time budget

- ~50 × 3 × ~30s avg (chat is ~5-10× faster than reasoner) = ~75 min wall
- ~$4 API (vs $25 for reasoner v3.1)
- Hard cap: 2× expected = 2.5h; 2× API = $8

## Execution order

1. Pre-registration frozen: this plan committed + `sample_N50_S74677.txt` + `frozen_analysis.py` (all from commits `906d64b`, `1fa9b32`, `e58e021`)
2. Run: `ACTIVE_MODEL=deepseek-chat ./run_interleaved.sh`
3. Frozen analysis: `python3 frozen_analysis.py --sample ... --oneshot ... --n1 ... --n3 ...`
4. Dual audit (Codex + Gemini) on results
5. If PASS: commit + push whitelist (same pattern as M4)
6. If thesis CONFIRMED: update notepad §2, queue v3.3 (Step-B bus.rs fix) as natural continuation
7. If thesis FALSIFIED: update notepad §3 retractions, queue mechanism engineering (C-034 direction)

## Constitutional alignment

- **Art. V.1.2 (ArchitectAI proposer)**: I propose this experiment. Subject to dual audit.
- **C-010 (generator ≠ evaluator)**: same external seed, same frozen analyzer, same protocol as v3.1 — no researcher-side choice that differs from v3.1 except model identity
- **C-012 (measurement freeze)**: metrics/analyzer frozen at commits `906d64b`/`1fa9b32`; no edits during v3.2 batch
- **C-027 (no hardcoded params)**: temperature, max_tokens still hardcoded (debt registered in notepad §6; not blocking v3.2 since values same as v3.1 control) — to fix before v3.3
- **C-033 (causal attribution)**: model change is the single variable → any Δ is attributable to model family, not confounded
- **C-034 (mechanism > prompt)**: testing whether mechanism engages under weaker model; this is mechanism-level test, not prompt engineering

## Risks

| Risk | Mitigation |
|---|---|
| DeepSeek chat API instability | Same retry logic as v3.1 (ResilientLLMClient) |
| Chat model produces different output format vs reasoner | Schema prompt unchanged; parse_agent_output should handle |
| Chat hits context limit faster | max_tokens=16000 (same); chat typically uses less anyway |
| n3 abort gate triggers (same as v3.1) | Accepted; paired-subset analysis remains valid |
| 20 n1 timeouts again | May repeat; same frozen metric treats as failure |

## Audit request

Read notepad §2, §3, §5. Verify:
- Q1. Is single-variable change (model) sufficient to isolate scaffold-vs-CoT contribution per C-033?
- Q2. Are frozen artifacts (sample, analyzer, metrics) unchanged since M4 commit?
- Q3. Is append_count a legitimate tertiary metric or secondary-by-stealth? (If secondary, must be in metrics.yaml.)
- Q4. Is the 80% / 50% / ≪ interpretation rule pre-registered and not a retrofit?
- Q5. Any hidden C-010 / C-012 issues?
- Q6. Any Step-B candidates that should precede v3.2?
