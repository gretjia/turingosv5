# Section A: Overall Verdict

**Verdict: CHALLENGE. Conviction: 4/5.**

Core runtime correctness looks good: the WorkTx arm is pure, the rejection path preserves K1/no-state-advance discipline, the six r2 P0 compile-shape issues are addressed, `cargo check --workspace`, `cargo test --test tb_2_runtime_boundary`, `cargo test --lib state::sequencer`, and `cargo test --workspace` all passed locally. However, I do not clear merge because the diff exceeds the exact preflight surface in public API visibility and misses one literal STEP_B red-line annotation requirement. These are not VETO-class runtime breaks, but they are before-merge scope/constitutional hygiene fixes.

# Section B: Per-Question Disposition

**Q1 — CHALLENGE.** Functionally, the implementation tracks the section-3 list: `SubmissionEnvelope` exists at `src/state/sequencer.rs:302`, `queue_tx` now carries it at `src/state/sequencer.rs:434`, `rejection_writer` is present at `src/state/sequencer.rs:444`, the WorkTx arm is filled at `src/state/sequencer.rs:159`, `WORKTX_ACCEPT_DOMAIN_V1` and `worktx_canonical_hash` are at `src/state/sequencer.rs:57` and `src/state/sequencer.rs:65`, rejection helpers are at `src/state/sequencer.rs:107` and `src/state/sequencer.rs:129`, and the rejection path is at `src/state/sequencer.rs:581`. The 544-line `sequencer.rs` delta is large but mostly tests and trace comments, and the preflight itself declares these targets at `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:76` and `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:422`. The challenge is public surface: the preflight snippet showed `WORKTX_ACCEPT_DOMAIN_V1` as `pub(crate)` and `worktx_canonical_hash` as private at `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:203`, but implementation exposes them as `pub` at `src/state/sequencer.rs:57` and `src/state/sequencer.rs:65` for integration tests (`tests/tb_2_runtime_boundary.rs:34`). That is small, but it is extra exported API.

**Q2 — PASS.** All six r2 P0s are honored. P0-1: the field is exactly `Arc<RwLock<RejectionEvidenceWriter>>` at `src/state/sequencer.rs:444`, matching `append_rejected(&mut self)` at `src/bottom_white/ledger/rejection_evidence.rs:258`. P0-2: `SYSTEM_AGENT_ID_STR` exists at `src/state/sequencer.rs:100`, and the rejection path uses `submitter_id().unwrap_or_else(...)` at `src/state/sequencer.rs:634`. P0-3: stake uses `micro_units()` at `src/state/sequencer.rs:185`, and the bridge uses `.escrows_t.0.contains_key` / `.task_markets_t.0.contains_key` at `src/state/sequencer.rs:198`; the newtypes are tuple wrappers at `src/state/q_state.rs:161` and `src/state/q_state.rs:224`. P0-4: `StaleParent` is reused at `src/state/typed_tx.rs:720`, only `EscrowMissing` and `MonetaryInvariantViolation` are added at `src/state/typed_tx.rs:788` and `src/state/typed_tx.rs:792`, and Display has exactly those new arms at `src/state/typed_tx.rs:825`. P0-5: integration tests use constructor injection and retained `Arc<RwLock<...>>` at `tests/tb_2_runtime_boundary.rs:142` and `tests/tb_2_runtime_boundary.rs:154`; no `rejection_writer_for_test` accessor was found. P0-6: rejection CAS puts take a write lock and call the 5-arg `put` at `src/state/sequencer.rs:597` and `src/state/sequencer.rs:615`, matching the signature at `src/bottom_white/cas/store.rs:163`.

**Q3 — PASS.** The `dispatch_transition` WorkTx arm is pure in the relevant sense. It reads `q`, checks parent/predicates/stake/escrow at `src/state/sequencer.rs:164`, `src/state/sequencer.rs:170`, `src/state/sequencer.rs:177`, `src/state/sequencer.rs:185`, and `src/state/sequencer.rs:197`, calls invariant functions at `src/state/sequencer.rs:213` and `src/state/sequencer.rs:229`, clones local `q_next` at `src/state/sequencer.rs:222`, mutates only that clone's `state_root_t` at `src/state/sequencer.rs:223`, and returns at `src/state/sequencer.rs:236`. There is no CAS, ledger writer, rejection writer, lock acquisition, or external mutation in `src/state/sequencer.rs:159` through `src/state/sequencer.rs:237`.

**Q4 — PASS.** The rejection path is disciplined. `dispatch_transition` is called once at `src/state/sequencer.rs:581`; on `Err`, the branch appends L4.E using `submit_id` from the envelope at `src/state/sequencer.rs:643`, then immediately returns `ApplyError::Transition` at `src/state/sequencer.rs:657`. It does not write `q` or `ledger_writer`; those locks are only acquired on the accepted path at `src/state/sequencer.rs:724` and `src/state/sequencer.rs:726`. It does not advance accepted `logical_t`: `rejection_logical_t` is only a snapshot for CAS metadata at `src/state/sequencer.rs:595`, while the only `next_logical_t.store(...)` is accepted-path-only at `src/state/sequencer.rs:732`. CAS writes on rejection are orphan-tolerant per preflight §3.6 at `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:337`.

**Q5 — PASS.** The 16-test battery is present and green. U1/U2/U3 are in `src/state/sequencer.rs` at `src/state/sequencer.rs:1001`, `src/state/sequencer.rs:961`, and `src/state/sequencer.rs:1060`; I1-I13 are in `tests/tb_2_runtime_boundary.rs` at `tests/tb_2_runtime_boundary.rs:418`, `tests/tb_2_runtime_boundary.rs:446`, `tests/tb_2_runtime_boundary.rs:188`, `tests/tb_2_runtime_boundary.rs:220`, `tests/tb_2_runtime_boundary.rs:255`, `tests/tb_2_runtime_boundary.rs:285`, `tests/tb_2_runtime_boundary.rs:314`, `tests/tb_2_runtime_boundary.rs:371`, `tests/tb_2_runtime_boundary.rs:510`, `tests/tb_2_runtime_boundary.rs:555`, `tests/tb_2_runtime_boundary.rs:585`, `tests/tb_2_runtime_boundary.rs:615`, and `tests/tb_2_runtime_boundary.rs:645`. I3-I6 assert both `submit_id` and `rejection_class` at `tests/tb_2_runtime_boundary.rs:211`, `tests/tb_2_runtime_boundary.rs:243`, `tests/tb_2_runtime_boundary.rs:277`, and `tests/tb_2_runtime_boundary.rs:306`. I7 asserts state root, ledger root, logical counter, and 4 L4.E rows at `tests/tb_2_runtime_boundary.rs:356`. I8 serializes both the raw record and public view and checks `raw_diagnostic_cid` absence at `tests/tb_2_runtime_boundary.rs:400` and `tests/tb_2_runtime_boundary.rs:410`. I12 asserts zero L4.E delta at `tests/tb_2_runtime_boundary.rs:620` and `tests/tb_2_runtime_boundary.rs:634`. I13 uses `replay_full_transition` (`src/bottom_white/ledger/transition_ledger.rs:395`) over entries collected from `LedgerWriter::read_at` at `tests/tb_2_runtime_boundary.rs:693`, not a `q_snapshot()` shortcut; `q_snapshot()` is only the live comparison target at `tests/tb_2_runtime_boundary.rs:682`.

**Q6 — PASS.** `assert_total_ctf_conserved` appears in `sequencer.rs` only as an import at `src/state/sequencer.rs:38` and one production call at `src/state/sequencer.rs:229`, and that call passes `&[]` at `src/state/sequencer.rs:232`. This satisfies preflight red line 4 at `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:554`.

**Q7 — CHALLENGE.** The changed file set matches the expected broad list from `git diff --name-only f9ace5e..HEAD`: `genesis_payload.toml`, smoke evidence, `rules/enforcement.log`, `src/bottom_white/ledger/transition_ledger.rs`, `src/state/sequencer.rs`, `src/state/typed_tx.rs`, and `tests/tb_2_runtime_boundary.rs`. There are no diffs to the forbidden `src/kernel.rs`, `src/bus.rs`, or `src/sdk/tools/wallet.rs`, whose ban is stated at `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:557`. The challenge is narrow: `transition_ledger.rs` is test-only, but it is not literally "JUST matches!() change"; it rewrites the explanatory comment at `src/bottom_white/ledger/transition_ledger.rs:1260` and the assertion message at `src/bottom_white/ledger/transition_ledger.rs:1294`. I do not see a Trojan there, but it exceeds the exact wording of the verification question.

**Q8 — PASS.** Smoke evidence integrity holds. The TB-2 oneshot log carries `prompt_context_hash":"a1f43584a17d1226"` at `handover/evidence/tb_2_phase1_smoke_2026-04-30/oneshot_run.log:2`. The TB-1 Day-1 spike records the same hash at `handover/tracer_bullets/TB-1_day1_spike_2026-04-29.md:15` and `handover/tracer_bullets/TB-1_day1_spike_2026-04-29.md:41`. The smoke README also says the hash is bit-identical at `handover/evidence/tb_2_phase1_smoke_2026-04-30/README.md:42`. This is a strong signal that prompt-build output was not perturbed by the TB-2 sequencer/runtime changes.

**Q9 — CHALLENGE.** Trojan sweep is mostly clean: no `unsafe ` occurs in `src/state/sequencer.rs`; `Sequencer::run` only changes receiver payload and forwards the envelope to `apply_one` at `src/state/sequencer.rs:514`; accepted-path CAS/sign/commit/Q update remains the existing sequence at `src/state/sequencer.rs:661` through `src/state/sequencer.rs:739`; `AcceptedLedger` appears only in a doc comment at `src/state/sequencer.rs:54`; `TypedTx` still has exactly the seven existing variants at `src/state/typed_tx.rs:608`. The challenge is public API creep: newly exported `WORKTX_ACCEPT_DOMAIN_V1` and `worktx_canonical_hash` appear at `src/state/sequencer.rs:57` and `src/state/sequencer.rs:65`, while preflight §3.4 showed narrower visibility at `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:203`. `try_apply_one` is acceptable because the preflight explicitly declared it at `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:100`, and `SubmissionEnvelope` public visibility is implied by public receiver/test-driver signatures at `src/state/sequencer.rs:474`, `src/state/sequencer.rs:516`, and `src/state/sequencer.rs:536`.

# Section C: New Constitutional Debt

1. `src/state/sequencer.rs:57` and `src/state/sequencer.rs:65` export `WORKTX_ACCEPT_DOMAIN_V1` and `worktx_canonical_hash`, while the preflight snippet specified `pub(crate)` / private helper shape at `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:203`. **Remediation: before merge**, either make the helper/const crate-private and adjust `tests/tb_2_runtime_boundary.rs:34` plus the expected-hash computation at `tests/tb_2_runtime_boundary.rs:533` to use already-public primitives, or explicitly amend the preflight/trace rationale to authorize this public API.

2. `src/state/sequencer.rs:197` does not carry the exact inline deletion-marker required by preflight red line 9 at `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:559`. The surrounding comment at `src/state/sequencer.rs:189` does say TB-3 deletes the bridge, so the semantic intent is visible, but the literal red-line requirement is not satisfied. **Remediation: before merge**, add the required inline `TB-2 P0-B option (a): drop this when task_open_tx lands in TB-3` marker or update the preflight to accept the existing adjacent comment.

# Section D: Trojan / Scope-Creep Findings

No runtime Trojan found. There are no forbidden-file diffs to `src/kernel.rs`, `src/bus.rs`, or `src/sdk/tools/wallet.rs`; no `unsafe` in `src/state/sequencer.rs`; no production `AcceptedLedger::append_accepted`; no `EscrowVault` call from the WorkTx arm; no new `TypedTx` variants; and `apply_one` accepted-path semantics remain the same CAS/sign/commit/Q update sequence at `src/state/sequencer.rs:661`.

Scope creep found but bounded: public helper visibility at `src/state/sequencer.rs:57` and `src/state/sequencer.rs:65`, plus test-comment/assertion-message churn in `src/bottom_white/ledger/transition_ledger.rs:1260` and `src/bottom_white/ledger/transition_ledger.rs:1294`. Neither changes runtime behavior, but both should be cleaned or explicitly sanctioned before merge.

# Section E: Recommendation

**Recommendation: revise diff; merge not cleared.**

Before merge:

1. Resolve the public API visibility mismatch for `WORKTX_ACCEPT_DOMAIN_V1` / `worktx_canonical_hash`, or explicitly amend the preflight to allow the exported test helper surface.
2. Add the exact TB-3 deletion-marker inline comment to the `TxId(work.task_id.0.clone())` bridge, or amend the red-line wording.
3. Either trim `transition_ledger.rs` to the expected assertion-only change or explicitly accept the comment/message update as test-only explanatory churn.
4. Re-run `cargo check --workspace`, `cargo test --test tb_2_runtime_boundary`, `cargo test --lib state::sequencer`, and `cargo test --workspace`; then re-audit only the remediation delta.
