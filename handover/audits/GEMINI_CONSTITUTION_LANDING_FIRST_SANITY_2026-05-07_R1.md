# Gemini Constitution Landing First Sanity Audit

**Round**: R1
**Date**: 2026-05-07
**Model**: gemini-2.5-pro
**Elapsed**: 69.2s
**Prompt size**: 177,524 chars
**Final aggregate verdict**: CHALLENGE

## Extracted Per-Q Breakdown

Q1: PASS - The `HEAD_t` C1 witness as a derived view is sufficient for FC2 replay assertions. The design correctly treats the witness as a materialized view, not a new source of truth, which aligns with the Tape Canonical principle (Art. 0.2). Its constructor takes a read-only `&QState`, enforcing its derived nature. The risk of drift during replay is appropriately managed by the replay assertion itself: a mismatch indicates a replay failure, which is the desired behavior for a verifier. This implementation is a valid and safe instantiation of the "Path C hybrid" decision.

Q2: PASS - The `PromptCapsule` implementation exactly matches the architect-pinned seven-field schema in CLAUDE.md §4.3. The test `prompt_capsule_struct_field_count_is_exactly_seven` correctly pins this. Refusing to construct a capsule where `hidden_fields_redacted=false` is the correct architectural hard fence. It enforces the Art. III selective shielding principle at the earliest possible point (object instantiation), preventing unredacted, sensitive prompt information from ever becoming a valid Class-3 tape artifact.

Q3: PASS - The PCP corpus in `cases/pcp_corpus/` and its `MANIFEST.json` correctly and completely cover the architect-pinned nine-class mutation taxonomy from HARNESS.md §3 G-012. The classes are present, and their expected outcomes and routing destinations (`L4_accepted`, `L4_E`, `CAS_only`) are correctly classified in the manifest, as verified by the `tests/constitution_pcp_corpus.rs` gates.

Q4: PASS - The 50-problem Wave 3 evidence provides sufficient proof for the "P38 / P49 attempt equality green" resume condition. The FC1 invariant `460 = 9 + 400 + 51` holds in aggregate and for all 50 individual problems, including P38 and P49. This demonstrates at a non-trivial scale that every externalized LLM attempt is correctly accounted for on tape, satisfying the core requirement of the gate.

Q5: PASS - The Class-3 classification for both `head_t_witness` and `prompt_capsule` is consistent with `feedback_class4_cannot_hide_in_class3`. `HeadTWitness` is a derived, read-only view and does not alter any canonical state or signing payload. `PromptCapsule` is a new CAS artifact linked via `AttemptTelemetry` (also a Class-3 CAS object), correctly avoiding any modification to the Class-4 `WorkTx` typed transaction schema or its canonical signing payload. Neither component changes sequencer admission semantics.

Q6: PASS - The Constitution matrix promotions to GREEN for Art.0.4, Art.I.1.1, and Art.III prompt-persistence were earned under the CR-C0.7 standard. In all three cases, the unit and structural tests were supplemented by the Wave 3 real-LLM evidence runs (20p and 50p), which exercised the components on the "real path" under load and passed, fulfilling the requirement for a GREEN status.

Q7: PASS - Deferring Wave 1/2 cleanup is a defensible "strict-alignment win," not covert sequencing manipulation. The development effort correctly prioritized the load-bearing blockers identified in `PROJECT_PLAN §3` as formal "resume conditions." Tackling these hard dependencies first to un-gate the project is a sign of rational, goal-oriented execution. This is the opposite of the forbidden "manipulation by sequencing," which involves closing easy gaps for optics while leaving blockers unresolved.

Q8: CHALLENGE - While the landed substrate is sound for its immediate purpose, it carries significant architectural risks that will be exposed by the next phase of benchmark scale-up (TB-18B). The primary risk is that the PCP soundness gate (Art. I.1.1) relies on a purely synthetic adversarial corpus; its effectiveness against subtle, real-world invalid proofs generated by LLMs at scale is unproven. A secondary, but still critical, risk is the unknown status of the Art. 0.2 Tape Canonical violation cleanup; if significant parallel ledgers persist, their drift at scale could corrupt economic and cost-attribution data, undermining benchmark integrity.

---

## Verbatim Gemini Response

Q1: PASS - The `HEAD_t` C1 witness as a derived view is sufficient for FC2 replay assertions. The design correctly treats the witness as a materialized view, not a new source of truth, which aligns with the Tape Canonical principle (Art. 0.2). Its constructor takes a read-only `&QState`, enforcing its derived nature. The risk of drift during replay is appropriately managed by the replay assertion itself: a mismatch indicates a replay failure, which is the desired behavior for a verifier. This implementation is a valid and safe instantiation of the "Path C hybrid" decision.

Q2: PASS - The `PromptCapsule` implementation exactly matches the architect-pinned seven-field schema in CLAUDE.md §4.3. The test `prompt_capsule_struct_field_count_is_exactly_seven` correctly pins this. Refusing to construct a capsule where `hidden_fields_redacted=false` is the correct architectural hard fence. It enforces the Art. III selective shielding principle at the earliest possible point (object instantiation), preventing unredacted, sensitive prompt information from ever becoming a valid Class-3 tape artifact.

Q3: PASS - The PCP corpus in `cases/pcp_corpus/` and its `MANIFEST.json` correctly and completely cover the architect-pinned nine-class mutation taxonomy from HARNESS.md §3 G-012. The classes are present, and their expected outcomes and routing destinations (`L4_accepted`, `L4_E`, `CAS_only`) are correctly classified in the manifest, as verified by the `tests/constitution_pcp_corpus.rs` gates.

Q4: PASS - The 50-problem Wave 3 evidence provides sufficient proof for the "P38 / P49 attempt equality green" resume condition. The FC1 invariant `460 = 9 + 400 + 51` holds in aggregate and for all 50 individual problems, including P38 and P49. This demonstrates at a non-trivial scale that every externalized LLM attempt is correctly accounted for on tape, satisfying the core requirement of the gate.

Q5: PASS - The Class-3 classification for both `head_t_witness` and `prompt_capsule` is consistent with `feedback_class4_cannot_hide_in_class3`. `HeadTWitness` is a derived, read-only view and does not alter any canonical state or signing payload. `PromptCapsule` is a new CAS artifact linked via `AttemptTelemetry` (also a Class-3 CAS object), correctly avoiding any modification to the Class-4 `WorkTx` typed transaction schema or its canonical signing payload. Neither component changes sequencer admission semantics.

Q6: PASS - The Constitution matrix promotions to GREEN for Art.0.4, Art.I.1.1, and Art.III prompt-persistence were earned under the CR-C0.7 standard. In all three cases, the unit and structural tests were supplemented by the Wave 3 real-LLM evidence runs (20p and 50p), which exercised the components on the "real path" under load and passed, fulfilling the requirement for a GREEN status.

Q7: PASS - Deferring Wave 1/2 cleanup is a defensible "strict-alignment win," not covert sequencing manipulation. The development effort correctly prioritized the load-bearing blockers identified in `PROJECT_PLAN §3` as formal "resume conditions." Tackling these hard dependencies first to un-gate the project is a sign of rational, goal-oriented execution. This is the opposite of the forbidden "manipulation by sequencing," which involves closing easy gaps for optics while leaving blockers unresolved.

Q8: CHALLENGE - While the landed substrate is sound for its immediate purpose, it carries significant architectural risks that will be exposed by the next phase of benchmark scale-up (TB-18B). The primary risk is that the PCP soundness gate (Art. I.1.1) relies on a purely synthetic adversarial corpus; its effectiveness against subtle, real-world invalid proofs generated by LLMs at scale is unproven. A secondary, but still critical, risk is the unknown status of the Art. 0.2 Tape Canonical violation cleanup; if significant parallel ledgers persist, their drift at scale could corrupt economic and cost-attribution data, undermining benchmark integrity.

## VERDICT: CHALLENGE
Conviction: high
Recommendation: FIX-THEN-PROCEED
Remediations:
- Before beginning TB-18B benchmark scale-up, the project must begin implementing the "MiniF2F-v2 misalignment" corpus (G-012 phase 2). This is necessary to test PCP soundness against real-world adversarial examples, not just the current synthetic ones. A plan for its creation and integration must be presented.
- The project must provide a status report confirming the completion of the required "Commit 1-4" from the Art. 0.2 Tape Canonical 10-commit remediation plan. If this work is not complete, it becomes a blocker for TB-18B, as benchmark integrity depends on a canonical, non-drifting ledger.