# Section A: Overall Verdict

**CHALLENGE, conviction 5/5.** Repo HEAD verified as `c5059a5e3a63b8c5c1dbe079b748cf258792f0bd`. v2 resolves the r1 concerns at the architectural-intent level, but not at the substrate/compile-shape level. The two biggest blockers are concrete: `Sequencer.rejection_writer: Arc<RejectionEvidenceWriter>` cannot call `append_rejected` because the writer append API is `&mut self`, and the v2 escrow bridge uses `.contains_key()` directly on `EscrowsIndex` / `TaskMarketsIndex` newtypes that do not expose that method (`src/bottom_white/ledger/rejection_evidence.rs:226`, `src/bottom_white/ledger/rejection_evidence.rs:258`, `src/state/q_state.rs:161`, `src/state/q_state.rs:224`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:106`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:133`). v2 should become a narrow v3 before STEP_B Phase-1 entry.

# Section B: Per-Q Disposition

**R2-Q1 [CHALLENGE].** `RejectionEvidenceWriter::default()` is constructible because the type derives `Default`, and the docs confirm RSP-0 is in-memory/deferred-persistence (`src/bottom_white/ledger/rejection_evidence.rs:20`, `src/bottom_white/ledger/rejection_evidence.rs:30`, `src/bottom_white/ledger/rejection_evidence.rs:226`). Literal shape:

```rust
pub struct RejectionEvidenceWriter {
    records: Vec<RejectedSubmissionRecord>,
}
```

The append shape is incompatible with v2's `Arc<RejectionEvidenceWriter>`:

```rust
pub fn append_rejected(
    &mut self,
```

(`src/bottom_white/ledger/rejection_evidence.rs:226`, `src/bottom_white/ledger/rejection_evidence.rs:258`, `src/bottom_white/ledger/rejection_evidence.rs:259`). Current `Sequencer` already protects mutable shared substrates as `Arc<RwLock<CasStore>>` and `Arc<RwLock<dyn LedgerWriter>>`, so the matching shape is `Arc<RwLock<RejectionEvidenceWriter>>` unless `RejectionEvidenceWriter` is refactored to internal mutability with `append_rejected(&self, ...)` (`src/state/sequencer.rs:238`, `src/state/sequencer.rs:241`). Existing `Sequencer::new` call sites under `src/ tests/` are exactly two: `src/state/sequencer.rs:483` and `src/state/sequencer.rs:627`; both need the new parameter if v3 keeps constructor injection.

**R2-Q2 [CHALLENGE].** The `TaskId` → `TxId` payload bridge itself is type-compatible: literal definitions are `pub struct TaskId(pub String);` and `pub struct TxId(pub String);`, so `TxId(tx.task_id.0.clone())` accepts the cloned `String` (`src/state/typed_tx.rs:33`, `src/state/typed_tx.rs:35`, `src/state/q_state.rs:67`, `src/state/q_state.rs:69`). There is no accessor requirement; `.0` is public. The compile break is the map access. Literal index definitions are `pub struct EscrowsIndex(pub BTreeMap<TxId, EscrowEntry>);` and `pub struct TaskMarketsIndex(pub BTreeMap<TxId, TaskMarketEntry>);` (`src/state/q_state.rs:159`, `src/state/q_state.rs:161`, `src/state/q_state.rs:222`, `src/state/q_state.rs:224`). v2's exact code `q.economic_state_t.escrows_t.contains_key(&lookup_tx_id)` will not compile against these newtype wrappers; it must use `.escrows_t.0.contains_key(...)` and `.task_markets_t.0.contains_key(...)` or add explicit helper methods (`handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:133`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:135`).

**R2-Q3 [CHALLENGE].** The in-crate U1-U3 placement is valid: this package is `turingosv4`, `src/lib.rs` exposes `pub mod state`, `src/state/mod.rs` exposes `pub mod sequencer`, and tests inside `src/state/sequencer.rs` can call `pub(crate)` `dispatch_transition` / `apply_one` (`Cargo.toml:1`, `Cargo.toml:2`, `src/lib.rs:12`, `src/state/mod.rs:15`, `src/state/mod.rs:16`, `src/state/sequencer.rs:47`, `src/state/sequencer.rs:332`). Integration tests under `tests/` are separate crates, so v2's `pub(crate) fn rejection_writer_for_test()` is not visible to I3-I13, yet v2's integration fixture plan says `assert_l4e_row_matches` uses that accessor (`handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:113`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:115`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:335`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:341`). `RejectionEvidenceWriter` does have public read APIs, `records()` and `public_view()`, so I3-I13 are expressible only if integration tests retain a public writer handle injected into the sequencer or v3 exposes a public read-only sequencer accessor (`src/bottom_white/ledger/rejection_evidence.rs:327`, `src/bottom_white/ledger/rejection_evidence.rs:330`, `src/bottom_white/ledger/rejection_evidence.rs:334`, `src/bottom_white/ledger/rejection_evidence.rs:340`). I13's replay foundation is public: `replay_full_transition` is `pub fn`, reruns CAS lookup and dispatch, and advances only from L4 entries (`src/bottom_white/ledger/transition_ledger.rs:371`, `src/bottom_white/ledger/transition_ledger.rs:395`, `src/bottom_white/ledger/transition_ledger.rs:442`, `src/bottom_white/ledger/transition_ledger.rs:486`).

**R2-Q4 [CHALLENGE].** Current `TransitionError` is not `#[non_exhaustive]`; it is a normal public enum (`src/state/typed_tx.rs:716`, `src/state/typed_tx.rs:717`). Existing variants are `StaleParent`, `SignatureInvalid`, `InvalidSystemSignature`, `StakeInsufficient`, `TargetWorkTxNotFound`, `TargetWorkTxNotVerifiable`, `ParentNotAcceptedYet`, `AcceptancePredicateFailed(PredicateId)`, `VerificationPredicateFailed(PredicateId)`, `SettlementPredicateFailed(PredicateId)`, `ChallengeWindowClosed`, `ChallengeWindowStillOpen`, `AlreadySlashed`, `CounterexampleInsufficient`, `ToolNotInRegistry`, `ToolCreatorMismatch`, `ClaimNotFound`, `TaskNotFound`, `TaskNotExpired`, `TaskHasOpenClaim`, `TerminalSummaryNotApplicable`, and `NotYetImplemented` (`src/state/typed_tx.rs:717`, `src/state/typed_tx.rs:787`). Existing direct `TransitionError::` use sites are the seven dispatch stubs, two sequencer tests, and one replay test (`src/state/sequencer.rs:54`, `src/state/sequencer.rs:60`, `src/state/sequencer.rs:583`, `src/state/sequencer.rs:611`, `src/bottom_white/ledger/transition_ledger.rs:1287`). The exhaustive `Display` match must also be updated for any new variant (`src/state/typed_tx.rs:790`, `src/state/typed_tx.rs:815`). v2's mapping table is not closed against today's enum: it uses `PredicateFailed` and `InvariantViolation`, neither of which exists or is among the three proposed additions; it proposes `StaleParentRoot` even though `StaleParent` already exists; and a no-wildcard match over only eight variants would omit most existing variants (`handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:222`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:241`).

**R2-Q5 [CHALLENGE].** v2 leaves several new substrate gaps. `WORKTX_ACCEPT_DOMAIN_V1` location is still ambiguous: §3.4 says `sequencer.rs or q_state.rs`, while §1 allows optional `q_state.rs`; this is consistent with the touched-file table but not a single implementable instruction (`handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:34`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:151`). `HasSubmitter` is implemented for all `TypedTx` variants, including `WorkTx`, and `TypedTx::tx_kind()` exists directly (`src/state/typed_tx.rs:620`, `src/state/typed_tx.rs:645`, `src/state/typed_tx.rs:698`), but `submitter_id()` returns `Option<AgentId>` while `append_rejected` requires `AgentId`, so v2's `envelope.tx.submitter_id()` argument will not type-check without an explicit `None` policy (`src/state/typed_tx.rs:641`, `src/state/typed_tx.rs:642`, `src/bottom_white/ledger/rejection_evidence.rs:258`, `src/bottom_white/ledger/rejection_evidence.rs:263`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:189`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:193`). `rejection_class_for` is named in §3.7, but `public_summary_for` is only referenced in the snippet and never specified (`handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:195`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:197`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:228`). I8 is expressible at the primitive level because `PublicRejectionView` omits `raw_diagnostic_cid`, `public_view()` is public, and `records()` can reveal the raw CID for the negative assertion, but only if tests can reach the writer handle (`src/bottom_white/ledger/rejection_evidence.rs:171`, `src/bottom_white/ledger/rejection_evidence.rs:178`, `src/bottom_white/ledger/rejection_evidence.rs:327`, `src/bottom_white/ledger/rejection_evidence.rs:340`). `canonical_hash(tx)` is not a generic existing helper in `src/state/`; the available generic primitive is `canonical_encode`, with unrelated `canonical_hash` methods only on CAS metadata and tool metadata (`src/bottom_white/ledger/transition_ledger.rs:559`, `src/bottom_white/ledger/transition_ledger.rs:562`, `src/bottom_white/cas/schema.rs:79`, `src/bottom_white/tools/registry.rs:89`). Additional compile-shape nits: `tx.stake > 0` cannot compare `StakeMicroCoin` to an integer, and `self.cas.put(canonical_encode(&envelope.tx))?` does not match the current `Arc<RwLock<CasStore>>` plus five-argument `CasStore::put` API (`src/state/typed_tx.rs:232`, `src/economy/money.rs:151`, `src/economy/money.rs:169`, `src/state/sequencer.rs:238`, `src/bottom_white/cas/store.rs:163`, `src/bottom_white/cas/store.rs:170`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:126`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:181`).

**R2-Q6 [PASS].** The EscrowVault red line is enforceable as written: there is no existing `EscrowVault` use in `src/state/`. All current references are the module itself/docs/tests, module export, one `q_state` doc mention, and one TB-1 test comment: `src/economy/escrow_vault.rs:66`, `src/economy/escrow_vault.rs:74`, `src/economy/escrow_vault.rs:91`, `src/economy/escrow_vault.rs:138`, `src/economy/escrow_vault.rs:146`, `src/economy/escrow_vault.rs:150`, `src/economy/escrow_vault.rs:283`, `src/economy/escrow_vault.rs:299`, `src/economy/escrow_vault.rs:307`, `src/economy/escrow_vault.rs:321`, `src/economy/escrow_vault.rs:337`, `src/economy/escrow_vault.rs:354`, `src/economy/escrow_vault.rs:367`, `src/economy/escrow_vault.rs:377`, `src/economy/escrow_vault.rs:384`, `src/economy/escrow_vault.rs:393`, `src/economy/escrow_vault.rs:407`, `src/economy/mod.rs:9`, `src/state/q_state.rs:159`, and `tests/tb_1_acceptance.rs:612`. `EscrowVault` is task-keyed and explicitly distinct from the tx-keyed QState escrow projection (`src/economy/escrow_vault.rs:15`, `src/economy/escrow_vault.rs:51`, `src/economy/escrow_vault.rs:53`).

# Section C: NEW P0 List

1. **P0-1: Fix L4.E writer ownership before Phase-1.** Evidence: v2 declares `rejection_writer: Arc<RejectionEvidenceWriter>`, but `append_rejected` is `&mut self` (`handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:106`, `src/bottom_white/ledger/rejection_evidence.rs:258`, `src/bottom_white/ledger/rejection_evidence.rs:259`). Remediation: use `Arc<RwLock<RejectionEvidenceWriter>>` and write-lock for append/read-lock for tests, or refactor the writer to own interior mutability and make `append_rejected(&self, ...)`. Estimated effort: 0.25-0.5 day.

2. **P0-2: Make the rejection append call type-correct.** Evidence: `TypedTx::submitter_id()` returns `Option<AgentId>`, while `append_rejected` requires `AgentId`; v2 passes it directly (`src/state/typed_tx.rs:641`, `src/state/typed_tx.rs:642`, `src/bottom_white/ledger/rejection_evidence.rs:262`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:192`). Remediation: specify the `None` policy for system-emitted/submitterless variants, import/use `HasSubmitter`, and call `append_rejected` with a concrete `AgentId` or explicitly change the L4.E schema to `Option<AgentId>`. Estimated effort: 0.25 day if using a sentinel/WorkTx-only policy, more if changing schema.

3. **P0-3: Correct the WorkTx validation bridge and stake gate syntax.** Evidence: v2's bridge calls `.contains_key` on `EscrowsIndex` / `TaskMarketsIndex` wrappers; v2's stake check compares `StakeMicroCoin` to integer `0` (`handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:126`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:133`, `src/state/q_state.rs:161`, `src/state/q_state.rs:224`, `src/economy/money.rs:151`, `src/economy/money.rs:169`). Remediation: use `.escrows_t.0.contains_key(...)` / `.task_markets_t.0.contains_key(...)` or helper methods, and use `tx.stake.micro_units() > 0` or `tx.stake > StakeMicroCoin::zero()`. Estimated effort: 0.25 day.

4. **P0-4: Rebuild the `TransitionError -> RejectionClass` table against the actual enum.** Evidence: today's enum has `StaleParent`, concrete predicate failure variants, and no `PredicateFailed` / `InvariantViolation`; v2's closed table omits most variants (`src/state/typed_tx.rs:717`, `src/state/typed_tx.rs:787`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:228`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:241`). Remediation: either cover every current and newly added `TransitionError` variant in a closed match, or explicitly allow a wildcard mapping for out-of-WorkTx variants; update the `Display` exhaustive match for any additions. Estimated effort: 0.5 day.

5. **P0-5: Make integration-test L4.E observation explicit.** Evidence: v2's integration fixture plan uses `rejection_writer_for_test()`, but that accessor is specified as `pub(crate)` and therefore invisible to `tests/tb_2_runtime_boundary.rs` (`handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:113`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:115`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:335`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:341`). Remediation: integration tests should retain a cloned writer handle supplied to `Sequencer::new`, or `Sequencer` should expose a public read-only test/support accessor with no mutation capability. Estimated effort: 0.25 day.

6. **P0-6: Align rejection-path CAS writes with the current CAS API.** Evidence: v2 uses `self.cas.put(canonical_encode(&envelope.tx))?`, but current `Sequencer` stores `Arc<RwLock<CasStore>>` and `CasStore::put` requires `&mut self`, bytes, `ObjectType`, creator, logical time, and schema id (`handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:181`, `src/state/sequencer.rs:238`, `src/bottom_white/cas/store.rs:163`, `src/bottom_white/cas/store.rs:170`). Remediation: mirror the accepted-path lock/encode/put shape and decide the rejection-path `ObjectType`, creator, logical timestamp, and schema tag. Estimated effort: 0.25 day.

# Section D: P1 List

1. **Pick one home for `WORKTX_ACCEPT_DOMAIN_V1`.** v2 permits `sequencer.rs` or `q_state.rs`; both are allowed by the touched-files table, but Phase-1 should not leave this to interpretation (`handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:34`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:151`). Estimated effort: 10 minutes.

2. **Name the TypedTx digest helper.** `canonical_hash(tx)` is not an existing `src/state` helper; v3 should define whether this is `sha256(canonical_encode(tx)?)` in `sequencer.rs` or a reusable helper near `canonical_encode` (`src/bottom_white/ledger/transition_ledger.rs:559`, `src/bottom_white/ledger/transition_ledger.rs:562`). Estimated effort: 20 minutes.

3. **Document the integration-test run strategy.** `Sequencer::run` is public but loops until the receiver closes; there is no single-poll API today (`src/state/sequencer.rs:304`, `src/state/sequencer.rs:316`). v3 should say whether tests spawn/abort the loop or add a narrow helper. Estimated effort: 20 minutes.

4. **Disambiguate the two `RejectionClass` enums.** There is a typed-tx status `RejectionClass` and an L4.E ledger `RejectionClass`; v2 intends the latter, so v3 should use the fully qualified import in snippets/tests (`src/state/typed_tx.rs:165`, `src/state/typed_tx.rs:173`, `src/bottom_white/ledger/rejection_evidence.rs:56`, `src/bottom_white/ledger/rejection_evidence.rs:67`). Estimated effort: 10 minutes.

5. **Fix stale field-shape examples in v2.** The v2 struct excerpt shows `cas: Arc<CasStore>` and `ledger_writer: Arc<dyn LedgerWriter>`, but source uses `Arc<RwLock<CasStore>>` and `Arc<RwLock<dyn LedgerWriter>>`; the same section also conflicts with §3.1's claim that constructor signature is unchanged (`handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:85`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:113`, `src/state/sequencer.rs:238`, `src/state/sequencer.rs:241`). Estimated effort: 15 minutes.

# Section E: Recommendation

**Revise preflight v3.** Do not abort the TB-2 charter: the target runtime gap remains real, `EscrowVault` non-use is enforceable, and the replay primitive is public. But v2 is not cleared for STEP_B Phase-1 because its P0-A/P0-B/P0-C/P0-D repairs still have compile-shape blockers at the source boundary. Phase-1 should start only after the v3 fixes above are incorporated.
