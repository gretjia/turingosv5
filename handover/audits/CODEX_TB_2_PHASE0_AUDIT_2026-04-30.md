## Section A: Overall Verdict

**CHALLENGE, conviction 5/5.** I verified repository HEAD as `3f06d512983a2bcb5ebc103b203cdaa7412b6362` with `git rev-parse HEAD`. The preflight is directionally right that TB-2 is necessary: `dispatch_transition` is still all-`NotYetImplemented`, `apply_one` returns before any rejection-evidence write, and `submit_id` is not carried into `apply_one` (`src/state/sequencer.rs:53`, `src/state/sequencer.rs:60`, `src/state/sequencer.rs:339`, `src/state/sequencer.rs:346`, `src/state/sequencer.rs:292`, `src/state/sequencer.rs:293`, `src/state/sequencer.rs:332`). However, Phase-1 should not start from this preflight unchanged because it under-specifies required L4.E writer ownership, assumes a clean `task_id` escrow lookup that the current `QState` shape does not provide, and proposes tests that are not all expressible against the current public/integration-test API surface (`src/state/sequencer.rs:230`, `src/state/sequencer.rs:247`, `src/state/q_state.rs:161`, `src/state/q_state.rs:224`, `src/state/sequencer.rs:47`, `src/state/sequencer.rs:332`).

## Section B: Per-Q Disposition

**Q1 [PASS-with-caveat].** The literal restricted set is exactly `src/{kernel,bus}.rs` plus `src/sdk/tools/wallet.rs` in `CLAUDE.md` and `src/kernel.rs`, `src/bus.rs`, `src/sdk/tools/wallet.rs` in STEP_B (`CLAUDE.md:14`, `handover/ai-direct/STEP_B_PROTOCOL.md:3`). `src/state/sequencer.rs` is not on that literal path list. STEP_B itself adds the institutional trigger: "Also applicable to any proposal that touches \"institution\" per C-031" (`handover/ai-direct/STEP_B_PROTOCOL.md:3`). C-031 does not explicitly mention STEP_B or path expansion; its relevant authority is institutional priority: "先确认制度 (规则/约束/结构) 正确，再调参数" and "`Build right, then tune`" (`cases/C-031_institution_over_tuning.yaml:16`, `cases/C-031_institution_over_tuning.yaml:18`). Verdict: the preflight applicability argument is correct through STEP_B line 3, but C-031 alone is only policy support, not an explicit path-rule authorization.

**Q2 [PASS-with-minor-overstatement].** Section 2's three broken-behavior claims are true at requested HEAD, although the preflight's "HEAD `459c747`" label is stale relative to verified HEAD `3f06d51` (`handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:42`). For (a), every match arm returns `Err(TransitionError::NotYetImplemented)`: Work through TerminalSummary are lines `54` through `60` (`src/state/sequencer.rs:53`, `src/state/sequencer.rs:60`). For (b), `apply_one` dispatches and uses `?`, so any transition error returns before CAS/sign/commit (`src/state/sequencer.rs:339`, `src/state/sequencer.rs:346`); the `log::debug!` happens in `run`, not inside `apply_one` (`src/state/sequencer.rs:308`, `src/state/sequencer.rs:313`). For (c), the queue is `Sender<TypedTx>`, `submit()` allocates `submit_id`, and `try_send` sends only `tx`; `apply_one` receives only `TypedTx` (`src/state/sequencer.rs:236`, `src/state/sequencer.rs:292`, `src/state/sequencer.rs:293`, `src/state/sequencer.rs:332`). The only overstatement is wording: "`apply_one` ... log::debug!s" should be "`run` log::debug!s after `apply_one` returns."

**Q3 [CHALLENGE].** The accepted path is already on `transition_ledger`/`LedgerWriter`: `apply_one` canonical-encodes the `TypedTx`, CAS-puts it, builds `LedgerEntrySigningPayload`, signs it, folds `ledger_root`, commits via `writer_w.commit(&entry)`, stores `next_logical_t`, writes `q_next`, and sets `q_w.ledger_root_t` (`src/state/sequencer.rs:351`, `src/state/sequencer.rs:377`, `src/state/sequencer.rs:386`, `src/state/sequencer.rs:423`). The WorkTx shape mostly supports the validation inputs: it has `task_id`, `parent_state_root`, `agent_id`, `predicate_results`, and `stake` (`src/state/typed_tx.rs:223`, `src/state/typed_tx.rs:235`), and `TypedTx` has `tx_kind()` plus `HasSubmitter::submitter_id()` rather than a `submitter_id` field (`src/state/typed_tx.rs:620`, `src/state/typed_tx.rs:631`, `src/state/typed_tx.rs:641`, `src/state/typed_tx.rs:698`). The cascade risk is real: `EconomicState` exposes `escrows_t` and `task_markets_t`, but both are keyed by `TxId`, while `WorkTx.task_id` is a `TaskId` (`src/state/q_state.rs:143`, `src/state/q_state.rs:149`, `src/state/q_state.rs:161`, `src/state/q_state.rs:224`, `src/state/typed_tx.rs:33`, `src/state/typed_tx.rs:35`, `src/state/typed_tx.rs:225`). A task-keyed `EscrowVault` exists, but it is separate from `QState` and explicitly "distinct from `state::q_state::EscrowEntry`" (`src/economy/escrow_vault.rs:15`, `src/economy/escrow_vault.rs:51`, `src/economy/escrow_vault.rs:53`, `src/economy/escrow_vault.rs:146`, `src/economy/escrow_vault.rs:168`). `assert_total_ctf_conserved(..., &[])` is callable, but non-empty exemptions short-circuit (`src/economy/monetary_invariant.rs:167`, `src/economy/monetary_invariant.rs:179`). `RejectionEvidenceWriter::append_rejected` has the named fields and is callable (`src/bottom_white/ledger/rejection_evidence.rs:258`, `src/bottom_white/ledger/rejection_evidence.rs:268`), but `Sequencer` currently has no rejection writer field or accessor (`src/state/sequencer.rs:230`, `src/state/sequencer.rs:247`). Missing method/field finding: the preflight must specify how L4.E writer ownership and task-id-to-economic-index lookup work before Phase-1.

**Q4 [PASS].** The two-L4-spines concern is technically real as a future-proofing fence, not a current runtime drift. `AcceptedLedger` is an RSP-0 wrapper with an in-memory `Vec` and `current_state_root`, and its own docs say `SystemSignature` attachment and real `Git2LedgerWriter` commit chain are out of scope/deferred (`src/economy/ledger.rs:28`, `src/economy/ledger.rs:32`, `src/economy/ledger.rs:159`, `src/economy/ledger.rs:162`). Its `AcceptedEntry` has seven fields and no system signature (`src/economy/ledger.rs:55`, `src/economy/ledger.rs:72`), while canonical `LedgerEntry` carries `system_signature` and `Git2LedgerWriter` commits one entry per git commit on `refs/transitions/main` (`src/bottom_white/ledger/transition_ledger.rs:68`, `src/bottom_white/ledger/transition_ledger.rs:92`, `src/bottom_white/ledger/transition_ledger.rs:608`, `src/bottom_white/ledger/transition_ledger.rs:629`). Current runtime `apply_one` uses `transition_ledger` and not `AcceptedLedger` (`src/state/sequencer.rs:27`, `src/state/sequencer.rs:30`, `src/state/sequencer.rs:351`, `src/state/sequencer.rs:423`). Observed `append_accepted` call sites are definition/tests and TB-1 integration tests, not runtime spine calls (`src/economy/ledger.rs:189`, `src/economy/ledger.rs:420`, `tests/tb_1_acceptance.rs:149`, `tests/tb_1_acceptance.rs:151`, `tests/tb_1_acceptance.rs:576`). Verdict: red line is valid future-proofing; it is not correcting an existing `apply_one` drift.

**Q5 [CHALLENGE].** No `TypedTx` variant carries a `submit_id` field today. The current u64-shaped fields are `timestamp_logical`, `last_logical_t`, and `total_attempts`, not submit IDs (`src/state/typed_tx.rs:234`, `src/state/typed_tx.rs:247`, `src/state/typed_tx.rs:266`, `src/state/typed_tx.rs:277`, `src/state/typed_tx.rs:309`, `src/state/typed_tx.rs:323`, `src/state/typed_tx.rs:347`, `src/state/typed_tx.rs:349`). Embedding `submit_id` inside `TypedTx` would force a typed ABI change across the enum variants (`src/state/typed_tx.rs:607`, `src/state/typed_tx.rs:616`) and would pollute canonical transaction bytes. A tuple `(u64, TypedTx)` would change the same channel/run/apply_one API surface as a named envelope: `queue_tx`, `new()`'s receiver, `run()`, `submit()`, and `apply_one()` (`src/state/sequencer.rs:236`, `src/state/sequencer.rs:270`, `src/state/sequencer.rs:306`, `src/state/sequencer.rs:291`, `src/state/sequencer.rs:332`). Hidden cost is not cloning: the current code already moves `TypedTx` into `try_send`, and an envelope/tuple would move it likewise (`src/state/sequencer.rs:291`, `src/state/sequencer.rs:293`). Hidden cost is API/test churn and concurrency semantics: `submit_id` is allocated before `try_send`, so under multiple producers a lower ID can be allocated before a higher ID but arrive later; HYPOTHESIS: tests must assert "receipt ID is preserved in the envelope" rather than "queue order is submit_id order" (`src/state/sequencer.rs:292`, `src/state/sequencer.rs:293`, `src/state/sequencer.rs:308`).

**Q6 [PASS].** The `exempt_tx_kinds` red line is technically justified. The implementation explicitly short-circuits on any non-empty exemption list: `if !exempt_tx_kinds.is_empty() { return Ok(()); }` (`src/economy/monetary_invariant.rs:177`, `src/economy/monetary_invariant.rs:179`). The function docs say mints/burns are rejected unless the list is non-empty and normal agent-submitted transitions should pass `&[]` (`src/economy/monetary_invariant.rs:154`, `src/economy/monetary_invariant.rs:166`). Current `src/` production call sites outside the function are absent; observed non-empty usage is a unit test named `ctf_exempt_short_circuits` (`src/economy/monetary_invariant.rs:320`, `src/economy/monetary_invariant.rs:328`). TB-1 integration calls pass `&[]` (`tests/tb_1_acceptance.rs:355`, `tests/tb_1_acceptance.rs:382`, `tests/tb_1_acceptance.rs:445`, `tests/tb_1_acceptance.rs:540`). Verdict: this red line prevents future regression, not existing runtime misuse.

**Q7 [CHALLENGE].** The 12-test battery is not fully expressible as written against current surfaces. Tests 1-2 need `SubmissionEnvelope` and/or `apply_one(envelope)`, but `apply_one` is `pub(crate)` and an integration test under `tests/` cannot call it (`src/state/sequencer.rs:332`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:130`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:131`). Tests 3-5 need WorkTx predicate/stake/task fixtures, which are structurally constructible from `WorkTx` fields (`src/state/typed_tx.rs:223`, `src/state/typed_tx.rs:235`), but L4.E row counting/matching needs a runtime-connected `RejectionEvidenceWriter`, which `Sequencer` does not own today (`src/state/sequencer.rs:230`, `src/state/sequencer.rs:247`, `src/bottom_white/ledger/rejection_evidence.rs:237`, `src/bottom_white/ledger/rejection_evidence.rs:245`, `src/bottom_white/ledger/rejection_evidence.rs:327`, `src/bottom_white/ledger/rejection_evidence.rs:340`). Test 6 is blocked as written: the preflight's "supply-delta-injection" fixture is not a WorkTx field, and WorkTx carries no requested post-state/economic delta (`handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:140`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:158`, `src/state/typed_tx.rs:223`, `src/state/typed_tx.rs:235`). Tests 7-8 can observe `next_logical_t_peek()` and `q_snapshot()`, but only after the runtime rejection path and L4.E writer are wired (`src/state/sequencer.rs:430`, `src/state/sequencer.rs:443`). Tests 9-11 can observe `state_root_t`, `ledger_root_t`, and `next_logical_t`, and `QState` exposes those fields (`src/state/q_state.rs:313`, `src/state/q_state.rs:317`, `src/state/sequencer.rs:430`, `src/state/sequencer.rs:443`); however, the exact interim hash is not implemented anywhere yet and the proposed domain string appears only in planning docs (`handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:148`, `handover/tracer_bullets/TB-2_charter_2026-04-30.md:56`). Test 12 again requires a runtime-connected L4.E writer (`src/state/sequencer.rs:230`, `src/state/sequencer.rs:247`). The preflight should either move some tests into `sequencer.rs` unit tests, expose carefully scoped APIs, or revise the battery.

**Q8 [CHALLENGE].** Adversarial sweep found four missed risks. First, rejection path atomicity is under-specified: current accepted path already writes CAS before sign/commit and can leave orphan CAS objects on later failure, while the proposed rejection path adds CAS writes plus L4.E append (`src/state/sequencer.rs:351`, `src/state/sequencer.rs:363`, `src/state/sequencer.rs:417`); current `append_rejected` is infallible and in-memory, so the immediate rollback risk is limited, but persistence semantics are deferred (`src/bottom_white/ledger/rejection_evidence.rs:30`, `src/bottom_white/ledger/rejection_evidence.rs:34`, `src/bottom_white/ledger/rejection_evidence.rs:258`, `src/bottom_white/ledger/rejection_evidence.rs:268`). Second, multi-producer submit order is not specified: `fetch_add` occurs before `try_send`, and `run` processes receiver arrival order (`src/state/sequencer.rs:292`, `src/state/sequencer.rs:293`, `src/state/sequencer.rs:308`); HYPOTHESIS: submit ID monotonicity must not be used as an arrival-order guarantee under contention. Third, replay determinism requires all accept-side effects to be represented in L4/QState because `replay_full_transition` re-fetches CAS payloads, re-runs `dispatch_transition`, checks `q_next.state_root_t`, and advances `q` from `q_next` (`src/bottom_white/ledger/transition_ledger.rs:371`, `src/bottom_white/ledger/transition_ledger.rs:389`, `src/bottom_white/ledger/transition_ledger.rs:442`, `src/bottom_white/ledger/transition_ledger.rs:486`); apply_one currently ignores `_signals`, so any TB-2 signal side channel would be UNVERIFIED for replay unless ledgered or declared out of scope (`src/state/sequencer.rs:341`, `src/state/sequencer.rs:346`). Fourth, the proposed `turingosv4.worktx.accept.v1` state-root domain is not registered in source or genesis; observed genesis anchors constitution and registry roots only, while the only existing state-root toy domain is `turingosv4.l4_state_root.v1` in `AcceptedLedger` (`genesis_payload.toml:118`, `genesis_payload.toml:127`, `genesis_payload.toml:133`, `src/economy/ledger.rs:350`, `src/economy/ledger.rs:357`). This is not a charter-killer, but it should be made an explicit constant and documented as an interim domain.

## Section C: P0 List

1. **Specify and wire runtime L4.E writer ownership before Phase-1.** Evidence: `Sequencer` fields include submit/logical counters, `queue_tx`, CAS, keypair, epoch, `ledger_writer`, registries, and `q`, but no rejection writer (`src/state/sequencer.rs:230`, `src/state/sequencer.rs:247`); `append_rejected` is available but disconnected (`src/bottom_white/ledger/rejection_evidence.rs:258`, `src/bottom_white/ledger/rejection_evidence.rs:268`). Remediation: revise the preflight to add the exact `Sequencer` field, constructor parameter, and read-only test accessor for `RejectionEvidenceWriter`, or explicitly route to an existing owner if one exists. Estimated effort: 0.5 day. Scope note: likely `sequencer.rs` plus tests only, unless constructor call sites outside tests appear in Phase-1.

2. **Resolve the `TaskId` versus `TxId` economic lookup mismatch.** Evidence: WorkTx carries `task_id: TaskId`, but `EconomicState.escrows_t` and `task_markets_t` are `BTreeMap<TxId, ...>` (`src/state/typed_tx.rs:225`, `src/state/q_state.rs:161`, `src/state/q_state.rs:224`). A task-keyed `EscrowVault` exists, but it is separate from `QState` and documented as distinct from the L4-state projection (`src/economy/escrow_vault.rs:15`, `src/economy/escrow_vault.rs:53`, `src/economy/escrow_vault.rs:146`, `src/economy/escrow_vault.rs:168`). Remediation: either pre-register a deterministic bridge such as `TxId(tx.task_id.0.clone())`, add a `q_state` helper/index change, or make `EscrowVault` a sequencer dependency. Estimated effort: 0.5 day for bridge, 1 day if `q_state.rs` changes. Scope note: any helper/index change outside `sequencer.rs` is itself a Phase-0 finding against the current "sequencer.rs primary" framing.

3. **Make the acceptance battery compile-expressible.** Evidence: `dispatch_transition` and `apply_one` are `pub(crate)`, but the preflight places tests in `tests/tb_2_runtime_boundary.rs` and explicitly wants an `apply_one` signature typecheck test (`src/state/sequencer.rs:47`, `src/state/sequencer.rs:332`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:124`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:131`). Evidence: Test 6's `supply-delta-injection` is not a WorkTx field (`handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:158`, `src/state/typed_tx.rs:223`, `src/state/typed_tx.rs:235`). Remediation: split private-API tests into `sequencer.rs` unit tests, expose only necessary public/test accessors, and rewrite the post-init mint test to use an actually representable transition or mark it as a lower-level monetary-invariant unit. Estimated effort: 0.5-1 day.

4. **Define concrete error and rejection-class mapping.** Evidence: `TransitionError` has `StakeInsufficient`, `TaskNotFound`, and `NotYetImplemented`, but no `EscrowMissing` or `PostInitMint` variants (`src/state/typed_tx.rs:717`, `src/state/typed_tx.rs:787`). `RejectionEvidence::RejectionClass` has coarse `PredicateFailed`, `PolicyViolation`, `EscrowMissing`, and `InvariantViolation`, not the exact `StakeRequired`/`PostInitMint` names in the battery (`src/bottom_white/ledger/rejection_evidence.rs:56`, `src/bottom_white/ledger/rejection_evidence.rs:67`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:138`, `handover/ai-direct/TB-2_SEQUENCER_RUNTIME_CLOSURE_2026-04-30.md:140`). Remediation: pre-register `TransitionError -> RejectionClass` mapping and adjust expected test names/classes, or explicitly approve new `TransitionError` variants in `typed_tx.rs`. Estimated effort: 0.25-0.5 day. Scope note: new `TransitionError` variants force an edit outside `sequencer.rs`.

## Section D: P1 List

1. **Clarify STEP_B institutional authority wording.** STEP_B line 3 is sufficient, but C-031 itself is not an explicit STEP_B path rule (`handover/ai-direct/STEP_B_PROTOCOL.md:3`, `cases/C-031_institution_over_tuning.yaml:16`, `cases/C-031_institution_over_tuning.yaml:18`). Estimated effort: 10 minutes.

2. **Justify `SubmissionEnvelope` over tuple `(u64, TypedTx)`.** A tuple changes the same channel/run/apply_one surface, while a named struct is clearer and extensible (`src/state/sequencer.rs:236`, `src/state/sequencer.rs:306`, `src/state/sequencer.rs:332`). Estimated effort: 15 minutes.

3. **Add a concurrency note for submit IDs.** `fetch_add` precedes `try_send`, so ID allocation order is not necessarily receiver arrival order under multi-producer scheduling (`src/state/sequencer.rs:292`, `src/state/sequencer.rs:293`, `src/state/sequencer.rs:308`). Estimated effort: 20 minutes.

4. **Name and document the interim state-root hash domain.** `turingosv4.worktx.accept.v1` appears only in planning docs, while source has `turingosv4.l4_state_root.v1` as the TB-1 toy mutator domain (`handover/tracer_bullets/TB-2_charter_2026-04-30.md:56`, `src/economy/ledger.rs:350`, `src/economy/ledger.rs:357`). Estimated effort: 15 minutes.

5. **Document orphan-CAS behavior on failed rejection/accepted writes.** CAS `put` is durable before later accepted-path commit steps, and rejection-path CAS plus L4.E append will have similar partial-write considerations (`src/bottom_white/cas/store.rs:162`, `src/bottom_white/cas/store.rs:195`, `src/state/sequencer.rs:351`, `src/state/sequencer.rs:363`, `src/state/sequencer.rs:417`). Estimated effort: 30 minutes.

## Section E: Recommendation

**Revise preflight before Phase-1.** Do not reject the TB-2 charter: the runtime gap is real, the accepted path already has the canonical `transition_ledger` machinery, and the `AcceptedLedger` red line is correct future-proofing (`src/state/sequencer.rs:351`, `src/state/sequencer.rs:423`, `src/economy/ledger.rs:28`, `src/economy/ledger.rs:32`). Do not proceed to Phase-1 until the P0s above are resolved in the preflight: L4.E writer ownership, task/escrow lookup semantics, compile-expressible test plan, and concrete error-class mapping.
